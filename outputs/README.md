# Local outputs

All generated files here are ignored; only this README is tracked. Keep future
runs in unique directories, with protocol/code versions, configuration, source
manifest reference, environment, command, seed where relevant, exclusions,
results, uncertainty, deviations, and errors. Do not overwrite unfavorable runs.

Record a non-sensitive result or failure summary in `docs/research_log.md`.
Review derived records, figures, and tables for release eligibility before
publishing them elsewhere. Synthetic software checks must never be presented as
real-match evidence. There are no analysis outputs yet.
