# Session 06 reserved evaluation decision brief

**Decision:** **D — EXECUTION INVALID** for the primary M1-versus-M0 question and
**4 — EXECUTION INVALID** for the secondary M2-versus-M1 question.

The protected evaluation did not reach population construction or scoring. The
first reserved alias's metadata and Dynamic Events products passed pinned Git
object verification and were stored as ignored opaque bytes. Acquisition then
stopped while verifying the tracking Git object, with the exact reported failure
`Git blob hash or size mismatch`. No tracking payload was stored, no reserved
product was parsed, and no utility, rank, or performance metric was calculated.

The access ledger records this as protected-value exposure because the two
verified opaque products were acquired. The frozen protocol therefore prohibits
an implementation correction or automatic rerun in this execution. The required
next direction is one bounded execution/integrity review; any rerun requires a
separate prospective authorization.

## Evidence map: 62 requested report items

1. **Starting checkpoint:** verified clean and synchronized at
   `74ff7b2d29caf1b1f1cb57e3c8770f6f36075849` before Session 6 work.
2. **Source revision:** fixed at
   `02a396ffd09b283c9f092fdedeff11da6d535b66`.
3. **Primary question:** M1 versus M0 on the reserved set was frozen but remains
   unanswered.
4. **Secondary question:** M2 versus M1 on the reserved set was frozen but
   remains unanswered.
5. **Reserved membership:** exactly `1874553`, `1927964`, `1959846`, `1986691`,
   `1996436`, `2006363`, `2007448`, `2007721`, `2010085`, and `2016236`.
6. **Withheld membership:** `1953632` remained unopened.
7. **Reservation qualification:** Prospectively reserved from this project stage
   forward. Session 1 schema inventory mechanically over-read post-header bytes
   into process memory, but no evidence indicates value-level content was
   surfaced, persisted, or used analytically.
8. **Protocol checkpoint:** Phase 6 was committed first as `903b8a02`.
9. **Protocol identity:** SHA-256
   `672457c8b563cd048889ae81bc68a950958d3224611cc3231046799469e54e28`.
10. **Implementation checkpoint:** implementation and final development models
    were committed as `0659515`.
11. **Pre-access validation correction:** the first preparation call stopped
    before any provider request because the wrapper expected an unrecorded
    `success` key rather than checking the fitter's actual frozen evidence.
12. **Correction checkpoint:** the validation-only correction and regenerated,
    scientifically identical authority were committed separately as `48a4a48`;
    no commit was amended or squashed.
13. **Implementation identity:** the committed runner SHA-256 is
    `60583c0c3254bafad967257065ec1e15ded5689d35be04ebf5647651522e6f83`.
14. **Final-model identity:** `final_development_models.json` SHA-256 is
    `0383561e45bcd9f8d84eccc7e9733b2409536f61c39193fa87d08c36c6f6fd65`.
15. **Closed-authority checks:** Session 2/3/4/5/6a manifests and bound outputs
    passed their hash checks before protected access.
16. **Development adapter replay:** byte equality with the Session 3 population
    passed.
17. **Development population hash:** reproduced
    `cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`.
18. **Development evaluation eligibility:** reproduced at 7,227.
19. **Development fit eligibility:** reproduced separately at 7,227.
20. **Population equivalence:** per-match counts, exclusions, target-outside
    reasons, ordering, and hashes matched Session 3 authority.
21. **Final M0 fit:** completed on all nine development matches and passed rank,
    separation, finiteness, convergence, and gradient gates.
22. **Final M1 fit:** completed on all nine development matches and passed the
    same gates.
23. **Final M2 fit:** completed on all nine development matches and passed the
    same gates, including nonzero within-choice attenuation information.
24. **Model nesting:** raw features, preprocessing, and standardized shared
    columns matched exactly.
25. **Training isolation:** only the authoritative development population was
    used to fit the final models.
26. **Environment:** Python 3.13.15, NumPy 2.5.3, and SciPy 1.18.1 were frozen in
    the final-model authority; no dependency was installed or upgraded.
27. **Projection boundary:** synthetic sentinel tests confirmed that unapproved
    metadata, event, and tracking fields are discarded.
28. **Identity contract:** synthetic tests covered invalid, padded, blank, and
    conflicting identities, valid fallback carriers, duplicates, team/roster
    resolution, and malformed intervals.
29. **Partition firewall:** synthetic tests rejected development, withheld,
    traversal, symlink, and unauthorized product requests from the reserved
    acquisition path.
30. **Preparation/scoring separation:** static tests proved preparation has no
    scoring or fitting call and scoring has no acquisition, parsing, or fitting
    call.
31. **Execution marker:** exclusive-create behavior and persistence were tested.
32. **Synthetic feature tests:** strict M0/M1/M2 nesting and frozen attenuation
    behavior passed through the active test suite.
33. **Timing tests:** strict same-period prior-frame selection and the 100 ms
    cadence-based rule passed; the tolerance remains a draft, not timing truth.
34. **Candidate independence:** tests confirmed target and outcome values do not
    control candidate membership.
35. **Target-outside semantics:** tests retained valid outside targets for zero
    evaluation credit and separate fit ineligibility.
36. **Active tests:** both the pre-access and post-failure runs recorded 79
    passes and 3 planned scaffold skips.
37. **Compilation:** all active source, script, and test modules compiled.
38. **Pre-access publication check:** the committed final development-model
    artifact passed.
39. **First protected access:** began under implementation commit `48a4a48` and
    protocol commit `903b8a02`.
40. **Processing order:** access began with `reserved_01`, the first alias in
    ascending provider-ID order.
41. **Metadata acquisition:** its pinned ordinary Git object passed hash and size
    verification and was stored only in ignored local storage.
42. **Dynamic Events acquisition:** its pinned ordinary Git object passed hash
    and size verification and was stored only in ignored local storage.
43. **Tracking acquisition:** stopped during Git-object verification before LFS
    pointer acceptance or payload download.
44. **Exact integrity failure:** `Git blob hash or size mismatch`.
45. **Failure granularity:** the frozen exception does not retain expected and
    observed components separately, so whether hash, size, or both differed is
    unavailable without a new integrity review.
46. **Access ledger:** start/completion records exist for metadata and events;
    start/failure records exist for tracking.
47. **Parsing status:** no reserved metadata, event, or tracking product entered
    the projected application schema.
48. **Population status:** no reserved choice-set population was constructed.
49. **Population summary:** unavailable and intentionally absent.
50. **Reserved aliases reached:** only `reserved_01`; `reserved_02` through
    `reserved_10` were not requested.
51. **Raw pass-attempt counts:** unavailable.
52. **Target completeness and usability:** unavailable.
53. **Exclusion waterfall and overlapping QC:** unavailable.
54. **Evaluation and fit denominators:** unavailable.
55. **Target-outside counts and candidate/defender distributions:** unavailable.
56. **Protected scoring:** never started; no execution marker or result files
    exist.
57. **Primary metrics:** M0/M1 MRR, Hit@1, Hit@3, paired differences, mean,
    median, signs, and tie counts are unavailable.
58. **Secondary metrics:** M2/M1 MRR, Hit@1, Hit@3, paired differences, mean,
    median, signs, and tie counts are unavailable.
59. **Result manifest:** unavailable because no aggregate package was created;
    consequently there is no result-manifest self-hash.
60. **Interpretation:** primary **D — EXECUTION INVALID** and secondary
    **4 — EXECUTION INVALID**; neither model comparison has protected evidence.
61. **Claim boundaries:** vendor targets retain Tier B limitations,
    accessibility remains **PROXY ONLY**, suppression remains **NOT SUPPORTABLE**,
    and extrapolated tracking remains an offline input without proven real-time
    availability.
62. **Next direction:** conduct exactly one bounded execution/integrity review of
    the tracking Git-object/LFS association. Any protected rerun requires a new
    prospective protocol and explicit authorization.

## Closure

The two acquired opaque products, partial acquisition directory, and append-only
access ledger remain ignored and preserved. No reserved passage, player/event
identity, coordinate, timestamp, candidate, utility, rank, or outcome was
inspected. No model was tuned, refit, or scored after access. Session 6 stops
here without Session 7 work.
