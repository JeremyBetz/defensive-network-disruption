# Session 14R9T — Canonical binary64 transition contract repair

Closed under the [frozen protocol](protocols/phase_14r9t_binary64_transition_contract_repair.md).

**A / readiness 1 / execution valid**, with final delivery conditional on the
closure CI and synchronization reported in the delivery handoff. The single
governed acceptance resolved the retained 65-probe trace with a complete
21-float terminal suffix, then completed the authorized expanding-candidate
path and every unchanged downstream gate. Runtime canonicalization matched the
saved-trace resolution exactly. No repair, fixture adjustment or rerun followed
exposure.

This is a finite representation convention supported by a separately retained
simple-crossing authority. It establishes neither stability beyond the inspected
window nor equality between the canonical binary64 coordinate and the
mathematical root. R9S remains BC / pattern F / PA / diagnostic readiness 1;
its historical operational repair status and all prior artifacts are preserved.
The phase provides no scientific field comparison or behavioral validation.

## Contract and preservation

The new internal canonicalizer preserves R9R's initial window and widening rule.
It inspects the complete visited windows, validates chronological cache misses
and the final cache, and selects the earliest float in the complete strict
post-sign terminal suffix. The suffix must contain at least two adjacent floats.
It requires a preceding observation, a mathematically valid pre-side witness,
and a canonical point and immediate successor strictly inside the structural
region. Widening solely to obtain a passing suffix is prohibited.

Both signed zeros interrupt the strict suffix; their bits remain private.
Legacy zero metadata is populated only for an entirely observed contiguous zero
gap after the last strict pre-sign witness. Other zero observations remain in
separate trace evidence. Production oscillation and rounded zeros do not create
mathematical interval equality. Multiway consistency, distinct root ordering,
onset/tie constraints and the unchanged owner rule remain blocking requirements.

The adapter uses normal imports and explicit composition. Mathematical
localization, production joint Simpson, degenerate-edge behavior, owner
certification, strict/repeat verification, the R9K structural comparator,
micro-residual routing, certificates and whole-integral gates are unchanged.
No empirical identifier selects repair behavior. Partial overlap of the
retained mathematical enclosure with R9Q's smaller excluded region is preserved
as a coordinate relationship, not a root assertion in that excluded region.

All 84 inherited committed-file bindings passed. Historical source and output
bytes, README, claim ledger, dependencies, release metadata and root-package
exports remain unchanged. Exact accepted structures, owners, partitions,
component order and resolutions were preserved across the complete synthetic
authority. The inherited 64-epsilon comparison was used only for corresponding
finite final components.

## Observed acceptance and exposure

All **15 trace controls** passed their frozen outcomes, including expected
rejections, the two explicit arithmetic expressions, separately bounded nearby
crossings, duplicate fields and root-free routing. All **10 negative controls**
blocked. Signed-zero, pair-reversal, determinism, serialization and forged-record
checks passed in the discoverable tests.

The actual R9T adapter completed **108/108 cases, 366/366 references and 399/399
mapped permutations**, compared with unchanged R9R and R9K executions. These
governed observations are retained in
[historical_regressions.csv](../outputs/continuous_occlusion_binary64_transition_contract_repair/historical_regressions.csv).
Earlier local and CI repetitions were validation replays.

After all checkpoint gates passed, the create-once acceptance first ran its
synthetic and publication controls. It then hash-checked the allowlisted R9S
records and reviewed the complete 65-probe trace without field evaluation.
The pure rule resolved a **21-float terminal suffix**, with **zero additional
trace probes**. Only then did the runner materialize R9S's selected-edge copy
and invoke the repaired expanding candidate once. Its inherited internal
determinism checks remained part of that invocation.

Separate synchronized trace and geometry receipts confirm **one retained trace
review and one retained state / one retained edge reopened**, with no exposure
uncertainty. One diagnostic candidate completed. Ordinary three-candidate edge
and state completion counts remain **zero**. No population, provider source,
prepared population, other edge or candidate, target, outcome, model, share or
partial scientific summary was accessed.

Owner certification, canonical partition construction, strict/repeat agreement,
the repaired structural comparator, direct Simpson and joint-maximum agreement
all passed. Strict and repeat verification each retained 14 ordinary pieces,
zero micro-residual pieces, zero adaptive/blocking warnings and zero certificate
pieces, with one outward aggregation level. No warning was suppressed, no
certificate was generated, and no tolerance or estimator changed.

R9Q/R9R/R9S did not supply completed downstream results for this edge. An exact
retained before/after comparison of those unavailable quantities is therefore
**unavailable**. Preservation there is supported by unchanged source bindings
and the complete synthetic regressions, not invented historical estimates.
Exact retained values, bits, geometry, roots, owners and traces remain private.

## Publication and performance

All five publication controls passed: success, pre-access failure, active
numerical failure, publication initialization failure and publisher failure.
Legacy replay tripwires, synchronized original tracebacks, independent publisher
failure preservation and rerun rejection passed. The actual successful
single-candidate closure used `diagnostic_completed` / `diagnostic_success`,
one R9J linear review and immutable authority reuse. It did not replay a
historical journal or claim a completed three-candidate empirical edge.

Exactly ten public artifacts passed persisted schema, hash, finite-value and
cross-file validation. Checking rederived decisions from stored observations
without field evaluation, missing-evidence reconstruction or journal replay.
The private index binds **521 records**.

- Private-index SHA-256: `128d32e3b00ec009e87daa76e9dcc605db8ad8eb4cb8b639c447dfb9a7d8a068`.
- [Manifest](../outputs/continuous_occlusion_binary64_transition_contract_repair/manifest.json)
  SHA-256: `46a287059a94ae648254125771c06d8981de1f29b26f2c235ae86c25230c4c22`.

The governed clock recorded **447.526924 seconds** wall time: 445.821613 seconds
exclusive process computation, 0.356090 seconds recorded observation I/O,
zero recorded waiting and 1.349221 seconds unattributed overhead. Inclusive
numerical timing was 446.491393 seconds and is not added to those exclusive
categories. Uninstrumented startup, I/O and closure work is not reassigned to a
numerical stage. CI waiting and validation suites are outside this clock.

The recorded work spans synthetic acceptance and the retained invocation:
284 localization calls, 18,460 inspected floats summed across inspections,
zero topology-classification subdivisions, maximum recorded topology depth zero
and zero unresolved regions. These are not 18,460 distinct empirical floats.
Isolation iteration counts are not separately reported. The complete governed
execution stayed within the 3,600-second total and 600-second operation limits;
the inherited per-inspection distinct-float cap was unchanged.

## Validation and three-commit chronology

The exact implementation checkpoint passed:

- Focused R9T: **32 run / 32 passed / 0 skips** (453.192 seconds).
- Relevant R9J/R9K/R9M/R9P/R9Q/R9R/R9S: **185 run / 185 passed / 0 skips**
  (444.811 seconds).
- Full local: **1,241 run / 1,238 passed / 3 retained skips** (2,401.331 seconds).
- Clean tracked-only Git checkout: **1,241 run / 1,238 passed / 3 retained skips**
  (2,385.936 seconds), without ignored empirical fixtures.

Earlier development full and tracked-only runs each completed 1,240 tests:
1,237 passed and 3 retained skips. The final trace-only closure regression accounts
for the additional test; the exact committed checkpoint was subsequently tested
in full locally and in the clean checkout. Compilation, distribution audit,
authority hashes, privacy/access guards and historical-byte checks passed.
Persisted publication, documentation links, append-only history, staged inventory
and diff checks were performed for closure. No source or fixture changed after
governed exposure.

[Checkpoint CI run 35537355117](https://github.com/JeremyBetz/defensive-network-disruption/actions/runs/35537355117)
passed distribution, Python 3.11 and Python 3.13 on the exact implementation
commit. Each Python job ran 1,241 tests: 1,238 passed and 3 retained skips.
Test-suite times were 2,546.697 seconds and 5,745.551 seconds respectively.
The immutable R9M receipt was captured only after all three jobs succeeded:
`3f9c02c2a12ba02cad426e130de98b7d4e11bd1b977e2ace9aa8331572d7deeb`.
Offline preflight and fresh-marker checks then passed before the one acceptance.

The chronology is exactly three commits:

1. `e9e0920781cd05e8af2464cd86b4fc91437fcc37` —
   `research: freeze binary64 transition contract repair`.
2. `9b0d931a4e402360ea50b3a42f6a3db3b648b1dc` —
   `fix: canonicalize oscillatory binary64 transitions`.
3. The commit containing this report —
   `research: close binary64 transition contract repair`.

Published history was neither amended nor force-pushed. The closure commit's
identifier, final CI and synchronized heads are supplied in the delivery handoff
to avoid self-referential commit hashes.

## Twenty-two-item handoff

1. **Starting HEAD:** `7ad2d95231355801d2c6cb9f4ce6bfac0aa5bd2d`.
2. **Ending HEAD:** the closure commit containing this report; verified full hash
   in the delivery handoff.
3. **Protocol:** linked above; commit `e9e0920781cd05e8af2464cd86b4fc91437fcc37`;
   SHA-256 `f836d89afef8f4d053c20f4daf4d2c622a38e6ed91a5561d1c063931d7a93597`.
4. **Implementation:** `9b0d931a4e402360ea50b3a42f6a3db3b648b1dc`.
5. **Closure:** commit containing this report, with the exact required message.
6. **Rule:** complete strict post-sign terminal suffix of at least two floats,
   valid pre-side authority, strict structural confirmation, no confirmation-only
   widening, observed zero metadata only and preserved mathematical routing.
7. **Synthetic controls:** 15/15 frozen outcomes passed; engineering and field
   fixtures remain distinguished.
8. **Negative controls:** 10/10 blocked; no weakened acceptance.
9. **Retained trace:** 65 probes complete; 21-float suffix; resolved with no new
   field probes. Exact transition and bits are private and hash-bound.
10. **Retained expanding path:** one invocation completed; runtime resolution
    matched the saved trace and every downstream numerical gate passed.
11. **Historical regression:** 108/108 cases, 366/366 references, 399/399 mapped
    permutations; required exact structure and finite-float contracts passed.
12. **Production preservation:** unchanged estimator/source and synthetic vectors,
    component order and accepted resolutions; missing historical downstream
    retained quantities remain unavailable.
13. **Comparator/piecewise preservation:** unchanged R9K comparator and strict/repeat
    routes; synthetic comparisons and the complete retained gates passed.
14. **Tolerance/certificate preservation:** unchanged settings and registry; no new
    certificate, suppression or empirical certificate use. Warning counts were zero.
15. **Publication:** five controls passed; one actual linear review, immutable
    authority reuse, diagnostic-only completion and valid persisted ten-file closure.
16. **Performance:** counts and scoped timings above; no limit was increased.
17. **Classification:** A, supported by validated acceptance observations and
    conditioned on final delivery checks.
18. **Readiness:** 1 after final green closure CI and synchronized delivery; it
    authorizes no automatic empirical execution.
19. **Tests/checks:** separate run/pass/skip totals above; all required local and
    persisted-evidence checks passed.
20. **CI:** checkpoint's three required jobs green; final closure CI is verified
    and linked in the delivery handoff.
21. **Repository/release:** historical bytes preserved; final clean local/tracking/live
    synchronization is verified in the handoff. `v0.1.0` remains at
    `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
22. **Sole recommended next governed action:** separately govern one fresh empirical
    continuous-occlusion representation retry using the repaired switch localization
    and canonical binary64 transition contract, R9K comparator, R9J linear
    publication validator, and R9M local checkpoint-CI authority.

No recommendation was executed. Accessibility remains **PROXY ONLY** and
suppression **NOT SUPPORTABLE**; carrier-position proxy semantics and all causal,
behavioral and value limitations remain unchanged.
