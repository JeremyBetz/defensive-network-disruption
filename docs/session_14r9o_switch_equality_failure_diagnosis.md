# Session 14R9O — Switch-equality and failure-closure diagnosis

2026-09-19. **Numerical NF; publication PD; readiness 4.** The single governed
execution stopped at a retained-authority discrepancy before geometry access.
R9N remains D/readiness 4. No numerical or scientific conclusion follows.

## Outcome and qualification

The approved R9O plan and committed protocol required a 30,881-record R9N
journal. The unchanged R9J linear validator read the journal once and validated
its canonical encoding, chain and frozen raw SHA-256, but counted **31,161
records**. The frozen raw hash matched
`ab5600ba8eea54092996bb2f4c613c76a5ba331c25c2c9d2b7bb65fa6e9555db`.
This is an authority-specification discrepancy, not demonstrated journal
corruption. The protocol treated a count mismatch as a shared authority failure,
so both branches stopped before selective materialization or governed controls.
The count was neither silently corrected nor used to authorize a second attempt.

Exactly zero states and zero edges were reopened; exposure uncertainty is false.
The review marker exists privately. No numerical marker, access attempt, or
materialization receipt was created. The independent expanding-field reference,
empirical failure reproduction and root/switch interpretation remain unavailable.
The eleven-file diagnostic package is hash-closed and passes its publication
checker. Package integrity does not mean the diagnostic execution was valid.

## Tested prospective tooling

The new diagnostic module provides exact-input expanding-field bounds, synthetic
localization characterization, selective prepared-edge extraction, observational
capture and the frozen 150-minute reproduction / 240-minute total budget.
No production numerical implementation was modified. No empirical reference was
computed and no new certificate was created.

Static inspection established the historical routing chain:
R9N `close_failure` → R9M original traceback capture → `Progress.failure` →
`core.snapshot` → historical numerical replay → repeated exposure-prefix replay.
The historical method attempts that snapshot before appending the terminal event
and again afterwards. R9N's later explicit R9J review did not prevent those calls.
The same historical progress class also snapshots before success.

The prospective terminal adapter directly appends the compatible terminal event,
then invokes R9J once and passes its immutable authority to persisted-package
validation. R9M captures the original traceback before journal handling. Synthetic
controls cover success, pre-access failure, active numerical failure,
publication-initialization failure and publisher failure. Tripwires make calls to
historical failure/success/snapshot/replay methods fatal during these controls.
A separate test observes exactly one R9J review. Traceback tampering and repeated
terminal closure are rejected. Original failure evidence survives publisher
failure. These are development-test results, not an accepted governed R9O
publication repair: the shared authority gate stopped the governed controls, so
**PA is not claimed** and publication remains **PD**.

## Validation

- Initial focused diagnostics: 10 run, 10 passed, 0 skipped, after correcting a
  temporary-directory test path before governed execution.
- Focused diagnostic and terminal suite: 17 run, 17 passed, 0 skipped; repeated
  after closure with the same result.
- Relevant R9 suite: 204 run, 204 passed, 0 skipped. This run began before two
  additional runner tests were added.
- Clean tracked-only Git checkout: 206 run, 206 passed, 0 skipped, including the
  two additional runner tests.
- Inherited switch-certification suite: 22 run, 22 passed, 0 skipped.
- Full active suite: 1,111 run, 1,108 passed, 3 retained skips. It began before the
  two additional tests; those passed separately and in the clean checkout.
- An archive-only portability attempt ran 206 tests: 203 passed, 3 errors,
  0 skipped. Existing tests needed Git metadata; the clean Git checkout above
  resolved that validation-environment limitation without source changes.

The full suite retained an emitted historical SciPy roundoff warning; passing
tests do not relabel it warning-free. All test replays were synthetic validation.
Compilation, finite JSON, exact public inventory, existing public/private hashes,
privacy scans, links, historical-byte preservation, staged inspection and diff
checks were performed. The diagnostic checker reads persisted evidence and does
not repeat the retained journal review or regenerate missing artifacts.

Checkpoint CI run 35444442179 and final closure CI are delivery gates; their
terminal results are reported in final delivery. Starting R9N CI run 35425846490
was independently verified green on all three required jobs before mutation.

## Twenty-item handoff

1. Starting HEAD: `a78b32fa824226fc3ac5ac269fdea4354374801c`.
2. Ending HEAD: the closure commit, reported after push and synchronization.
3. Retained failing-edge authority: frozen R9N journal/prepared/failure hashes
   in the [protocol](protocols/phase_14r9o_switch_equality_failure_diagnosis.md).
   Private linear-review receipt hash:
   `37a945ee1a7ec93a15f4fe7ba3456d4a233f6a5bc1d27c2d92866e8effeef08d`.
   Geometry authorization was not reached.
4. Exact failure reproduced: unavailable; no empirical numerical call executed.
5. Equality semantics: source inspection identifies exact binary64 pair
   subtraction and an assumed monotone sign transition; the empirical mechanism
   remains unavailable. Owner-block tolerance is a different contract.
6. Competing-function reference: unavailable; no empirical reference calculation.
7. Root/switch classification: unavailable; no certified-root claim.
8. Synthetic controls: characterized by focused tests; governed controls not run.
9. Numerical classification: **NF — insufficient evidence**.
10. Prospective numerical repair: unavailable; no diagnosis supports a repair yet.
11. Legacy publication call chain: documented above from unchanged source.
12. Routing defect: historical terminal progress methods reconstruct snapshots
    before the wrapper's explicit linear review.
13. Linear-route regression: five synthetic closure paths pass; one-review,
    legacy-tripwire, traceback and rerun controls pass in development tests.
14. Publication classification: **PD — unresolved** for governed acceptance.
15. Tests/checks: counts above; errors and retained skips reported separately.
16. CI: starting authority green; checkpoint/final delivery results reported
    separately after terminal completion.
17. Commits: protocol `49eaa2d`; diagnostic tooling `44a6181`; prospective terminal
    adapter `f13eb282889d4fc49dda56ba4c53816a14cb2bd6`; closure follows this report.
18. Clean/sync state: verified before execution; final verification follows push.
    Release target remains `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
19. Readiness: **4 — neither blocker sufficiently resolved**. Execution validity
    is false due to the frozen authority mismatch; persisted-package integrity
    validation passes. Reopened exposure is **0 states / 0 edges**.
20. Sole recommendation: **separately govern a bounded reconciliation of the R9O
    journal-count authority with the hash-verified R9N evidence before authorizing
    another diagnosis.**

All historical files, numerical formulas, tolerances, certificate semantics,
dependencies, public APIs, README and claim ledger remain unchanged. No targets,
outcomes, model shares, protected/withheld/pose evidence or partial scientific
summaries were inspected. Accessibility remains **PROXY ONLY** and suppression
**NOT SUPPORTABLE**. No empirical representation retry is authorized by this work.
