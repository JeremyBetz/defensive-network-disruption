# Session 14R9J — R9I blocker diagnosis and linear publication validation

2026-09-18. One governed diagnosis; numerical repairs remain prospective.

## Decisions

**Numerical NA — production result accurate; onset-only verifier numerically
unreliable. Publication PA — linear replacement proven semantically equivalent.
Execution valid. Overall readiness 3 — publication resolved, numerical not.**

The unchanged R9I/R9E route reproduced `GateFailure: piecewise_unsplit` at
`onset_adaptive`. An independent rational/interval Taylor enclosure establishes
production accuracy under the existing `1e-6` gate and structural-piece accuracy
under `1e-10`. The onset-only estimate is outside the latter authority even at
the nearest point of the independent enclosure. This is a verifier disagreement,
not evidence against the football field construct.

The prospective publication replacement preserves the historical authority
objects and hash meanings while removing repeated prefix replay and projection
scans. Its single retained-journal pass verified 30,881 records in 0.507 seconds.
No missing R9I package was reconstructed or retrospectively accepted. PA's
delivery condition also requires the final CI and synchronization checks reported
in the final handoff.

The [protocol](protocols/phase_14r9j_r9i_blocker_diagnosis.md) and
[manifest](../outputs/continuous_occlusion_r9i_blocker_diagnosis/manifest.json)
bind the evidence. R9I remains **D — blocked or invalid / readiness 4**.
Historical publication was interrupted: neither its natural completion time nor
nontermination was established. Its numerical failure occurred first, and no
normal QC, manifest or completed publication-authority snapshot exists.

## Numerical evidence and limits

Only retained prepared state ordinal 4, receiver ordinal 7 was reopened. Its
successful materialization receipt and original failure context were verified
before selection. The prepared file was hashed without decoding other rows;
lexical selection materialized only the carrier, authorized receiver, ordered
defenders and required authority identity. No canonical population, provider
file, other receiver geometry or partial scientific summary was opened. The
new attempt and materialization receipts confirm one reopened state and edge,
with no unresolved receipt.

Observation hooks delegated to the unchanged R9E numerical route. The production
vector visited 256, 512, 1,024 and 2,048 intervals and first converged at 2,048.
Every component met the unchanged `1e-7` stopping rule there. Full verification
still failed, so the production result remains provisional in that execution.
Exact vectors, stopping deltas, estimates, errors and gate inputs are private.

The structural audit found eight onset records, one switch, no tie intervals,
ten positive structural pieces and zero micro-residual pieces. Domain coverage,
ordering, certified witnesses, preserved endpoints and residual routing passed.
The independent scalar-oracle check passed under inherited authority. Exact
geometry, widths, coordinates, pair ordering and owner blocks remain private;
tolerance owner blocks are not assertions of exact field equality.

Strict/repeat structural integration passed its existing interval-aware gate.
The onset-only comparator used nine pieces split at canonical onsets, not a
single integral over the domain. Its failing contribution is sanitized onset
interval ordinal 3; the common boundary refinement maps it to structural pieces
3 and 4. This interval contains the certified switch, which is not an onset.
All ten structural contributions and the other eight onset contributions satisfy
their reference-relative `1e-10` checks. This localizes the observed inaccuracy
to a comparator interval that omits a known nonsmooth maximum transition. It
does not recover QUADPACK's unreturned subdivision history or prove the exact
internal error-estimation mechanism.

There were 29 adaptive calls, zero warnings and zero certified contributions in
the failing path. Session 14ar's terminal-interval certificate therefore did not
participate; it cannot certify the whole integral or waive this disagreement.
The direct-Simpson and subsequent joint-maximum gates were unreachable after
the original failure and remain historically unexecuted for this reproduction.
Adaptive stopping is not a Simpson ladder. Missing terminal arrays and historical
stage timings remain unavailable.

The independent reference uses exact binary64 input rationals, 256-bit outward
dyadic arithmetic, integer square-root bounds, independently verified ownership,
degree-16 shifted exponential integration and degree-32 range-reduced center
exponentials with rigorous remainders. Captured boundaries supply cuts only;
detector owners supply no reference truth. It met the frozen outward width
target of `1e-12` in 4.204 seconds with 85 leaf cells: 80 smooth-owner Taylor
cells and five conservatively bounded cells. Every cell remains represented.
The target is diagnostic precision, not a production tolerance or a registered
certificate. This encloses the mathematical field from exact binary64 geometry;
agreement with floating-point field evaluation is a separate scalar-oracle check.

Accuracy uses the worst-case discrepancy to the reference enclosure; inaccuracy
uses the nearest possible discrepancy. The production and piecewise paths pass
their respective existing gates, while the onset-only path fails. Exact bounds,
differences, meaningful relative differences and per-piece contributions remain
private and hash-bound. No estimate was replaced by the independent bound.

The unchanged isotropic and expanding paths then ran once each on the same edge.
Both passed their inherited verification gates, at 2,048 and 8,192 accepted
Simpson intervals respectively. This is a same-edge diagnostic comparison, not
independent whole-edge accuracy certification for those candidates, population
generality, or scientific candidate selection.

## Publication replacement and measured work

The historical closure route can reach eight full semantic reconstructions:
pre-failure and terminal snapshots; two inside the initial progress derivation;
one during private-package creation; two inside validation's progress derivation;
and one inside the nested numerical-package validator. The corresponding source
path can perform eleven complete read/parse/chain-verification passes. These
are reachable source-path counts, not claims that all completed in interrupted
R9I. The preserved trace confirms the original exception independently of replay.

Within one historical reconstruction, growing-prefix replay visits
`N(N+1)/2` lifecycle records on the preparation-heavy controls. Repeated scans
of earlier attempts add cubic work as states grow. For the frozen ten-edge
fixture with `S` states and `N = 4S + 3` records, the attempt-scan count is
`2(S^3-S)/3`. A 27-record instrumented control measured exactly 378 prefix visits
and 140 prior-attempt scans. The new path visited each record once without calling
the old prefix replayer. Operation counts and source structure establish this
conclusion; timing is supporting evidence only.

| Actual valid records | Old replay seconds | New replay seconds | Equality |
| ---: | ---: | ---: | --- |
| 1,003 | 0.900 | 0.00144 | Exact |
| 2,003 | 5.205 | 0.00291 | Exact |
| 4,003 | 33.455 | 0.00587 | Exact |
| 8,003 | Censored at 60.003 | 0.01194 | Old result unavailable |

Old completed doubling ratios were approximately 5.79 and 6.43; new ratios were
2.01, 2.02 and 2.03. These measurements time replay of already constructed valid
synthetic records, not file loading. The retained one-pass measurement includes
file reading, raw hashing, chain/schema validation, transitions and final
authority construction. No old full-size R9I replay, 16,000-record benchmark or
runtime extrapolation was performed.

The new implementation indexes projection attempts and pending attempts by state,
updates lifecycle and numerical-stage legality incrementally, retains geometric
edge and field-call sets separately, and derives final summaries once. Work is
linear in journal bytes, events and declared edge references with expected
constant-time dictionary/set access. Final summary scans are linear; canonical
JSON ordering is over fixed schema keys. Memory retains indexed lifecycle state.
No sorting of a growing historical journal or repeated full reconstruction is
required. An immutable, source-bound authority can be reused across one closure.
Source mutation invalidates reuse.

The digest named `snapshot_sha256` remains the historical numerical-wrapper
digest. A separately named lifecycle digest is additional diagnostic evidence;
it does not replace that meaning. Persisted package validation compares actual
QC, manifest, evidence, progress and traceback records. Missing records and
cross-file mismatches block acceptance; validators do not manufacture substitutes.

Ten governed controls, 270 prefix comparisons and 20 invalid controls passed.
Complete authority objects, active context, uncertainty, failure identity and
hashes matched. Tests also used actual historical R7/R8/R9 control constructors
and Session 14ai ordinary/diagnostic fixtures. Valid persisted success/failure
packages and rejection controls exercised the real old and new validators.

The sole retained review verified the full raw-file hash and chain, selected
receipt and original traceback. Its derived counts match R9I: 7,227 states and
72,270 geometric edges opened; four states and 40 edges completed; 148 field
calls started and 147 completed; 72,230 exposed edges unresolved. Candidate work
remains 50/50 isotropic, 50/50 expanding and 48/47 constant_width. These historical
counts are distinct from R9J's one reopened edge. Comparison with a historical
completed full snapshot is unavailable because none exists. Later publication
checking validates the persisted R9J receipt and index, without another historical
journal replay.

## Runtime, validation and preservation

The governed window through preclosure was 233.946 seconds. Publication occupied
100.291 seconds, including the deliberately censored old benchmark; numerical
work occupied 133.458 seconds. The accounting separates 133.347 numerical and
100.284 publication seconds excluding measured evidence I/O, 0.118 seconds of
that I/O, and 0.197 seconds of unattributed overhead. These reconcile to the
window; overlapping inclusive timers are not added again. The exclusive figures
are disjoint wall-time accounting, not process CPU measurements. Unseparated
internal waiting/I/O and observer overhead remain within stage wall time.

The instrumented constant-width route took 41.695 seconds, including 41.503
inclusive seconds in structural certification and only 0.049 inclusive seconds
in adaptive calls. Instrumentation affects runtime; these are new observations,
not reconstructed historical timings or population runtime estimates. No
numerical operation reached its ten-minute cap, and the numerical branch stayed
within sixty minutes. No repair or second governed execution followed exposure.

Focused validation: **44 run, 44 passed, 0 skipped**. Relevant Session 14
validation: **199 run, 199 passed, 0 skipped**. The initial full collection:
**1,031 run, 1,028 passed, 3 retained skips**. A final full collection includes
the additional focused relative-difference test; its counts are recorded at
delivery. Numerical test replays are validation, not new governed acceptance.

A clean tracked Git clone with ignored evidence absent passed **86 run, 86
passed, 0 skipped**. An earlier archive-only simulation ran 86 tests with 84
passes and two setup errors because it lacked Git/tag metadata required by the
historical R9H tests. Repeating that validation in a real tracked clone resolved
the setup error without any product change. This was not a governed rerun.

Compilation, strict complete/partial/invalid schemas, finite JSON, LF CSV,
public/private hashes, publication checks, privacy/access guards, links,
historical-byte preservation, append-only log inspection, staged review and
diff checks are required for closure. The code and tests were committed before
the single diagnosis. Final cross-version and distribution CI and synchronized
heads are verified after the closure commit and reported in the final handoff.

README, claim ledger, dependencies, public API, release metadata, prior protocols,
implementations and artifacts remain unchanged. There is no scientific result,
new certificate, numerical repair, tolerance change or empirical representation
retry. Carrier-position proxy semantics, PROXY ONLY accessibility and NOT
SUPPORTABLE suppression remain unchanged.

## 27-item handoff map

1. **Starting HEAD:** `137b7cc2109fc18f1efe203c715484a547a287c6`.
2. **Ending HEAD:** the closure commit containing this report; its exact identifier
   and synchronized remote checks are supplied after commit in the final delivery.
3. **Protocol:** commit `cd2775b7d098e6aca231bf7962e16b2e51d80e29`, SHA-256
   `8c205e768840260dc81ea7bb000d8edaa143de75eb0158d3a80bb7f3e43de9dd`.
4. **Failed-edge authority:** selected receipt, prepared hash and failure context
   verified; one state/edge reopened, no uncertain receipt, no canonical reopening.
5. **Production integral:** private; controlled-vector record SHA-256
   `24abef47244505c8e9f40c594eb41146d89ba9edb3adcde1fb25255a87bade6a`.
6. **Piecewise integral:** private; strict record SHA-256
   `6beaff6e0378b196c793be9dc6becda43d81a5a713444f7a32cd49fe56459841`.
7. **Onset-only integral:** private; record SHA-256
   `4c87e5f184ed2edf58a016cf21012214012828f904dc46ac4fbe7aeeee7b5827`.
8. **Disagreements/tolerances:** exact absolute/relative values private. The actual
   `piecewise_unsplit` gate failed at `1e-10`; strict/repeat passed at `1e-10`.
   Later direct `1e-9` and joint `1e-6` gates were unexecuted in that failed path.
   Independent reference comparisons use their applicable existing authorities.
9. **Ladder:** 256→512→1,024→2,048; exact vectors/deltas private, accepted 2,048.
10. **Partitions:** ten pieces, eight onsets, one switch, no ties or micro pieces;
    complete structural audit passed; coordinates/owners/widths private.
11. **Problem interval:** onset interval 3, covering structural pieces 3/4 and
    their interior switch; exact contributions private. See
    [localization](../outputs/continuous_occlusion_r9i_blocker_diagnosis/interval_localization.csv).
12. **Independent reference:** eligible rigorous enclosure, width ≤`1e-12`, 85
    leaves. Exact private reference record SHA-256
    `ff2d0278aae0fb87f9e870ad2d9138e3d0ec78585f5afe8a7cabd2df04b45b73`.
13. **Candidates:** constant_width failed its exact gate at resolution 2,048;
    isotropic passed at 2,048; expanding passed at 8,192. No scientific selection.
14. **Numerical classification:** **NA**, with independent production/piecewise
    accuracy and onset-only inaccuracy; no inferred healthy path from warnings alone.
15. **Repair specification, plan only:** govern an onset-only comparator repair
    that respects certified switch/tie boundaries while retaining production
    estimates, independent accuracy checks and all existing tolerances. Require
    frozen nonsmooth-maximum controls and the bounded retained-edge regression;
    do not waive whole-integral disagreement or extend the terminal certificate.
16. **Historical complexity:** triangular prefix work plus cubic preparation
    scans; eight semantic reconstructions reachable in normal failure closure.
17. **Benchmarks:** 1,003/2,003/4,003 exact comparisons; 8,003 old path censored at
    sixty seconds. New timings approximately double; no extrapolation.
18. **Replacement:** one hash/transition pass, indexed attempts, incremental sets,
    one immutable content-bound result, actual persisted-file validation.
19. **Equivalence:** ten controls, 270 prefixes, 20 invalid controls; full authority
    objects and legacy hash meanings preserved, plus historical fixture tests.
20. **Retained review:** one pass, 30,881 records, 0.507 seconds; available counts
    and original failure match. Missing historical full snapshot remains unavailable.
21. **Publication:** **PA**, subject to completion of stated delivery gates.
22. **Tests:** counts and the archive-setup qualification above; final count in delivery.
23. **CI:** Python 3.11, Python 3.13 and distribution required on implementation
    and closure. Exact run links/results supplied after the closure commit.
24. **Commits:** protocol `cd2775b`; numerical tooling `8b259b0`; equivalent linear
    validator `14164dd`; this report/evidence/log form the fourth closure commit.
25. **Clean/sync/release:** final verification follows push; release target remains
    `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
26. **Readiness:** **3**. Publication repair does not operationally resolve the
    numerical blocker, and diagnosis alone does not authorize an empirical retry.
27. **One next action:** separately govern a bounded onset-only comparator repair
    using certified switch/tie partition authority, preserving production estimates
    and existing agreement tolerances, with the proven linear publication validator.

No recommendation was executed. The thirteen-file package and all private evidence
are bound by private-index SHA-256
`9b64e7d87f0a1076ca27c6c1828dbacd4f15e7afe8a507719d08b83b497b0192`
and manifest SHA-256
`cbe421fe0fec781e2456e811806e437ad0dc711f958f05a0d9f1ed7d13602a96`.
