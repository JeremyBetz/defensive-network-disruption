# Session 12b — Normalized progression prerequisite stop

## Result

**Scientific D — BLOCKED BY THE FROZEN COORDINATE REQUIREMENT.**
**Software 3 — KEEP INTERNAL.**
**Execution status: valid prerequisite stop; empirical analysis not executed.**

All nine permitted development metadata objects passed pinned Git identity/size
verification. Only pitch length and width were retained by the selective parser.
The canonical population matched its hash, all 7,227 states, and the exact
historical match counts. The strict bounds check found **12 carrier/receiver
point occurrences outside the longitudinal pitch domain across seven matches**.
These are point occurrences, not a count of unique players or affected states.
No such additional count was computed.

The approved rule requires stopping on any such occurrence. No coordinate was
clipped, observation excluded, formula extended, population regenerated or
scientific rule changed. No empirical utility, share, horizon, contribution,
concentration comparison or typology frequency was calculated. No analysis
execution marker exists. This is not evidence that the method adds little.

## Frozen method and scope

The [protocol](protocols/phase_12b_internal_threat_baseline.md) and
[metadata amendment](protocols/phase_12b_metadata_dimensions_amendment.md) were
committed before implementation and new data access.

The selected **normalized goalward progression proxy** is
`V(x',y') = 0.5 + x'/L`, requiring finite metric coordinates and actual match
length L. The accepted longitudinal domain is exactly [-L/2,L/2], with no
boundary tolerance. V is in [0,1] there; it is not a goal or probability scale.
Native y is retained but does not affect V. Pitch width was verified as provenance
only and did not introduce a new lateral exclusion.

The rejected formula `u*(1-|2v-1|)` imposes an additional centrality assumption.
The formula comparison was limited to synthetic u/v values 0, 0.5 and 1.
No parameter search or empirical comparison of formulas occurred.

Signed destination changes remain `delta_V = V(receiver)-V(carrier)`.
`H_m=sum(p_m*delta_V)` is model-weighted goalward displacement divided by pitch
length. The shift `H1-H0` also equals `sum((p1-p0)*V(receiver))` because the
carrier baseline cancels. Receiver bookkeeping `(p1-p0)*delta_V` sums to this
shift; individual signs are not defender credit or a unique receiver-to-receiver
transfer. These identities passed synthetic checks only.

The implementation preserves the numerical identity tolerance
`64*float64_epsilon*max(1,sum(abs(summands)),abs(compared totals))`. The frozen
nine-cell concentration/shift typology uses +/-1e-12 as numerical equality,
not practical equivalence. Neither typology frequencies nor any empirical
magnitude were inspected.

## Metadata and access evidence

Forty-five bounded GitHub commit/tree/path requests succeeded without transport
failure or retry. All nine metadata products used existing local bytes after
verification against the pinned ordinary Git blobs. No new metadata payload
download was required. The scanner validates JSON syntax and duplicate keys but
retains only the two root dimension values; mixed-content values do not enter
application records or errors. Detailed mapping, receipts and append-only access
records remain ignored.

The source stayed at the existing SkillCorner revision. Only historical
nine-match development metadata and canonical development geometry were used.
No raw tracking or event rows, target analysis, reserved detail, withheld data,
pose, temporal reconstruction or empirical rendering occurred. Targets were
projected out before analytical state construction. No external xT surface,
external parameter, organizer message or dependency installation occurred.

| Stable match alias | Canonical states | Out-of-domain point occurrences |
| --- | ---: | ---: |
| development_01 | 885 | 2 |
| development_02 | 801 | 1 |
| development_03 | 952 | 3 |
| development_04 | 877 | 2 |
| development_05 | 861 | 1 |
| development_06 | 629 | 1 |
| development_07 | 764 | 2 |
| development_08 | 734 | 0 |
| development_09 | 724 | 0 |

The audit does not establish why these positions lie outside the domain. It
provides no evidence here to distinguish ordinary boundary behavior, tracking
quality or coordinate interpretation. No distances beyond the boundary, player
identities, state examples or football passages were inspected to diagnose it.

## Software and checks

New internal modules implement an immutable progression surface, a generic
`SpatialValueSurface.value_at(x,y)` contract, immutable horizon/decomposition
results, and the selective dimension scanner. The separate runner enforces
metadata, preparation and analysis boundaries. Historical modules, root exports,
version, dependencies, lockfile, claim ledger and past results are unchanged.

The internal interface passed synthetic evaluation but did not complete the
empirical coordinate prerequisite. Keep it internal; no public API readiness or
new package release is claimed. The synthetic SVG renderer was tested using only
the frozen illustrative shares. No final public illustration was generated,
because the prospective protocol stops at the prerequisite blocker.

- Focused Session 12b suite: **40 passed, 0 skipped**.
- Full active suite before access and after the stop: **259 passed, 3 skipped**
  out of 262 tests. Skips remain historical scaffold tests.
- Synthetic end-to-end preparation, analysis, output closure, publication and
  rendering passed in temporary fixtures; this did not execute the empirical run.
- Compilation, source/model/population integrity, strict projection, frozen
  M0/M1 feature equivalence, signed identities, equal-match weighting, complete
  typology, numerical guards, failure preservation and route separation passed.
- Protocol/report links, generated output hashes, publication safety, staged
  contents, append-only history and `git diff --check` passed.

An initial attempt to invoke pytest found it absent. The suite was implemented
with the repository's standard-library unittest framework without installation.
Initial synthetic fixture errors concerning the expected integrity exception
and temporary-directory symlinks were corrected before the implementation commit
and before data access. No implementation changed after metadata access began.
The coordinate stop is the planned gate, not a post-access implementation repair.

## Authority and twenty-four handoff items

1. **Starting HEAD:** `9c48bf5b3146222e27e9eeb151af448e6beeb882`, clean and live-remote synchronized.
2. **Ending HEAD:** supplied in the final delivery handoff to avoid a recursive commit reference; reviewed commits are pushed and live synchronization checked at delivery.
3. **Protocol:** `f4040af`; protocol SHA-256 `19d491348b89bfff8d52e98569d5b50db47c6042b1f736f21d7c2666286622dc`. Amendment SHA-256 `f535056bd692a291bcb5494f368fd1828bc4ec11acb152851499d7d0c6fcf634`.
4. **Formula:** V=0.5+x'/L, unchanged.
5. **Rationale:** transparent normalized progression, no learned parameters or centrality penalty.
6. **Coordinate contract:** centred metres, verified attacking x, native y, exact longitudinal bounds; blocked by the observed occurrences.
7. **Metadata access:** required and completed for nine development products; two retained dimension fields only.
8. **Value range:** [0,1] on the accepted domain; no continuation beyond it.
9. **Horizon definition:** sum(p*delta_V); equivalent normalized model-weighted progression.
10. **H_M0 aggregate:** not executed; unavailable.
11. **H_M1 aggregate:** not executed; unavailable.
12. **Value-shift aggregate:** not executed; unavailable.
13. **Shift sign counts:** not executed; unavailable.
14. **Concentration versus value findings:** synthetic formal distinction only; empirical comparison not executed.
15. **Typology frequencies:** not executed; unavailable.
16. **Receiver decomposition:** synthetic identities passed; empirical verification not executed.
17. **Scientific decision:** D, blocked coordinate prerequisite; not a negative scientific finding.
18. **Software decision:** 3, keep internal pending resolved coordinate contract and governed use.
19. **Claim ledger:** unchanged, including C09/C10.
20. **Synthetic visual:** renderer tested deterministically; final SVG not generated after the stop. No empirical visual.
21. **Protected boundaries:** no reserved-detail, withheld or pose access.
22. **Checks:** pass/skip counts and qualifications above; no empirical rerun.
23. **Commits:** protocol `f4040af`, tested implementation `8cab95c`, coordinate authority `30dea7c`, followed by the reviewed closure commit. Push/synchronization reported at delivery. Release v0.1.0 remains at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
24. **One next direction:** a separately governed bounded coordinate-prerequisite review of the longitudinal boundary occurrences. Do not execute it here or assume it authorizes clipping, exclusions or a changed surface.

[Coordinate authority](../outputs/internal_threat_baseline/value_surface_authority.json)
SHA-256: `03311326e7339ca093e0f50da6b0603e650f53b20df24b6f68501896d0470936`.
[QC](../outputs/internal_threat_baseline/qc.json)
SHA-256: `fd870f22276d1a15c19376abc4e905ac483fefb2124fcff80a9fe9d7c24152dc`.
[Failure-aware manifest](../outputs/internal_threat_baseline/manifest.json)
SHA-256: `e53062040d7c89914924c50a44b1729c655f7725433a456471c387c94c1b586d`.
Unavailable result files are listed explicitly rather than filled with zeros.

Accessibility remains **PROXY ONLY**; suppression remains **NOT SUPPORTABLE**.
No calibrated xT, success/control probability, causal defensive effect,
attribution, prevented goals, defensive value, intent or pressing-trap claim
follows. Development models are in-sample, Tier B vendor-target trained, and
based on offline/extrapolated tracking. A star and its summaries add no
information beyond the complete edge vector and geometry. Session 12a's external
branch remains deferred. Session 12b stops with this preserved prerequisite result.
