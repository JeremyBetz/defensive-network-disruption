# Session 14as — Independent-bound verifier-evidence contract repair

Date: 2026-09-15. **F — invalid / execution failure; readiness 4**.

Session 14as froze and implemented a narrow prospective contract for retaining
an adaptive roundoff warning while accepting its numerical evidence only when a
pre-registered rigorous certificate matches the exact interval authority. The
implementation passed its focused tests and two pre-exposure historical
numerical replays. The single governed acceptance then failed before its
108-case replay because the runner loaded a package-relative authority module
as a standalone file. Python rejected its relative import.

The create-once marker and independent traceback record were preserved. Under
the frozen stop rule, the implementation was not repaired and the acceptance
was not rerun. The public package therefore records unavailable governed
certificate, oracle, and synthetic-regression evidence. No empirical state,
edge, geometry, or scientific result was opened.

## Contract implemented but not accepted

The internal immutable certificate binds authority and source-manifest hashes,
interval identity and endpoint bits, field and parameters, combination,
integrand and structural fingerprints, rigorous bounds, approved method,
independence statement, provenance, and the unchanged `1e-10` authority. The
only eligible warning is SciPy
`scipy.integrate._quadpack_py.IntegrationWarning` with exact message hash
`3f2b9b58ff1c98b7c27fff799d18b42a6dd19e50c2b5f6b60a0b80bfb2798ab3`.

The four implementation branches keep micro-residual bounds, ordinary
warning-free points, independently certified warning pieces, and blocking
failures distinct. Certificate use requires exact authority equality,
containment, and realized width at most `1e-10`. The adaptive point remains the
production estimate. Aggregate lower and upper endpoints use `math.fsum` and
one final outward `nextafter` per side. Historical readiness remains unchanged;
the prospective readiness object derives its result from certificate,
regression, failure-enforcement, determinism, and integrity checks.

These are implementation facts and pre-exposure test results, not an accepted
governed verifier authority. The failed execution did not persist its in-memory
certificate regression or oracle results, so the closed package marks them
unavailable rather than reconstructing them after exposure.

## Numbered handoff

1. **Starting HEAD:** `cc20812c87d3d43506cf4a2b0100660fa51b16bf`.
2. **Ending HEAD:** the closure commit containing this report; its identifier is
   supplied in the delivery response.
3. **Protocol:** [Phase 14as](protocols/phase_14as_independent_bound_verifier_contract.md),
   commit `7374ff7`, SHA-256
   `4a64b5e5b16fa8ddb4c82c343b7db014f8e304ae4715a40deb3cfa0a671f2a96`.
4. **Eligible warning:** exact SciPy `IntegrationWarning` class and committed
   roundoff message hash `3f2b9b58ff1c98b7c27fff799d18b42a6dd19e50c2b5f6b60a0b80bfb2798ab3`.
5. **Certificate schema:** the nineteen frozen fields listed in
   [contract.json](../outputs/continuous_occlusion_independent_certificate_contract/contract.json).
6. **Authority match:** exact identity, endpoint bits, field, parameters,
   combination, integrand, structure, method, provenance, and tolerance.
7. **Width rule:** realized binary64 `upper-lower <= 1e-10`, with no epsilon.
8. **Containment:** the adaptive estimate must lie inside the closed enclosure.
9. **Aggregation:** lower and upper sums use `math.fsum`, then one final outward
   `nextafter`; nested widening is forbidden.
10. **Session 14ar authority:** reference-result SHA-256
    `d98447c98ab0f04ef2c608b98b9554cecf47ed167bf1fc91775fa53bab6c2dd6`;
    source-manifest SHA-256
    `bb3d352e9b43b579f897e36f4f000f3fb770701fe11877f23af76a36668d97b4`.
11. **Governed Session 14ar regression:** unavailable because acceptance failed
    before results were persisted. Its focused pre-exposure test passed.
12. **Warning preservation:** implemented and tested before exposure; governed
    acceptance result unavailable.
13. **Positive oracles:** pre-exposure tests passed; governed rows unavailable.
14. **Negative controls:** pre-exposure tests passed; governed rows unavailable.
15. **Micro-residual regression:** inherited suite passed before exposure;
    governed acceptance result unavailable.
16. **Ordinary quadrature regression:** a complete 108-case onset-aware replay
    passed before exposure; governed acceptance result unavailable.
17. **Aggregation:** focused mixed-branch and outward-rounding tests passed;
    governed acceptance result unavailable.
18. **Readiness:** derived object implemented and tested; final result is false
    because governed evidence is incomplete.
19. **108 cases:** 108 passed in pre-exposure validation; 0 completed in the
    governed acceptance.
20. **366 references:** 366 passed in pre-exposure validation; 0 completed in the
    governed acceptance.
21. **399 permutations:** 399 passed in pre-exposure validation; 0 completed in
    the governed acceptance.
22. **Empirical access:** zero states and zero edges; no scientific output opened.
23. **Classification:** **F — invalid / execution failure**.
24. **Readiness:** **4 — not ready**.
25. **Claim ledger:** unchanged; this phase establishes no scientific result.
26. **Tests and checks:** focused 12/12 and the selected 22/22 pre-exposure set
    passed; two complete historical replays passed. The full suite passed 899
    run / 896 passed / 3 retained skips. Compilation, independent failure-package
    schema/hash validation, finite JSON, LF CSV, privacy, links, history, and diff
    checks passed. The runner's success-only publication checker rejected the
    invalid package schema, so normal publication acceptance is unavailable.
    CI is reported in the delivery response.
27. **Commits and push:** protocol `7374ff7`, implementation `edc39f1`, and this
    closure commit, exactly three. Push and synchronization are delivery gates.
28. **Recommendation:** separately govern a bounded Session 14as acceptance-runner
    repair that loads the synthetic authority in its package context and proves
    valid failure publication before any new acceptance attempt.

## Failure and scope

The exception was `ImportError: attempted relative import with no known parent
package` while loading the existing onset-owner synthetic authority. Private
failure SHA-256 is
`7a03b64d23051988dc284a181f16e213fb9ef5e86d6985ddd78b361346fefb1d`;
marker SHA-256 is
`53eb276c32adcde99ce6c28161b7f11a9cb5c716241bfe7e7690023d6151f017`.
The marker makes the original acceptance non-repeatable.

The committed runner's normal publication checker also lacks the frozen invalid
manifest schema and rejected the preserved failure package. An independent
read-only closure check verified the eight persisted files, their strict
failure fields, finite JSON, LF CSV records, and manifest hashes. This supports
failure preservation only; it does not convert the run into an accepted
governed execution.

No certificate was generated, no Session 14ar calculation or warning interval
was rerun, no tolerance changed, and Session 14R remains paused. Historical
artifacts, numerical implementations, public API, dependencies, release,
claims, and scientific outputs remain unchanged.
