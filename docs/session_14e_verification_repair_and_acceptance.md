# Session 14e — Verification repair and synthetic acceptance audit

## Decision

**Execution: VALID.** **PASS — VERIFICATION CONTRACT REPAIRED; SESSION 14R MAY
RESUME.**

This result authorizes planning Session 14R under the repaired verification
contract. It does not execute Session 14R, validate a cover shadow, promote a
scientific claim, or change any continuous-field formula. Sessions 14–14d remain
preserved historical authorities, including Session 14d's BLOCKED result.

## Twenty-six-item handoff

1. **Starting authority.** Clean local `HEAD`, `origin/main`, and live GitHub
   `main` matched `9188222c4270ba1a89963ae1bbdb9003b0bf9f1e`.
2. **Release authority.** Annotated `v0.1.0` remained unchanged and peeled to
   `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
3. **Prospective protocol.** The [Phase 14e protocol](protocols/phase_14e_verification_repair_and_acceptance.md)
   was committed as `caa9271` before repair implementation.
4. **Implementation authority.** The new internal verifier, runner, and focused
   tests were committed as `d22f459` before the one governed audit.
5. **Historical preservation.** Session 14–14d formulas, helpers, tests,
   protocols, outputs, and reports remained byte-identical to the starting
   authority. The repair is additive and internal.
6. **Scope.** The execution was synthetic only. It opened no empirical
   population, provider product, target, fitted model, utility, option share,
   protected or withheld record, pose, xT, or progression artifact.
7. **Continuity argument.** The Gaussian is continuous. Smoothstep agrees in
   value and first derivative at both joins, directional widths remain positive,
   and the remaining coordinate and exponential operations are continuous on
   the valid domain. Finite maximum and finite-product union preserve
   continuity. Origin–defender coincidence remains excluded at the historical
   `1e-9` implementation tolerance; no uniform near-origin stability is claimed.
8. **Independent value checks.** Seventy-two production/oracle approach records
   covered both directional joins, all three candidate families, axial and
   rotated coordinates, and metric steps `1e-1` through `1e-6`. Every comparison
   passed the inherited absolute/relative `1e-12` rule.
9. **Discontinuous control.** A separate `0.2 -> 0.8` step was rejected from its
   unequal analytical one-sided limits. Slopes were not used to decide
   continuity.
10. **Switch continuity.** Every frozen maximum switch was probed at normalized
    steps `1e-3` through `1e-7`, capped by neighboring partitions. Production
    maxima matched maxima independently assembled from scalar individual-field
    oracles.
11. **Tiny-sign repair.** Opposite raw signs are detected with nonzero checks and
    `signbit`; differences are never multiplied. Oracles passed at magnitudes
    `1e-8`, `1e-12`, `1e-15`, and `1e-200`, plus same-sign, exact-zero,
    structural-zero, and adjacent-node controls.
12. **Owner semantics.** Raw root discovery remains separate from the unchanged
    `1e-12` maximum-owner block. The `1e-200` root is discovered without being
    misrepresented as an ownership switch.
13. **Tie certification.** Exact-equality plateaus now retain entry/exit
    enclosures whose outside and inside endpoints are adjacent float64 values.
    Inside equality, positive maximal support, owners, outside inequality, and
    neighboring topology are verified; both endpoints become partitions.
14. **Boundary oracles.** Grid-aligned `[0.25, 0.75]` and off-grid
    `[0.251, 0.749]` plateaus were enclosed exactly under the frozen numerical
    predicate. The known mathematical endpoints lie inside their certified
    enclosures.
15. **Tie topology tests.** Focused tests covered isolated roots, endpoint and
    interior plateaus, three-way plateaus, non-envelope equality, all-zero
    support, mapped permutations, adjacent boundaries, and root failure.
16. **Finite-grid qualification.** The 65,536-grid scan is verified only for the
    frozen fixture family. A synthetic two-crossings-within-one-cell example
    remains deliberately undetected and prevents any global completeness claim.
17. **Frozen cases.** All 108 fixture/candidate maximum cases reproduced through
    the repaired path, with deterministic repeated records.
18. **Permutation evidence.** All 399 complete defender-permutation comparisons
    passed after inverse mapping of owners, pairs, intervals, boundaries,
    partitions, and maximizing-defender sets.
19. **Controlled integration.** Each of the 108 cases used one joint vector for
    all individual, union, and maximum components over the frozen
    256-to-16,384 doubling ladder. Every case converged under the all-component
    `1e-7` successive-change rule before cap exhaustion.
20. **Reference evidence.** All 366 accepted components passed the inherited
    `1e-6` reference-error bound against 362 Session 14b references and the four
    Session 14c piecewise replacements. The largest absolute error was
    `4.1576548232002963e-08`; no reference or tolerance changed.
21. **Machine readiness.** `VerificationReadiness` is frozen and accepts exactly
    nine boolean obligations. Its `ready` property is their conjunction and
    cannot be supplied or overridden. All nine governed inputs were true.
22. **Failure enforcement.** Twelve independent injections—continuity,
    discontinuous surrogate, missed tiny sign, uncertified tie, root failure,
    unavailable reference, controlled non-convergence, permutation mismatch,
    nondeterminism, blocking warning, integrity failure, and marker collision—
    each produced `ready=False`; the valid control produced `ready=True`.
23. **Execution lifecycle.** A clean committed preflight preceded the audit. An
    exclusive ignored marker was created before calculation. The runner exposes
    only `preflight`, `audit`, and `publication-check`; it has no acquisition,
    empirical, model, fitting, or rendering route.
24. **Public evidence.** The ten requested files are under
    [`outputs/continuous_occlusion_verification_repair/`](../outputs/continuous_occlusion_verification_repair/).
    The [manifest](../outputs/continuous_occlusion_verification_repair/manifest.json)
    has SHA-256 `1dfbc80010c97349ba0489abb88972146981bbd6a2de0582068fd435b5c947a7`
    and binds the protocol, implementation, locked environment, historical
    inputs, and nine pre-manifest outputs.
25. **Verification.** Focused Session 14e tests: **13 passed**. Relevant Session
    14-series tests: **72 passed plus 12 subtests**, zero skips. The documented
    full active unittest suite: **348 passed, three retained scaffold skips**.
    Compilation, numerical regressions, closed-output schemas and hashes,
    publication/privacy checks, append-only history, and `git diff --check`
    passed. A first full-suite invocation omitted the test-only pytest package
    needed to import the new focused module; the unchanged suite passed when
    invoked with that package available. No governed calculation was rerun.
26. **Claims and next action.** The claim ledger is unchanged. Accessibility
    remains **PROXY ONLY** and suppression **NOT SUPPORTABLE**. The one
    recommendation is: **resume Session 14R under the audited and repaired
    verification contract**. This session does not execute that recommendation.

## Interpretation limits

PASS establishes that the proposed numerical verification path is adequately
supported for the finite, frozen synthetic authority. It does not prove that a
65,536-node scan detects every possible between-node crossing, that a field is a
valid cover shadow, or that any representation explains behavior. Session 14R
must remain label-free and development-only, use synthetic equal-minimum cases,
report continuous union-minus-maximum distributions without practical
thresholds, and finish synthetic visual QA before empirical access.

The output package was closed and hash-bound before this interpretation was
written. Execution records and dense traces remain ignored. No dependencies,
released API, package version, field parameter, candidate family, numerical
tolerance, integration method, or reference value changed.
