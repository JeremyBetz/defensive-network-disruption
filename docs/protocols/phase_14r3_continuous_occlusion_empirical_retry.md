# Phase 14R3 — Label-free continuous occlusion representation retry

Prospective authority, 2026-09-12. Start: `8da64387fe62d5bd15f0a347456252e931988945`;
live main verified identical. Preserve release `v0.1.0` at
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. Operator: project-author-directed
Codex execution. No new scientific claim is authorized.

## Question and inherited authority

Do expanding and constant-width carrier-origin fields describe complementary
structure beyond isotropic proximity, and does complete evidence justify one
candidate for later behavioral validation? Origins are carrier-position proxies
for ball origin, not measured ball coordinates. This is label-free, spent
development description, not behavioral validation.

Bind all tracked historical files at the starting commit, particularly the
[Session 13 report](../session_13_defender_edge_influence_report.md),
[14R2 protocol](phase_14r2_continuous_occlusion_empirical_retry.md),
[14ad protocol](phase_14ad_launch_prerequisite_enforcement.md), and their
manifests. The Session 14c–14ac chain remains authoritative with its recorded
qualifications: 14aa was historically unsuccessful; 14ab separates canonical
structure from raw solver provenance; 14ac governs finite final-float comparison.
The historical 14f protocol prohibits test changes whereas its report describes
a permitted fallback. Neither record is rewritten.

Planning source inspection found hardcoded guard results and missing unexpected
failure closure in the 14ad audit runner. New R3 wrapper tests must observe the
guards, and its outer boundary must close startup and callback failures. The
unchanged GovernedLauncher must be used. Any defect requiring historical
launcher or numerical primitive modification blocks R3; no repair or retry is
authorized inside this execution.

## Commands and launch contract

The only governed launch is `uv run --locked python
scripts/session_14r3_occlusion_study.py <command>`. Commands are `preflight`,
`verify-production`, `render-synthetic`, `run`, and `publication-check`.
Only `run` can reach development access; no independent prepare/analyze CLI.

Order: verify environment; produce prerequisite using the same interpreter and
fixed discoverable R3 enforcement tests; validate exact schema, command,
nonzero passing collection, implementation/protocol/lock/environment hashes;
validate committed synthetic, visual and green cross-platform CI evidence;
then enter the guarded callback. A launch-attempt marker reserves execution
only. The prerequisite uses the inherited producer identifier required by the
unchanged validator, while its command and implementation bindings identify R3.
No prerequisite may be supplied externally in place of executing the producer.

Observe context, environment, digest, direct-entry, marker, producer, warning
and downstream-failure injections with callback spies. Failed prerequisites
must cause zero downstream and access calls. Callback failures must retain
actual counts. Direct startup/environment failure is also recorded. No failed
execution resumes. Tests may inject synthetic callbacks; production exposes no
callback argument. Readiness derives from observed results, never fixed flags.

## Frozen numerical path and pre-access gates

Use unchanged representation_retry primitives: isotropic Gaussian, expanding
directional and constant-width directional fields, sigma 2 m, onset 1 m,
half-angle 10 degrees, float64 and origin–defender exclusion 1e-9 m. Stable
bounded union is primary; maximum is a comparator, neither a probability.
Directional tails persist without longitudinal attenuation.

Joint Simpson uses 256,512,1024,2048,4096,8192,16384 intervals and the first finer
vector with every component change at most 1e-7. Canonical partitions preserve
certified tie endpoints. Positive piece count N sets residual eligibility
width <= 1e-12/N, represented by integral bounds, never zero or midpoint.
Preserve interval-aware strict/repeat and onset-adaptive agreement 1e-10,
direct-Simpson agreement 1e-9, joint-maximum agreement 1e-6. The four historical
split-Simpson obligations are synthetic only. Degenerate edges use endpoints.

Run the assembled path for 108 cases, 366 references and 399 mapped permutations.
Require exact canonical structure, component order and accepted resolution;
finite final values use 64*epsilon64*max(1,abs(a),abs(b)); signed-zero bits remain
diagnostic. Observe continuity, tiny signs, certification, canonicalization,
residual routing, determinism and failure propagation. No empirical references
or factorial permutations are invented. Warnings, failed roots, certification,
nonconvergence or mismatches block.

Render the inherited synthetic geometry twice, byte-identical 1800x800 SVG:
origin (0,0); defenders (5,0),(18,-9),(18,9),(-7,4); receivers
(20,-10),(25,0),(20,10),(-10,5); extent x[-15,35], y[-20,20]. Three equal-aspect
union panels share [0,1] scale. Labels: SYNTHETIC; CARRIER-ORIGIN GEOMETRIC
OCCLUSION HYPOTHESIS; NOT VALIDATED COVER SHADOW; NOT SUPPRESSION. Full-frame
visual inspection precedes access. No empirical rendering or animation.
Green Python 3.11, Python 3.13 and distribution CI at the tested checkpoint is
mandatory before run.

## Population and descriptive specification

After authorization only, hash before selective parsing:
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`.
Require 7,227 states, historical alias counts 885,801,952,877,861,629,764,734,724.
Skip target values before analytical records; identity supports membership and
duplicate checks only, then discard event references. Prepare private geometry
and freeze its hash before fields, inside the single launch. No clipping,
exclusion, population repair, new data or model access.

Freeze the exact summary families and row definitions of the starting-commit
Session 14R2 runner's Collect, summarize_state and add_across functions:
endpoint and length-normalized segment-average fields over t in [0,1];
union-minus-maximum distributions and 1e-12 numerical equality; Session 13
nearest distances, d2-d1/d3-d1, fractional top-1/2/3 and multi-edge/support
correspondences; all three second-minus-first candidate comparisons with
Spearman and strict agreement/reversal, tie creation/removal, both tied;
endpoint/segment ordering; receiver/corridor divergence and opposing-pair field
ordering; across-state overlap/redundancy associations. Spearman only, average
exact-tie ranks; ordering uses 1e-12 block-anchor ties. Zero support is ownerless;
zero-strength fractions and unassessable correlations are unavailable.

Equal match -> state -> edge weighting; eligible pairs average within states.
All nine matches, represented counts, unavailable denominators, means, extrema,
weighted inverse-ECDF 5/25/50/75/95 percentiles. Session 13 gap percentiles are
distributions of state-average gaps. No outcomes, bins, practical thresholds,
extra correlations or winner score. Run once, deterministic state/candidate/edge
order, with persistent private stages and actual access/completion counts.

## Output schemas, failure preservation and interpretation

Public namespace: outputs/continuous_occlusion_retry_14r3/. The eleven artifacts
are candidate_summary.json, structural_correspondence.csv,
receiver_segment_divergence.csv, candidate_pair_comparison.csv, union_vs_max.csv,
multi_edge_summary.csv, match_summary.csv, synthetic_stress_summary.json,
qc.json, manifest.json and synthetic_field_comparison.svg. Authority records
live in authority/; details, mappings, geometry, traces and execution records
remain ignored under local/.

CSV schemas and row families inherit the exact R2 COLUMNS/KEYS definition at
the starting commit. JSON success schemas inherit R2 with explicit access
accounting in QC. Failure permits absent scientific files and records their
unavailability in the manifest; it cannot imply a complete comparison. New
authority records bind observed checks and stage, counts, hashes and environment.
Unexpected fields/nonfinite values/count or hash disagreement reject closure.
Atomic writes precede hash closure; publication verifies, never regenerates.
No partial empirical summary is public. An unexpected failure stops without
dropping edges, repairing code or restarting. Missing pre-access evidence is
unavailable, not an inferred pass.

A requires complementary directionality with coherent advantage for one
candidate; B both plausible; C little added description; D blocked/invalid.
Readiness separately 1–4 (1 later governed behavioral test only, 2 bounded
discrimination, 3 blocker diagnosis, 4 deeper construct issue). Only A freezes a
candidate. Guaranteed information differences, correlation or appearance alone
cannot select one. Retain PROXY ONLY accessibility, NOT SUPPORTABLE suppression,
shared geometry, fixed scales, tails, saturation, finite-grid coverage, offline
tracking and already-spent formerly reserved matches. No claim-ledger change.

## Checkpoints and delivery

Three commits: protocol/specification; tested wrapper, observed synthetic
acceptance and approved visual; closed evidence/report plus append-only log.
If a pre-access gate blocks, preserve tested failure evidence and explicitly
mark downstream artifacts unavailable instead of completing unauthorized stages.
Run focused/relevant/full tests, compilation, schemas/hashes, privacy/firewalls,
links, historical preservation, staged inspection and diff checks; report run,
pass and skip counts separately. Push reviewed commits, require green final CI,
verify synchronized heads and unchanged tag. Report 36 handoff items.

Exactly one recommendation: A/readiness1 separately governed behavioral
receiver-choice validation beyond M1; B bounded candidate discrimination;
C simpler geometry; D blocker diagnosis. Execute none.
