# Phase 14ab — Canonical comparison contract

Status: **FROZEN BEFORE IMPLEMENTATION OR NEW NUMERICAL EXECUTION**

Date: 2026-09-11

Session 14ab begins from synchronized `69b0ef4749cbb151f0f0dad5825165e911dc8bcb`; `v0.1.0` remains peeled to `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. Session 14aa remains historically **H — execution failure**, readiness 4. Its manifest-bound generated D/readiness-3 output remains preserved and qualified by its later report.

## Question and boundaries

This synthetic-only phase repairs one comparison defect: Session 14aa compared complete routing records and therefore treated the intentionally retained raw Brent return value as canonical structure. It may change comparison semantics and tests only. Root certification, onset certification, field formulas, owners, tie rules, partitions, joint Simpson, residual routing, convergence and reference tolerances, dependencies, expected vectors, released API, package version and Session 14R remain unchanged.

No empirical development state, provider product, target, outcome, model, option share, protected/reserved/withheld material, pose, xT, progression data, or Session 14R partial scientific output may be accessed.

## Two evidence layers

### Canonical production authority

Exact cross-platform equality is required for every production-relevant field:

- canonical directional onset coordinates and bits;
- canonical switch coordinates and certified pre/equality/post enclosures;
- owners before, at and after each switch and crossing pairs;
- tie enclosures and their endpoints;
- ordered structural partition coordinates and bits;
- micro-interval routing, piece widths and bounded-piece selection;
- residual bounds;
- accepted Simpson resolution;
- final component names, ordering, values and IEEE-754 bits.

Any difference in these fields makes `canonical_structural_equal=false`. No tolerance or provenance exclusion may hide a production difference. Algorithmic reproducibility additionally requires exact canonical partitions, ownership, routing, residuals and accepted resolutions. Full deterministic success requires all 366 final values to be bitwise identical.

### Diagnostic provenance

Raw Brent returns explain how certification began but do not define the production partition after the committed Session 14aa adapter replaces them with the certified first-post-switch float. Preserve raw root values, bits, residual information available from the frozen record, bracket/enclosure evidence available from certification, and source environment. Report them through `raw_solver_provenance_equal`; never delete, overwrite or silently normalize them.

A supported record may therefore state `canonical_structural_equal=true` and `raw_solver_provenance_equal=false` without contradiction.

## Comparator and regression contract

Add an internal comparator that projects explicit canonical and provenance views before equality. It does not recompute roots or numerical values. The Session 14aa regression contains local raw root `0.14285714285714282` and CI raw root `0.14285714285714285`, one ULP apart, with canonical root `0.14285714285714285`, identical partitions, owners, routing, residuals, accepted resolutions and final values. The historical whole-record comparison must fail; the repaired canonical comparison must pass while provenance equality fails.

Negative controls independently alter canonical root, owners, partitions, routing, accepted resolution and a final component. Every control must make canonical equality false. Tests also require raw provenance retention in local, CI, comparison and report records; deterministic serialization; and unchanged authenticated file-artifact transport.

The two historical `historical_vector_changed` tests are inspected but not modified. Diagnose whether they use raw/precanonical values, stale expected authority, another exact-equality path, environment ordering or another cause. A test repair is not authorized here unless a later protocol separately freezes it.

## Governed reproduction

After committing tested comparison tooling:

1. Run one local full synthetic diagnostic through the unchanged Session 14aa numerical path.
2. Push that checkpoint.
3. Dispatch exactly one Python 3.13 GitHub Actions diagnostic using the proven file-artifact transport.
4. Retrieve its artifact once and verify the declared/downloaded SHA-256 before parsing.
5. Compare all 108 vectors and 366 components under the two-layer contract.

No second governed dispatch or corrective rerun is permitted. Unexpected integrity, execution or post-exposure implementation failure closes the phase without repair.

## Outputs

Publish exactly these files under `outputs/cross_platform_canonical_comparison/`:

- `comparison_contract.json`
- `local_diagnostic.json`
- `ci_diagnostic.json`
- `canonical_comparison.json`
- `provenance_comparison.json`
- `historical_test_review.json`
- `qc.json`
- `manifest.json`

All outputs are synthetic, finite, sanitized, atomically written and hash-bound. Dense traces and execution records remain ignored.

## Decisions

Choose exactly one result:

- **A — CANONICAL CROSS-PLATFORM DETERMINISM ESTABLISHED; RAW PROVENANCE DIFFERENCE ONLY**.
- **B — CANONICAL STRUCTURE EQUAL; FINAL FLOAT EQUIVALENCE STILL NEEDS CONTRACT WORK**.
- **C — COMPARATOR REPAIR REVEALS A REAL CANONICAL DIFFERENCE**.
- **D — HISTORICAL TEST PATH STILL CONFLATES RAW AND CANONICAL AUTHORITY**.
- **E — MULTIPLE ISSUES**.
- **F — UNRESOLVED**.
- **G — INVALID / EXECUTION FAILURE**.

Readiness is 1 only when the complete production cross-platform contract and every required delivery gate pass. Readiness 2 applies when production determinism is established but ordinary CI remains blocked solely by a historical test-contract defect. Readiness 3 indicates remaining numerical or structural repair; readiness 4 indicates insufficient or invalid evidence.

If A/readiness 1 is earned, recommend exactly **separately govern a fresh Session 14R empirical representation retry**. If A or D/readiness 2 is earned, recommend exactly **perform one tiny historical-vector test-contract repair, require full CI green, then authorize Session 14R retry**. Other outcomes receive one bounded recommendation tied to the observed defect. Execute no recommendation.

## Closure

Preserve protocol, tested implementation, and closed evidence/report commits. Run focused Session 14ab tests, relevant Session 14 tests, the full local suite, compilation, 108/366 regressions, schema/hash and privacy checks, documentation links, history preservation, staged inspection, `git diff --check`, the single governed diagnostic and ordinary CI. Report governed and ordinary CI separately. Keep the claim ledger unchanged and stop without Session 14R execution.
