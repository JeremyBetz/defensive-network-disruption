# Phase 14R9L — Fresh empirical continuous-occlusion representation retry

**Frozen 2026-09-18 before implementation or development access.** Starting
authority is synchronized commit `3b8a6e578a9ca0c5be4b5836c00bafce1ef2c043`;
`v0.1.0` remains `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

## Authority and question

R9L asks whether the frozen expanding or constant-width carrier-origin fields
add complementary structural description beyond isotropic proximity and whether
one warrants later behavioral validation. It is a label-free development study.
R9J supplies the semantically equivalent linear publisher (PA); R9K supplies the
valid structural comparator repair (A/readiness 1). Their reports, protocols,
manifests, implementations, the R9/R9a nineteen-file schema, and the frozen
population authority are hash-bound by the R9L implementation.

No target, chosen receiver, outcome, model, utility, share, protected detail,
withheld match, pose, threat, progression, sister-project data, behavioral
validation, API, dependency, certificate, tolerance, field or scientific design
change is authorized. Historical files and classifications remain immutable.

## Execution contract

The isolated runner exposes `preflight`, `run`, and `publication-check`; only
`run` may authorize geometry. It uses fresh pre-access, empirical-attempt,
preparation and execution markers and a fresh
`outputs/continuous_occlusion_empirical_retry_r9l/` namespace. Startup and
publication failures retain independent emergency evidence. Marker existence
never authorizes access or permits repetition.

The implementation calls the R9K adapter explicitly and never patches a
historical module. It preserves R9E's summary definitions and deterministic
match → state → candidate → receiver order. The R9J stream validator is the
sole journal authority: it reads the journal once, returns an immutable bound
authority, and that result is reused for package creation and checking. The
historical prefix replay is prohibited.

Unlock requires exact inherited hashes, committed implementation, locked project
environment, passing prerequisite tests, fresh markers, green Python 3.11,
Python 3.13 and distribution CI for the implementation commit, and population
SHA-256 `cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`.
The population must contain 7,227 states with match counts 885, 801, 952, 877,
861, 629, 764, 734 and 724.

Preparation uses the unchanged lexical target-free projector. Projection
attempts and complete materialization receipts precede downstream work. Private
prepared bytes and hashes are frozen before evaluation. A complete run has
7,227 completed states, 72,270 completed geometric edges, 216,810 completed
candidate calls and no uncertain exposure.

The candidates are `isotropic`, `expanding`, and `constant_width`, with unchanged
parameters, stable union, maximum comparison, endpoint and length-normalized
segment fields, joint Simpson estimator, strict/repeat piecewise checks,
onset-aware certification, tie boundaries, micro-residual routing, direct and
joint agreement gates, certificate semantics, and R9K structural comparator.
The first numerical or execution failure stops the entire run. Preserve context,
stage, warnings, certificates, exposure, counters, completed work and traceback;
do not skip, repair, retry, continue or interpret partial science.

## Summaries, publication and decisions

Only complete execution permits the frozen receiver/segment, union-minus-max,
Session 13 correspondence, overlap/redundancy, multi-edge, receiver/corridor,
candidate-pair, ordering and nine-match summaries. Use Spearman only, inherited
ties, equal match → state → edge weighting, state means for pair-derived
continuous observations and state proportions for ordering indicators. Publish
raw and reduced counts, denominators, assessability and unavailable reasons.

The public package uses exactly the inherited nineteen filenames. Lifecycle,
exposure, markers, candidate work, warnings, certificates and publication state
live in `qc.json.progress_authority`. Success requires all files and complete
processing. Failure/blocked packages mark scientific artifacts unavailable and
contain no partial rows. Publication validates actual persisted files and hashes
against the single linear authority; it never regenerates evidence.

Choose A for one coherent, match-stable directional candidate, B for multiple
plausible candidates, C for little value beyond simple geometry, or D for
blocked/invalid. Readiness is independently 1 behavioral validation of one
frozen field, 2 descriptor/visualization only, 3 representation revision, or 4
not justified. Assess the inherited ten dimensions without weights or new
thresholds. Correlation, synthetic separation or visual appeal alone cannot
select a candidate. Accessibility remains **PROXY ONLY** and suppression
**NOT SUPPORTABLE**.

## Delivery

Preserve three commits: protocol; tested orchestration and authority bindings;
closed evidence/report/log. Before access run focused R9L, R9K, R9J, relevant
orchestration/lifecycle/access tests, full and tracked-only suites, compilation,
schemas/hashes, privacy, links, history and diff checks; require checkpoint CI.
After the single execution repeat closure validation, push, require final CI and
verify synchronized clean heads and the unchanged tag.

The report maps the requested 32 items. Recommend exactly one next action:
behavioral validation beyond M1 for A/readiness 1, bounded discrimination for B,
simpler geometry for C, or one bounded blocker diagnosis for D. Execute none.
