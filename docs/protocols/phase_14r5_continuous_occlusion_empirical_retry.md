# Phase 14R5 — Label-free continuous occlusion representation retry

Prospective protocol, 2026-09-12. Starting authority is clean local, tracking,
and live main `557644ccff2255b78311acf3e2e17334d61ddf06`. Preserve `v0.1.0`
at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. This protocol is committed
before numerical execution or development access. The operator is Codex under
the project author's explicit Session 14R5 execution instruction.

## Question and authority

Do the frozen directional fields provide complementary structural description
beyond isotropic proximity, and does one warrant later behavioral validation?
This is label-free, in-sample description of spent development geometry. There
is no behavioral or practitioner validation and no promotion of claims.

Bind the committed [Session 13 report](../session_13_defender_edge_influence_report.md),
[original field protocol](phase_14_continuous_occlusion_hypotheses.md),
[R3 protocol](phase_14r3_continuous_occlusion_empirical_retry.md), and applicable
Session 14c–14af authorities through the starting repository tree and an explicit
implementation hash inventory. Later numerical authorities qualify historical
results without rewriting them. Session 14aa's failed execution remains failed;
14ab separates canonical structure from raw solver provenance. The Phase 14f
protocol's test-change prohibition and its report's test-fallback description
remain contradictory historical records. Session 14ad's hardcoded guard flags
and incomplete unexpected-failure closure do not supply observed R5 evidence.

Planning-time static review found that 14af accumulates its journal in memory
and writes it at audit closure, its runner lacks an outer unexpected-failure
handler, and its pre-initialization validator does not inspect the supplied
journal. R5 repairs these in its new wrapper only. Preserve 14af's bytes and
historical classification. No earlier execution marker is reopened.

Preserve historical implementations, outputs, reports, README, claim ledger,
dependencies, lockfile, released APIs and competition-authorship policy. No
targets, outcomes, models, coefficients, utilities, option shares, acquisition,
protected detail, withheld material, pose, threat/progression work, historical
partial scientific summaries or empirical visualization are authorized.

## Launch, persistence, and publication

The separate runner exposes only `preflight`, `verify-production`,
`render-synthetic`, `run`, and `publication-check`. Only `run` can open development
geometry. The governed launch form is `uv run --locked python`. A new adapter
uses the unchanged `GovernedLauncher`, verifies the project interpreter,
installed environment and committed bindings, produces a fixed discoverable R5
enforcement-test record with that same interpreter, and validates collection,
nonzero passes, exit status, command, environment and hashes. It then validates
committed synthetic acceptance, visual approval and green Python 3.11, Python
3.13 and distribution CI for the exact tested checkpoint before the callback.

An exclusive attempt marker reserves execution without authorizing access.
Durable ignored journal entries have sequence numbers and predecessor hashes;
each append is flushed and synchronized before subsequent risky work. Access
attempts and successful-open receipts are separate. Unresolved attempts are
explicitly unavailable, never confirmed zero. Unique state/edge opens derive
from successful receipts, independently of processing. Preparation exposure
counts when geometry opens; rereading prepared geometry does not increment
unique access. Interrupted writes, truncation, reset, publication failures and
journal-write failures block success and retain available evidence.

Use the unchanged lifecycle transitions through an R5 adapter. A geometric
receiver edge starts once and completes only after all three field candidates
pass. Candidate calls have separate start/completion records. A state completes
only after all its edges and required state summaries are retained. The
candidate-major loop is state → isotropic/expanding/constant_width → receiver.
The active candidate-call context is retained separately from lifecycle totals.

Startup, producer, callbacks and publication have outer failure handling. Retain
pre-failure and terminal snapshots, active context, actual access, completed
work, exception category and private traceback. Four explicit contexts are
`pre_access`, `empirical_partial`, `empirical_failure`, `empirical_success`.
Partial is an internal stopped checkpoint, never scientific closure. A
pre-initialization claim requires an actually empty persisted access history.
Validate persisted QC, manifest, lifecycle summary and private evidence directly;
never synthesize replacement evidence from QC. Recompute hashes and reject
cross-file discrepancies. A defect requiring historical launcher, lifecycle or
numerical changes blocks R5 before access rather than expanding its repair.

## Frozen numerical contract

The three exact Session 14 formulas are unchanged: isotropic Gaussian proximity,
smooth expanding directional, and smooth constant-width directional fields.
Use sigma 2 m, smoothstep onset length 1 m, expansion half-angle 10 degrees,
float64, and origin–defender exclusion at or below 1e-9 m. Empirical origin is a
carrier-position proxy for ball origin; no measured ball position or colocation
is asserted. Directional persistence is an assumption, not a validated mechanism.
Stable bounded union is primary and maximum is the strongest-single comparator;
neither is a probability or suppression measure.

Compose unchanged primitives: uniform joint Simpson intervals 256, 512, 1024,
2048, 4096, 8192, 16384; accept the first finer vector only if every component
changes by at most 1e-7. Use canonical onsets/switches, certified tie endpoints,
and exact canonical partitions. Retain raw solver provenance separately. A
positive-width piece is bounded-residual eligible iff width <= 1e-12/N using
the actual number N of positive pieces. Retain rigorous integral intervals.
Strict/repeat and onset-adaptive agreement use 1e-10, direct 65,536 Simpson
uses 1e-9, and joint maximum uses 1e-6, all interval-aware. Historical split
Simpson applies only to the four frozen synthetic failures. Degenerate edges
retain endpoint behavior. No obsolete fixed-spacing integration route is used.

Replay the assembled path against all 108 cases, 366 references and 399 mapped
permutations. Exact canonical structure, component order and accepted intervals
are required; finite final historical values use
`abs(a-b) <= 64*epsilon64*max(1,abs(a),abs(b))`. Signed-zero bits remain diagnostic.
Reject nonfinite values. Observe continuity, tiny-sign, certification,
canonicalization, residual-routing, determinism and injected failure propagation;
derive readiness from observations. Empirical edges do not run factorial
permutations or invent reference vectors. Warnings or numerical disagreements
stop execution without changing a tolerance, omitting an edge or rerunning.

## Population and frozen analysis

Only after launch authorization, hash-check canonical bytes to SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`.
Require 7,227 states and historical alias-order counts
`885,801,952,877,861,629,764,734,724`. Selective projection skips target values
before analytical records exist. Identity supports membership/duplicate checks
only; discard event references. Preserve geometry, candidate order and anonymous
state-local ordinals. Preparation computes no fields, exclusions or clipping.
Freeze private prepared bytes and hashes before the first field calculation.

Freeze these families for all candidates and both combinations:

- Receiver endpoint and length-normalized segment-average field over t in [0,1].
- Union-minus-maximum distributions and absolute 1e-12 numerical equality.
- Session 13 nearest distances, d2−d1/d3−d1, fractional top-1/2/3 memberships,
  nearest/support-set overlap and multi-edge correspondence.
- Every candidate pair's second-minus-first differences, within-state Spearman,
  strict agreement, strict reversal, tie creation/removal and both-tied counts.
- Endpoint-versus-segment ordering within candidate and combination.
- Receiver/corridor nearest-role divergence and field ordering on opposing
  raw-distance pairs; exclude tied raw-distance pairs from that denominator.
- Inherited across-state overlap/redundancy Spearman associations.

Use exact-tie average ranks for Spearman and absolute 1e-12 block-anchor ordering.
All-zero individual support has no owner. Zero-strength fractions and constant
or insufficient correlations are unavailable. Weight equally by match → state
→ edge; average eligible pairs within states first. Report all nine matches,
means, extrema, weighted inverse-ECDF 5/25/50/75/95 percentiles, denominators,
represented matches and unavailable reasons. Historical Session 13 gap
percentiles are distributions of state-average gaps. No Pearson, empirical
matching, bins, practical thresholds, composite winner or post-exposure metric.

## Synthetic visual and engineering fixtures

Freeze origin (0,0); defenders (5,0),(18,-9),(18,9),(-7,4); receivers
(20,-10),(25,0),(20,10),(-10,5); plane x [-15,35], y [-20,20]. Render one
1800×800 SVG, three equal-aspect union panels, shared [0,1] scale and anonymous
labels. Display SYNTHETIC; CARRIER-ORIGIN GEOMETRIC OCCLUSION HYPOTHESIS;
NOT VALIDATED COVER SHADOW; NOT SUPPRESSION. Locked metadata/hash salt and two
byte-identical renders are required, followed by full-frame visual inspection
before development access. No historical image changes.

Before access, test real wrapper paths for environment/prerequisite failures,
direct entry/context/digest/marker guards, all 14af cross-file injections,
forged pre-initialization, journal truncation/reset/interruption, failed
publication, partial access, active-edge/between-edge failures and completed
processing followed by publication failure. Include candidate-major two-edge
and three-candidate lifecycle oracles, malformed projection and access receipts,
target skipping, population/path guards, exact equal-minimum stress fixtures,
summary/tie/order/weight/unavailability tests, complete synthetic success and
failure publication. Synthetic fixtures use neutral invented states and edges;
they do not open real data. Unexpected defects are preserved, not converted to
expected passing behavior. Historical-core defects stop further acceptance.

## Artifacts, checkpoints, and stop

Under `outputs/continuous_occlusion_retry_14r5/`, final success comprises exactly:
`candidate_summary.json`, `structural_correspondence.csv`,
`receiver_segment_divergence.csv`, `candidate_pair_comparison.csv`,
`union_vs_max.csv`, `multi_edge_summary.csv`, `match_summary.csv`,
`synthetic_stress_summary.json`, `lifecycle_summary.json`, `qc.json`,
`manifest.json`, `synthetic_field_comparison.svg`. Pre-access records live in
`authority/`; ignored `local/` retains journals, geometry, traces and partial
science. Failure manifests enumerate unavailable artifacts without placeholders
purporting to be computed evidence. Freeze exact row families and strict finite
success/failure schemas in the tested implementation before execution. Validate
counts, denominators, cross-file relationships and hashes before interpretation;
publication checks never rebind changed outputs.

Preserve three commits: this protocol/specification; tested implementation,
observed acceptance and approved visual; closed results and 39-item report plus
one append-only research-log entry. If a gate blocks, preserve incomplete gates
as unavailable and close without empirical execution. Run focused, relevant,
full suites, compilation, links, privacy, historical preservation, staged review
and diff checks. Record run/pass/skip separately. Push tested checkpoint for
mandatory green three-job CI before run; require green final CI and clean
synchronized heads and unchanged tag at closure.

Scientific A requires complementary structure and coherent advantage for one
candidate; B both remain plausible; C little additional description; D blocked
or invalid. Readiness 1–4 is independent: 1 permits only recommending separately
governed behavioral validation; 2 descriptive use with further discrimination;
3 revision/blocker diagnosis; 4 no justified continuation. Guaranteed information
differences, correlation and visual appeal alone cannot select a candidate.
Only A freezes a later candidate specification. Numerical blockage does not
falsify a field construct. Accessibility remains PROXY ONLY and suppression
NOT SUPPORTABLE. Discuss carrier-origin uncertainty, shared geometry, fixed
scales, persistent tails, union saturation, finite-grid switching coverage,
offline tracking and spent formerly reserved matches. No causal/value claim.

Recommend exactly one next action based on evidence; on A/readiness 1:
**separately govern behavioral receiver-choice validation of the frozen
directional field beyond M1.** B recommends bounded candidate discrimination,
C retaining simpler geometry, D a bounded diagnosis of the observed blocker.
Execute no recommendation automatically.
