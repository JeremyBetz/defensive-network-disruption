# Phase 06e — Official LFS transport and corrected protected evaluation

Status: PROSPECTIVELY FROZEN BEFORE IMPLEMENTATION AND RENEWED ACQUISITION.

## Authority and scope

Starting checkpoint: `aecdb860f6a3b354c4eb9631aff54b694fefe88f`, verified against live origin/main. Preserve every existing governed artifact byte-for-byte; append-only research-log findings are permitted. Sessions 6 and 6c remain D / 4 — INVALID; 6b remains B — bounded verifier repair; 6d remains B — official LFS protocol is the secure route. No historical implementation changes.

Source: `02a396ffd09b283c9f092fdedeff11da6d535b66`. Exactly ten reserved matches, ordered numerically: `1874553,1927964,1959846,1986691,1996436,2006363,2007448,2007721,2010085,2016236`, publicly aliased reserved_01 through reserved_10. Withheld `1953632` is prohibited. No development population regeneration, fitting, development scoring, dependency installation, passages, pose, orientation, network features, or scientific changes.

Prospectively reserved from this project stage forward. Session 1 schema inventory mechanically over-read post-header bytes into process memory, but no evidence indicates value-level content was surfaced, persisted, or used analytically.

Final-model SHA-256: `0383561e45bcd9f8d84eccc7e9733b2409536f61c39193fa87d08c36c6f6fd65`. Reuse original means, scales, feature order, coefficients, fitting QC and environment. Existing Session 6c identity-only source authority and Session 6d manifests are hash-bound inputs; do not open mixed twenty-match manifests or historical downloaded football records.

## Commands and commit boundaries

Separate runner: `scripts/session_06e_reserved.py`; transport helper: `src/defensive_network_disruption/data/session6e_transport.py`. Commands: preflight, verify-sources, acquire-reserved, prepare-reserved, score, publication-check. No automatic crossing of commit gates.

1. Commit this protocol before implementation or source inspection.
2. Commit implementation and tests, including synthetic command-boundary rehearsal, before upstream inspection.
3. verify-sources obtains bounded pinned commit/tree and authorized pointer metadata only, reconciles thirty source identities, and creates transport_authority.json. Commit it before acquisition.
4. acquire-reserved acquires all thirty products in ascending match order, metadata/events/tracking within match. All thirty must verify before any scientific parsing.
5. prepare-reserved applies unchanged Session 6 projection/input contract/population helpers. Commit population_summary.json before scoring.
6. score requires clean committed authorities, unchanged hashes and absent results/marker. Atomically create exclusive persistent marker before utilities. Close results after validation and before display. Commit reviewed results separately, then push and verify synchronized heads.

## Transport contract

Official endpoint: `https://github.com/SkillCorner/opendata.git/info/lfs/objects/batch`. Request one frozen lowercase SHA-256 OID and nonnegative integer size, operation download, transfers [basic]. Exactly one matching response object, no object error, duplicate JSON keys, missing identity, unsupported transfer or malformed action. Batch response limit 65536 bytes. Pointer limit 1024 bytes. Git API metadata bounded by the existing 4 MB verifier limit; pointer envelopes additionally bounded before decoding.

Download action must use HTTPS, hostname github-cloud.githubusercontent.com, default port, no userinfo or fragment, and only public routable DNS addresses. Reject redirects on every request, different origins, substituted URLs and private/reserved addresses. Normal TLS certificate-chain and hostname verification is mandatory. Never use the historical hand-built media URL. No git-lfs installation.

Batch requests have no repository credentials. Action headers may contain only Authorization, Accept, Accept-Encoding (identity only), or Content-Type, with string values without CR/LF and total size at most 8192 bytes. Reject cookies, Host, proxy authorization, ranges and hop-by-hop headers. Repository API credentials, if used, remain scoped to api.github.com; raw public products receive none. Signed URLs/action secrets are transient, never logged or included in exceptions. Access logs contain sanitized labels, origins, expected identities, attempt/category, phase, time and protocol/implementation identities only.

Verify commit → actual tree → authorized path/blob type/OID/size → decoded Git blob hash/size → LFS pointer OID/declared size → downloaded SHA-256/size before parsing. Contents API size is diagnostic only. Reuse corrected LF/CRLF/trailing-blank pointer semantics and ordinary Git verification. Reject traversal/symlinks/unauthorized products and matches before opens/requests.

## Retries and immutable state

At most two retries after initial request, waits 1 then 2 seconds, for timeout, reset/dropped connection, explicit interrupted-stream exception, or HTTP 500/502/503/504 only. Each attempt uses identical pinned object identity and a fresh exclusive temporary file. Discard incomplete bytes; never resume/combine streams. No retries for TLS, redirects/substitution, hash/size/OID mismatch, cleanly completed truncation, schema or input-contract failure. Exhaustion stops execution. Verified products cannot be automatically redownloaded; failed acquisition/preparation commands cannot automatically restart.

After an eligible payload transport failure only, refresh an expired signed action with the same OID/size. At most two refreshes, without resetting the three payload-attempt budget. Each batch request has its own three-attempt transport bound. Prefer expires_in relative to monotonic receipt; otherwise use timezone-aware expires_at. Reject malformed/initially expired expiry. Missing expiry means reuse; 401/403 never trigger refresh. Preserve completed products and failure metadata, discard incomplete transport bytes.

## Scientific and projection authority

Reuse Session 6 strict input contract unchanged. Metadata projection: home_team.id, away_team.id, home_team_side, players[].id/team_id/playing_time.by_period[].name/start_frame/end_frame. Events: event_id,event_type,pass_outcome,period,time_end,player_id,player_in_possession_id,player_targeted_id. Tracking: frame,period,timestamp,player_data[].player_id/x/y. No excluded vendor values in retained application records or diagnostics.

Conflicting populated carrier references and unsupported identities, clocks, schema/directions/intervals stop. Existing missing targets/states and duplicate-event semantics retain the six-category waterfall, including independent candidate construction and target-outside evaluation/fit distinction. Never repair a reserved case. Latest same-period frame strictly before pass transition at most 100 ms old remains a prospective cadence-based draft, not timing truth. No interpolation or tuning. Preserve x'=s*x,y'=y, active finite teammates/opponents including goalkeepers, backward options, no distance/offside filter.

M0/M1/M2 and numerical ordering remain frozen; attenuation is sorted-distance math.fsum(exp(-d/5)); segment tolerance 1e-9 m is implementation-only. Scoring cannot acquire, parse provider records, fit or estimate preprocessing. Expected tie credit uses 1e-12 block-high ties, target-outside zero credit, within-match means and equal ten-match means. No additional metric/statistical test/effect threshold.

## Outputs, closure and failure schemas

Fresh ignored data/session_06e and outputs/reserved_evaluation_v3/local contain raw bytes, source mapping, alias map, detailed populations, receipt/access ledger and exclusive stage markers. Append every request attempt and every projected match access immediately. Structural preparation counts as protected-value exposure. No implementation amendment after acquisition begins.

Public namespace outputs/reserved_evaluation_v3 has transport_authority.json, population_summary.json, three model match CSVs, m1_m0_paired.csv, m2_m1_paired.csv, aggregate_metrics.json, qc.json, manifest.json. Public identities are hashes/stable aliases only. Population reports raw attempts, explicit target numerators/denominators, six exclusions and overlapping QC, separate evaluation/fit counts, target-outside reasons, candidate/defender summaries and hashes. Ten nonempty evaluation groups required.

Success schemas require exact fields/aliases, integer counts, finite bounded metrics, model denominator equality, paired/aggregate consistency, and complete authority/output hashes. Atomic generated files are validated before closed manifest/marker and any performance display. Never manually edit result bytes. Manifest self-hash belongs in decision brief.

Failure manifest status invalid contains failed command, sanitized exception category, stage/exposure status, completed verified-product counts, hashes of available public outputs, unavailable artifacts and historical/protocol/implementation identity. It must not require population/metrics to exist or fabricate results. Retain partial files and mark them unclosed. No rerun. If performance was computed or exposed, preserve it and stop without patch/rescore. Failure publication checks must be rehearsed before access.

## Acceptance and interpretation

Test batch/cardinality/identity/schema, HTTPS/DNS/redirect/header/TLS/redaction, all retry branches/expiry/fresh bytes, Git/LFS regression133/90729279, unsafe paths/partitions, sentinel projection, frozen timing/identity/candidate/features/metrics, no-score preparation/no-fit scoring, exclusive execution, and synthetic full lifecycle and failure closure. Run focused and full active synthetic tests, compile, schema/hash/publication/changed-artifact/staged/diff checks; passes/skips separate. Provider checks only through authorized commands. Verify historical hashes before/after.

Report primary M1−M0 before secondary M2−M1: all paired MRR differences, means/medians/sign counts and Hit@1/3 agreement. Primary A replication/B mixed/C no replication/D invalid; secondary 1 replication/2 mixed/3 no replication/4 invalid, descriptively without automatic thresholds. Tier B vendor targets, accessibility PROXY ONLY, suppression NOT SUPPORTABLE, offline extrapolated tracking; no causal/tactical/best-pass claims.

Decision brief docs/session_06e_corrected_reserved_evaluation_decision_brief.md maps visible requirements and inherited closure to evidence/unavailable without inventing missing report-item counts. Append research-log findings. Recommend exactly one separately governed development construct-validity review if primary replicates, development error/passage audit if primary mixed/absent, or bounded integrity review if invalid. Do not execute it. Stop after Session 6e.
