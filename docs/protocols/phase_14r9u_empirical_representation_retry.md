# Phase 14R9U — Fresh empirical continuous-occlusion representation retry

**Frozen 2026-09-21 before implementation or development access.** Starting
authority is clean synchronized commit
`5fb13b22f18c52498c88533314fd5f265493dc0e`; annotated `v0.1.0` remains
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

## Question, scope, and inherited authority

R9U performs exactly one label-free development representation run to determine
whether either frozen directional carrier-origin field adds complementary,
coherent and match-stable structural description beyond isotropic proximity and
therefore warrants a later behavioral receiver-choice study. It is not
behavioral validation. No target, chosen receiver, rank or pass outcome, M0/M1/M2
utility, M1 share, reserved or withheld detail, pose, threat, progression,
sister-project data, model, dependency, public API, certificate, tolerance,
field or scientific redesign is authorized.

The following committed protocol/report/manifest SHA-256 triples are authority:

- R9J: `8c205e768840260dc81ea7bb000d8edaa143de75eb0158d3a80bb7f3e43de9dd`,
  `7b1f4a2ddd0c4b8dc3030aa156c7a4fe058298b0132a3934bf0a95c767af2465`,
  `cbe421fe0fec781e2456e811806e437ad0dc711f958f05a0d9f1ed7d13602a96`.
- R9K: `a065af8026f1e4e0da8387b0a3d354d8c24adc6447df83167b6922e1aab56f36`,
  `3796f1e1403e7954e06a2a20213ee8cdf76c4b023dbc7f0e11d5e3f55e62519f`,
  `bb50db6e665a13089d76079216bdd2a28596e98c7dbb97281807be39d81e6df5`.
- R9M: `4c17117ae483ffddcaa4859de292cba795a8682754f15474df811b007c7b1e28`,
  `995378793b2bcd20a553190c7227fed42de8655273de956c5739c303d331baf2`,
  `222364223e3c6a9b4b9a91605794593d4ddacf2a42ba3cf509c2bd4ad0f41e26`.
- R9P: `5a8f39510d87454e02e0523ee562a595adf2f447ca9eb7dd1381d39d35df80a3`,
  `39afc9785f66e37c772d5499068b0dfb124469573593d60768ca883a043f65a6`,
  `fdac49eece5fe3f2d545561bec1a98d73534a31c3a692705903fd58539f655f7`.
- R9Q: `71a7268cc5f8c793a964eb31dab576b68b87c45e787ff167678bee0ca7d74651`,
  `e378019d005cceba0f8d310741930b865dadf4a97d1987ae9400e9796ee1879e`,
  `8d24c86ac54f8e2484dc7f77e796650cdfbe223eefaa0ce00cf4b9e223de1bc6`.
- R9R: `55efc2d087904e6d2f13e203089022c22c812c4f5327e98f4efd0e228865ccfa`,
  `14dae860c82471b355537c8575c07231203eabc9f248b4e1651de9b1c48b9870`,
  `448b3a389b29b4d6a7e447cb8f72bf3b47a1061d39f85079ed978f1ac0281b07`.
- R9S: `ae135b538eb2efbb722354cedd471ed25b56f5a44fa6e14e458c4e0e5b24aa05`,
  `86543925eaa01b9674de10d712e8b4202f16f10652f9dd50e7e34bb540f84206`,
  `512901bfc5a56f2ae2afe9693149cad4a29c01e2fb7bb5c03878ff7948183aaa`.
- R9T: `f836d89afef8f4d053c20f4daf4d2c622a38e6ed91a5561d1c063931d7a93597`,
  `c459e09148bfbba070bc6a6d7eb7ce90ee39444f8c0896c5f298fff7199ffed8`,
  `46a287059a94ae648254125771c06d8981de1f29b26f2c235ae86c25230c4c22`.

R9J is PA, R9K is A/readiness 1, R9M is A/readiness 1, R9P is
A/readiness 1 for retained-journal authority, R9Q is NC/topology I/PA,
R9R is D/readiness 3 historically, R9S is BC/pattern F/PA/diagnostic
readiness 1, and R9T is A/readiness 1. All historical files and decisions
remain immutable.

## Fresh orchestration and checkpoint authority

The isolated R9U runner exposes only `preflight`, `run`, and
`publication-check`; only `run` may authorize geometry. It uses
`outputs/continuous_occlusion_empirical_retry_r9u/` and fresh pre-access,
empirical-attempt, preparation, execution and publication records. It must not
reuse R9L or R9N markers, journals, outputs or prepared data.

The runner explicitly calls the R9T adapter. That adapter reuses R9R
localization, R9T canonical binary64 transitions, R9K structural comparison,
and the unchanged production estimator, owner certification, strict/repeat
piecewise verifier, micro-residual routing, direct and joint gates, warning
policy and certificates. Historical implementations are neither patched nor
substituted at runtime.

Startup and imports remain inside R9M failure protection. Terminal success and
failure use the prospective terminal adapter: the original traceback is
synchronized before one terminal event, R9J performs exactly one linear review,
and its immutable authority is reused for private and public validation.
`Progress.failure()`, historical snapshot reconstruction and prefix replay are
prohibited and protected by tripwires.

Before access, the protocol and tested runner must be committed and pushed.
The exact implementation commit must pass terminal-success jobs `distribution`,
`test (3.11)`, and `test (3.13)`. Exactly one canonical R9M receipt then binds
that commit, repository, workflow name/path/hash, run and job IDs, completion
times, protocol hash, runner hash, lockfile hash and provenance. Governed
validation is offline. Missing, altered, noncanonical, pending, failed,
cancelled, wrong-commit, wrong-workflow or wrong-implementation authority blocks
before any empirical marker or geometry access.

A bounded pre-access correction, if required before an empirical marker exists,
must be a separate forward commit. Shared history is never amended. All local
and CI gates then rerun and the only usable receipt is captured for the corrected
HEAD.

## Population, preparation, and single execution

The canonical population must have SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`,
7,227 states, and match counts 885, 801, 952, 877, 861, 629, 764, 734,
and 724. Mismatch blocks access.

After all pre-access gates pass, reserve exactly one empirical attempt before
possible geometry exposure. Use the unchanged lexical target-free projection,
persist every attempt and complete materialization receipt, and freeze private
prepared bytes and hashes before evaluation. Opened states and edges derive
only from durable receipts; interrupted receipts remain uncertain.

Execute canonical match then state then candidate then receiver order. Candidate
order is `isotropic`, `expanding`, `constant_width`; receiver order is canonical.
A complete run requires 7,227 states, 72,270 geometric edges and 216,810
candidate evaluations, with no unresolved exposure. Stable union is primary;
maximum remains the comparator. Preserve sigma 2 m, onset 1 m, expansion 10
degrees, constant-width definition, float64 behavior, the 1e-9 m
origin-defender exclusion, carrier-position proxy semantics, receiver endpoint
field and length-normalized segment-average field.

The first genuinely new numerical or execution failure stops the whole run.
Preserve exact stage, exception, traceback and hash, active match/state/edge/
candidate, warning and certificate status, lifecycle and exposure, and completed
work. Do not skip, repair, continue, retry or interpret partial science.

## Scientific summaries, publication, and decision

Only all-state completion permits scientific publication. Retain candidate
summary; receiver and segment fields; union-minus-maximum; Session 13 distance,
gap and multi-edge correspondences; overlap/redundancy; receiver/corridor
ordering; candidate differences; five ordering categories; and nine-match
summaries. Candidate differences remain second-minus-first at corresponding
receivers.

Use Spearman only with inherited exact-tie and 1e-12 block-anchor behavior.
Weight equally by match, then state, then edge. Reduce pair-derived continuous
values to state means and ordering indicators to state proportions before
extrema or inverse-ECDF percentiles. Retain raw source counts, published
observation counts, denominators, assessability and unavailable reasons.

Use the reconciled nineteen-file schema: the nine inherited pre-access and
synthetic artifacts, candidate summary, seven scientific CSVs, `qc.json`, and
`manifest.json`. No twentieth lifecycle file is allowed. Lifecycle, exposure,
markers, candidate work, warnings, certificates and publication status reside
in `qc.json.progress_authority`. Hash-close private evidence before public
interpretation. Failure packages mark scientific artifacts unavailable and
publish no partial rows.

Choose exactly A for one complementary, coherent and match-stable directional
candidate warranting later behavioral validation; B for multiple plausible
directional candidates; C for little additional description beyond simpler
geometry; or D for blocked/invalid execution. Assign readiness independently:
1 for behavioral validation of one frozen field, 2 for descriptor/visualization
only, 3 for representation revision, or 4 for not justified. Correlation,
synthetic discrimination or visual appeal alone cannot support A. Only complete
A/readiness 1 evidence freezes the selected field and full numerical contract.

Accessibility remains **PROXY ONLY** and suppression **NOT SUPPORTABLE**. No
causal effect, interception probability, defender value, player rank, best pass,
tactical intent or validated cover shadow may be claimed.

## Validation, chronology, and stop

Before access run focused R9U, R9T, R9R, R9K, R9M, R9J, lifecycle, projection,
publication and relevant R9 tests; the full active suite; clean tracked-only
validation; compilation; schemas/hashes; privacy and access guards;
documentation links; historical-byte checks; staged inspection; and
`git diff --check`. Tests cover direct-entry rejection, CI receipt failures,
authority drift, marker collisions, interrupted receipts, uncertain exposure,
candidate-versus-edge accounting, deterministic order, target exclusion,
first-failure closure, traceback binding, legacy tripwires, complete synthetic
success, unavailable failure packages, summary definitions, ties, state-first
reduction, nine-match aggregation and persisted-package tampering.

Preserve three principal commits: `research: freeze R9U empirical retry`, tested
R9U orchestration and authority bindings, and `research: close R9U empirical
retry`. After the one execution, validate persisted private/public evidence,
run the frozen post-execution checks, close the report and append-only log, push,
require final green distribution/Python 3.11/Python 3.13 CI, and verify clean
synchronized local/tracking/live heads and the unchanged tag.

The report maps all 34 handoff items and returns exactly one recommendation:
behavioral receiver-choice validation beyond M1 for A/readiness 1; one bounded
candidate-discrimination phase for B; simpler geometry without behavioral
escalation for C; or one bounded blocker diagnosis for D. Execute no
recommendation automatically.
