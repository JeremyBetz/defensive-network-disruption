# Phase 14R9 — Continuous occlusion representation retry

Frozen 2026-09-15 from clean synchronized
`d7a66699f133c36a6148303d3f420336473e58ce`. Preserve annotated `v0.1.0`
at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa` and every historical Session
14R/R2 artifact, marker, output, protocol, report, and classification.

## Question and scope

This label-free development study asks whether the frozen expanding or
constant-width carrier-origin directional field adds complementary structural
description beyond isotropic proximity, and whether one warrants separately
governed behavioral receiver-choice validation. The empirical origin is the
**carrier-position proxy for ball origin**. Accessibility remains **PROXY
ONLY** and suppression **NOT SUPPORTABLE**.

R9 uses fresh protocol, report, output, pre-access, retained-observation,
synthetic-acceptance, and empirical markers under
`outputs/continuous_occlusion_empirical_retry_r9/`. Historical 14at remains
A/readiness 1, but R9 independently establishes certificate orchestration,
runner-level success and failure publication, import failure closure, and a
fresh synthetic acceptance.

No target, chosen receiver, rank, outcome, model utility, option share, M2,
formerly reserved or withheld detail, pose, xT, progression, sister-project
data, empirical visualization, or behavioral validation is authorized.

## Pre-access contract

Only `run` may authorize development geometry. The isolated runner exposes
`preflight`, `verify-production`, `render-synthetic`, `run`, and
`publication-check`. Standard-library startup code claims the attempt and
catches package imports inside its governed failure boundary. It persists
original exceptions independently of normal publication.

Gate A uses only hash-verified retained Session 14ar evidence at the
quadrature-result boundary. It passes the retained adaptive observation through
the same R9 maximum-evidence orchestration available to empirical execution:
warning preservation, registry lookup, exact authority validation, containment,
width validation, independently certified piece status, conservative
aggregation, readiness propagation, and publication. It performs no field
evaluation or geometry access. Its certificate scope is terminal interval 8
only; it certifies neither a whole integral nor an unrelated
`piecewise_unsplit` disagreement. Empirical use requires an independently
derived exact runtime request. Similarity, copied authority identifiers,
automatic generation, or fallback use is forbidden.

Gate B invokes the actual runner in isolated namespaces with controlled
package-import, post-marker, certificate-authority, and publication-validation
failures. The runner must catch each failure and publish its stage, exception,
traceback, progress, false readiness, absent success, marker state, and zero
empirical access. Gate C runs a valid control through the same entrypoint.
Publication success and readiness derive from observed validator results. The
initial package is validated, its receipt is persisted, then the final
hash-closed package is validated again.

Gate D evaluates the accepted synthetic fields separately from Gate A. It must
pass 108 cases, 366 component references, and 399 mapped permutations with
exact canonical structure, component order and accepted resolution, and the
current 64-epsilon finite-component contract. It covers field evaluation,
switching, ties, joint Simpson, micro-residual routing, ordinary maximum checks,
and permutation behavior. It does not validate Session 14ar provenance.

Gate E renders the inherited deterministic 1800×800 SVG twice with isotropic,
expanding, and constant-width union fields, identical geometry, equal-aspect
panels, shared `[0,1]` scale, and the labels `SYNTHETIC`, `CARRIER-ORIGIN
GEOMETRIC OCCLUSION HYPOTHESIS`, `NOT VALIDATED COVER SHADOW`, and `NOT
SUPPRESSION`. Full-frame review must confirm legibility and no clipping or
overlap.

The immutable pre-access readiness conjunction requires Gates A–E, exact
certificate scope, runner failure and success publication, 108/366/399,
determinism, integrity, and zero empirical access. There is no override. An
unexpected governed gate failure closes R9 blocked without repair or rerun.

## Numerical contract

The fields are isotropic Gaussian proximity, smooth expanding directional, and
smooth constant-width directional with `sigma=2 m`, onset `a=1 m`, expansion
half-angle `10 degrees`, float64 arithmetic, and the `1e-9 m` origin-defender
exclusion. Stable bounded union is primary; maximum is a comparator. Neither is
a probability, suppression, interception likelihood, attribution, or value.

Production remains the unpartitioned joint Simpson vector at
`256,512,1024,2048,4096,8192,16384`, accepting the first finer vector when
every component changes by at most `1e-7`. Preserve onset-aware certification,
canonical partitions, certified tie endpoints, degenerate endpoints, and
micro-residual eligibility `width <= 1e-12/N`. Preserve strict/repeat,
onset-only, direct-Simpson, and joint-maximum agreement gates. Historical
split-Simpson applies only to the four frozen synthetic failures. A certified
piece cannot waive a whole-integral gate. Warnings remain recorded; unmatched
blocking warnings fail closed.

## Population, summaries, and aggregation

After green Python 3.11, Python 3.13, and distribution CI for the committed
pre-access checkpoint, reserve the fresh empirical attempt before opening
geometry. Verify canonical population SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`,
7,227 states, and match counts `885,801,952,877,861,629,764,734,724`.
Lexically skip targets, discard event references after integrity checks, and
persist projection receipts before evaluation. Freeze private prepared bytes.

One geometric edge has three candidate calls. Execute state → candidate →
receiver deterministically. An edge completes only after all candidates and
checks; a state completes only after all edges and summaries. Any numerical or
execution failure stops the full run. Do not omit edges, interpret partial
science, repair, or restart.

Publish endpoint `receiver_field` and length-normalized `segment_integrated_field`
for all candidates and union/maximum; union-minus-maximum with absolute
`1e-12` equality; frozen Session 13 distances, gaps, top-k involvement,
support overlap, nearest-role divergence, and multi-edge correspondence; all
three candidate-pair second-minus-first differences, Spearman correspondence,
and five ordering categories; and endpoint-versus-segment ordering. Equal-
minimum fixtures remain synthetic formal-capacity evidence only.

Use Spearman only, exact-tie average ranks, and `1e-12` block-anchor ordering.
Unavailable or constant groups remain explicit. Use equal match → state → edge
weighting. Pair differences/support overlap reduce to one state mean and
ordering indicators to one state proportion before extrema and inverse-ECDF
percentiles. Preserve raw source counts separately.

## Evidence, decisions, and closure

The public namespace contains exactly the nineteen requested artifacts:
`preaccess_contract.json`, `retained_observation_certificate_gate.json`,
`runner_failure_oracles.json`, `runner_success_oracle.json`,
`synthetic_acceptance.json`, `evidence_type_summary.json`, `visual_qa.json`,
`candidate_summary.json`, `structural_correspondence.csv`,
`candidate_pair_comparison.csv`, `receiver_corridor_ordering.csv`,
`union_vs_max.csv`, `overlap_redundancy.csv`, `multi_edge_summary.csv`,
`match_summary.csv`, `synthetic_stress_summary.json`, `qc.json`, `manifest.json`,
and `synthetic_field_comparison.svg`. Lifecycle, exposure, and candidate-work
counters live inside QC. Detailed records remain private. Failure schemas mark
scientific artifacts unavailable. Publication checks validate existing hashes
without regeneration.

Choose scientific A for one defensible directional candidate, B for multiple
plausible candidates, C for little additional description, or D for blocked or
invalid. Choose readiness independently: 1 behavioral validation of one frozen
field, 2 descriptor/visualization only, 3 representation revision, or 4 not
justified. Assess interpretability, smoothness, directionality, composability,
multi-edge behavior, Session 13 correspondence, equal-minimum discrimination,
isotropic distinction, software simplicity, and visualization usefulness
without weights. Correlation or visual appeal alone cannot select a candidate.

Preserve three commits: this protocol; tested pre-access authority and visual;
closed results/report/log. Run focused, relevant, and full tests, compilation,
schemas, hashes, privacy, links, historical preservation, staged inspection,
diff checks, and CI. Leave README and claim ledger unchanged. Recommend exactly
one next action dictated by the result and execute none.
