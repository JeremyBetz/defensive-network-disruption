# Phase 14s — Independent-maximum integration warning diagnosis

Status: **FROZEN BEFORE DIAGNOSTIC IMPLEMENTATION AND REPRODUCTION**
Date: 2026-09-11

## Authority and question

This bounded numerical diagnosis begins from clean synchronized commit
`3f3952d99987ebb0e753484adc82825d884fe383`. Annotated release `v0.1.0`
must remain at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

Session 14R remains historically **D — BLOCKED**, readiness 3. Its preserved
failure records show one completed development state followed by SciPy
`IntegrationWarning` in strict piecewise maximum integration. Session 14s asks
only: what exact numerical condition caused that warning, and is it confined to
the independent verifier or does it indicate a production-field or integration
defect?

The Session 14R manifest, canonical population and prepared population retain
SHA-256 identities `8a639f7a2b29a6aec026e176d80d8901b28839b0d2091dd0256751d77b6f70ed`,
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`
and `15a809060f0ba4a8cb3790011dab8008db3dad6e931c287ccb395f61ef1530b0`.
Historical Session 14c–14i numerical qualifications and implementations remain
unchanged.

## Authorized record and reconstruction

The retained Session 14R failure has `states_completed=1`. The runner increments
that value only after `summarize_state` completes, so the failing state is the
next canonical prepared record: zero-based row ordinal 1. Recover only that
row's stable development alias, carrier/candidate/opponent coordinates and
candidate ordering. Recover the corresponding canonical integrity key through
the existing restricted projection, retain it privately and publish only its
SHA-256 plus a neutral state key. Never decode or retain target fields.

The historical traceback did not preserve candidate family or receiver ordinal.
Locate them during one governed reproduction by traversing the frozen family
order `isotropic`, `expanding`, `constant_width` and canonical receiver ordinals,
stopping at the first warning. Capture the active integration piece. If the
exact class and message are not reproduced in this state, close **G —
UNRESOLVED** and inspect no other empirical state.

The only permitted development geometry is this one state and, after location,
the one failing edge. No Session 14R partial summary may be read or interpreted.
No targets, ranks, outcomes, models, coefficients, utilities, option shares,
M2, provider acquisition, protected/reserved detail, withheld data, pose, xT or
progression material is authorized.

## Frozen numerical diagnosis

Preserve every field formula, scale, tolerance, partition rule and estimator.
Do not modify the joint Simpson production estimator. For the failing edge only:

- evaluate the unchanged all-component Simpson ladder 256, 512, 1024, 2048,
  4096, 8192 and 16384, accepting the first finer vector whose every change is
  at most `1e-7`; report convergence, interval count, maximum change, finiteness
  and repeated-run determinism;
- reconstruct the maximum envelope using the accepted 65,536-interval switch
  scan and certified boundaries; report defender, onset, switch, tie interval,
  enclosure and piece counts plus normalized piece-width extrema and median;
- preserve adjacent-float enclosure endpoints and identify every piece whose
  endpoints are adjacent representable float64 values;
- reproduce the exact warning once through the unchanged strict piecewise path
  (`epsabs=epsrel=1e-13`, `limit=1000`), recording the active subinterval,
  warning class/text and stack location;
- call locked SciPy full-output quadrature only on that failing piece, retaining
  bounded estimate, error estimate, evaluation/subdivision counts and
  termination text; dense work arrays remain private;
- verify maximum values and owner transitions around relevant boundaries with
  the previously audited independent scalar oracle. Derivative/slope probes are
  diagnostic and do not prove continuity;
- compare current strict and repeated piecewise adaptive values, onset-only
  unsplit adaptive value, direct 65,536-interval Simpson, and existing
  switch-split Simpson at nominal 32,768 and 65,536. Report pairwise absolute
  differences without changing any tolerance or treating a warning as success.

Separate warning emission, failure to meet requested tolerance and demonstrated
estimate disagreement. Diagnose whether the inherited `1e-13` adaptive demand,
piece topology, adjacent-float enclosures, subdivision behavior, roundoff,
slow convergence, partition wiring, or field continuity explains the warning.
Do not define a new tolerance or minimum-width rule.

If and only if an adjacent-float or exceptionally narrow certified piece is
present and the empirical diagnostics cannot distinguish generic quadrature
behavior, construct one synthetic control with the same piece topology and no
provider coordinates. Its use and reason must be recorded. No second empirical
state is permitted.

## Implementation, tests and execution

Add one internal diagnostic module, discoverable synthetic tests, and a separate
runner exposing only `preflight`, `diagnose` and `publication-check`. The runner
uses exclusive ignored markers and append-only ignored records, requires clean
committed protocol and implementation identities, and refuses automatic rerun.
Warnings and failures remain blocking. Unexpected exposed failure closes and
stops without repair.

Before governed reproduction, tests use synthetic geometry or mocked restricted
records only. They cover deterministic state/edge location, warning capture,
full-output parsing, partition widths, continuity/oracle checks, method
comparisons, optional topology control, strict schemas, failure preservation,
marker collision and absence of target/model/share/acquisition routes. Tests do
not reproduce the empirical warning. Commit tested tooling before the one
governed `diagnose` command.

## Outputs, decisions and closure

Publish exactly these compact files under
`outputs/continuous_occlusion_max_warning_diagnosis/`:

- `failure_authority.json`
- `partition_summary.json`
- `method_comparison.json`
- `warning_diagnostics.json`
- `qc.json`
- `manifest.json`

Coordinates, identities, original event key, dense work arrays and detailed
traces remain ignored. Public output uses stable alias, neutral row/edge keys,
bounded diagnostics and hashes. Schemas reject unexpected/nonfinite fields;
publication validation verifies existing hashes and never rebinds outputs.

Choose exactly one classification: A verifier-only warning with healthy
production estimator; B fragile/over-strict piecewise verifier contract; C
partition or tie-enclosure defect; D production estimator also affected; E field
or maximum-representation defect; F multiple independently demonstrated issues;
G unresolved. Separately choose readiness 1 bounded verifier-contract repair, 2
small implementation repair, 3 production/representation numerics need revision,
or 4 more evidence required.

Close and hash evidence before interpretation. Discuss a repair boundary only;
do not implement it or rerun Session 14R. Preserve three commits: this protocol;
tested diagnostic implementation; closed outputs/report plus append-only research
log. Run focused, relevant and full tests, compilation, schema/hash, privacy,
links, history, staged and diff checks; push and verify CI, synchronized heads
and unchanged tag. The final report maps all 28 requested handoff items and
recommends exactly one separately governed next action.
