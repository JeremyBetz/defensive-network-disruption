# Phase 14g — Production verification wiring and failure enforcement

## Prospective authority

Frozen 2026-09-11 from clean local, tracking and live `main`
`8ef0a27ebe9f0ac7ca6b4a754cfa7168c8b34ba5`. Preserve release target
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. Session 14R remains paused.
This is a synthetic-only software verification repair. No empirical population,
provider product, target, model, share, protected/withheld/pose, xT or progression
access is authorized. No formula, parameter, dependency, release or claim change.

Planning source inspection found that Session 14e computes the controlled vector
before detecting certified partitions, and that its failure records change
readiness booleans rather than inject failures into numerical execution. These
are static source findings, not new numerical results. Session 14f's protocol
explicitly prohibits test changes, while its report describes a test fallback
as protocol-permitted. Preserve both contradictory records and all historical
code, tests, protocols, outputs and classifications. This protocol supersedes
only prospective production-wiring and failure-enforcement authority; it does
not rewrite or retrospectively validate those records.

## Exact numerical contract

One new internal entrypoint combines the existing field evaluator, repaired
switch detector, independent maximum verification, unchanged controlled vector,
reference validation and immutable readiness. No parallel field implementation.
The new runner exposes only `preflight`, `audit`, `publication-check`.

The uniform-grid joint Simpson vector retains the exact existing component order,
float64 operations and interval ladder 256, 512, 1024, 2048, 4096, 8192, 16384.
Accept the first finer vector only if every component changes by at most `1e-7`;
cap exhaustion blocks. Require exact historical accepted values and interval
choices in the locked environment, and errors at most `1e-6` against the 362
Session 14b plus four Session 14c references. Certified partitions do not alter
this vector or its stopping rule.

Certified maximum verification uses endpoints, analytical onset points, repaired
switch partitions and both endpoints of each tie-boundary enclosure. Validate
finite ordered points in [0,1], adjacent float64 boundary endpoints, equality
inside and inequality outside, positive maximal support and owner consistency.
Use exact duplicate removal only: adjacent enclosure endpoints remain distinct.
Integrate every positive-width piece, including adjacent-float pieces, with
scalar float64 SciPy quad at epsabs=epsrel=1e-13 and repeat at 1e-11, limit=1000;
combine pieces with math.fsum. No positive-width piece is skipped. All numerical
warnings block. Strict/repeat agreement is at most 1e-10. The independent
onset-split but switch-unsplit adaptive comparator must agree within 1e-10;
direct 65,536-interval Simpson within 1e-9. For the four historical failures,
switch-split Simpson at nominal 32,768 and 65,536 uses at least two even intervals
per positive-width piece and must agree within 1e-10. Preserve existing Brent,
owner, residual and switch tolerances. Do not widen thresholds or tune grids.

The 65,536-grid switch detector remains fixture-scoped, not a guarantee of every
possible between-node crossing. Unknown topology or a new detector defect is
outside this repair and blocks acceptance. Do not repair the historical detector.

## Frozen engineering fixtures and acceptance

Reuse all 36 Session 14 fixtures times three candidates (108), all 366 component
references and 399 complete fixture permutations. Reuse analytical continuity
and independent scalar field oracles, tiny-sign magnitudes 1e-8, 1e-12, 1e-15,
1e-200, exact/structural zero and same-sign controls. Preserve the discontinuous
0.2-to-0.8 negative control.

Freeze additional engineering functions on t in [0,1]: constant .6; t versus
1-t; three identical positive constant fields; and three identical plateau
branches against a constant .6 field. The plateau branch is
`.6 - .1*max(.251-t, t-.749, 0)`. Also retain the historical exact-predicate
plateau `.6 inside [.251,.749], nextafter(.6,0) outside`. Distinguish the
mathematical boundary from float64 equality extended by rounding; do not replace
one oracle with the other. Check full mapped owner, pair and enclosure records
under all permutations. The known within-grid-cell double crossing remains a
limitation, not an acceptance oracle for global completeness.

New unittest-discoverable tests must exercise actual paths and explicit collection.
Use dependency injection/mocking only in synthetic tests and audit injections;
normal production calls receive no failure-selection argument. Inject failed
continuity/oracles, roots, malformed certification, missing references,
nonconvergence, warnings, permutation mismatch, nondeterminism and integrity.
Require each failure to prevent accepted results and ready=True. A valid control
must pass. Test atomic exclusive creation, marker collision before calculation,
persistent failure evidence and refusal of automatic reruns. No hardcoded true
warning/integrity gates in the new route.

## Execution and evidence

Commit this protocol, then implement and test, then commit the tested tooling.
The one governed audit creates an exclusive persistent marker before calculation.
Run engineering obligations first; a discovered out-of-scope defect stops further
governed computation. Preserve completed evidence, mark unexecuted cases and
references unavailable, and close a failure package. No post-exposure repair or
rerun. Tests after closure must not rerun the governed audit.

Public outputs in `outputs/continuous_occlusion_production_verification/` are
`contract.json`, `wiring_checks.json`, `reference_comparison.csv`,
`failure_injection.json`, `readiness.json`, `qc.json`, `manifest.json`.
Success and failure schemas reject unexpected fields, nonfinite values and
inconsistent counts. Dense arrays and execution/access records stay in ignored
`local/`. Atomic writes and hashes precede interpretation. The manifest binds
starting, protocol and implementation commits, environment, historical source
and evidence hashes and all six preceding outputs; its hash goes in the report.
Publication validates existing hashes without rebinding changed output bytes.

PASS requires every obligation and comparison, all 108 cases, 366 references and
399 fixture permutations. BLOCKED denotes an established remaining defect;
UNRESOLVED denotes insufficient evidence; INVALID denotes execution/integrity
failure. No unavailable obligation counts as satisfied.

Run focused and relevant historical tests, the full active synthetic suite,
compilation, links, hashes/schemas, privacy, append-only and historical
preservation, staged inspection, full-range diff checks and CI. Report run,
passed and skipped counts separately. Add the report and bounded research-log
entry; preserve protocol, implementation and closure as three separate commits.
Push reviewed commits and verify clean synchronized heads and unchanged tag.
On PASS recommend only resuming Session 14R protocol planning under the verified
production contract. Otherwise recommend one bounded action addressing the
remaining defect. Execute neither recommendation.
