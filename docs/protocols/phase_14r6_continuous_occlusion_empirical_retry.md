# Phase 14R6 — Continuous occlusion empirical representation retry

Prospective protocol, 2026-09-12. Starting authority is clean local, tracking,
and live `main` `23cea4de5a218b87fdf614ad3bc31a9cae465fd2`. Annotated
`v0.1.0` remains at peeled target
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. This protocol is committed before
numerical execution or development-geometry access.

## Question and evidence boundary

Session 14R6 asks whether the frozen carrier-origin directional fields provide
useful structural description of Session 13's multi-edge defender geometry
beyond isotropic defender proximity, and whether one directional field is
coherently preferable enough to freeze for later behavioral validation. The
study is label-free and development-only. Carrier position is a proxy for ball
origin; measured ball position and colocation are unavailable.

Historical Sessions 14R, 14R2, 14R3 and 14R5 remain blocked attempts. Session
14R5 stopped before development access after a synthetic interruption exposed
an edge-accounting defect. Session 14ag repaired the prospective boundary:
receiver ordinals are discovered before coordinate projection, a projection
attempt precedes materialization, and the complete materialized edge set is
durably recorded before registration or evaluation continues. Session 14R6
adopts that contract without changing historical modules.

The authority chain includes Session 13's multi-edge result; the Session 14i and
14w verification and routing work; Session 14aa/14ab canonical structure and raw
solver-provenance distinction; Session 14ac's final-float contract; Session
14ad's governed launcher; Session 14af's empirical lifecycle and publication
contexts; and Session 14ag's projection-exposure accounting. Session 14aa's
historical failed execution remains qualified, not converted into a success.

No target, outcome, fitted model, utility, option share, provider acquisition,
protected or withheld detail, pose, xT, progression, sister-project data,
empirical image, behavioral validation, or claim-ledger promotion is permitted.
The public repository, released API, dependencies and historical evidence remain
unchanged.

## Fields and numerical contract

The three candidates are unchanged: isotropic Gaussian defender proximity,
smooth expanding directional field, and smooth constant-width directional
field. Retain `sigma=2 m`, onset length `a=1 m`, expansion half-angle `10°`,
float64 arithmetic and the `1e-9 m` origin-defender exclusion. Directional
persistence behind defenders remains a design assumption. Stable bounded union
is primary and maximum is the strongest-single-field comparator. Neither is a
probability, accessibility, suppression, attribution or value.

For each edge, evaluate every individual, union and maximum component on the
joint Simpson ladder `256, 512, 1024, 2048, 4096, 8192, 16384`. Accept the first
finer vector for which every component changes by at most `1e-7`; fail at cap
exhaustion. Use canonical onsets and switches, certified tie endpoints and
structural partitions. For `N` positive-width pieces, widths at most `1e-12/N`
use rigorous residual intervals; all other pieces retain the audited adaptive
verification. Preserve strict/repeat, onset-adaptive, direct-Simpson and
joint-maximum interval-aware gates. Historical split-Simpson checks apply only
to the four frozen synthetic failures. Degenerate edges retain endpoint behavior.

Synthetic acceptance replays 108 cases, 366 references and 399 mapped
permutations through the assembled R6 path. Canonical structure, component order
and accepted resolution must agree exactly. Corresponding finite final values
use only `abs(a-b) <= 64*epsilon64*max(1,abs(a),abs(b))`; signed-zero bits remain
diagnostic and nonfinite values fail. Continuity, tiny-sign detection,
certification, canonicalization, residual routing, determinism and actual
failure propagation are observed rather than inferred from historical flags.
Empirical edges have no factorial permutation or historical-reference demand.

## Launch, projection, lifecycle and failure authority

The isolated runner exposes `preflight`, `verify-production`,
`render-synthetic`, `run` and `publication-check`; only `run` may reach
development geometry. The governed launch form is `uv run --locked python`.
One top-level path verifies the project environment and lock, produces and
validates the fixed discoverable prerequisite test record with the same
interpreter, verifies committed synthetic and visual authority and green
Python 3.11, Python 3.13 and distribution CI for the exact tested checkpoint,
then enters the guarded empirical callback.

Before projection, lexical inspection may obtain only membership keys and the
candidate sequence length while skipping target values. It must not materialize
coordinates. The wrapper registers the discovered state and receiver ordinals,
persists a projection attempt, calls the unchanged restricted projector, then
synchronously persists one receipt containing the complete receiver-ordinal set
before preparation or evaluation. A callback failure records not-materialized;
an interruption between return and receipt remains an unresolved attempt and
cannot support confirmed-zero exposure.

The hash-chained append-only journal is authoritative for projection and access.
Lifecycle records are authoritative for evaluation and completion. One
carrier-to-receiver connection is one geometric edge; the three candidate calls
are field work, not three edges. Complete an edge only after all candidates and
required verification pass. Complete a state only after all required edges and
state summaries are retained. Derive all counters from persisted records.

An exclusive attempt marker prevents concurrency and automatic rerun but never
authorizes access. Persist synchronized stage records before risky operations,
including state, edge, candidate, numerical stage, partition and routing counts,
warnings and completion counts. Startup, callbacks and publication are enclosed
by failure handling. A failure preserves the pre-failure and terminal snapshots,
active context, access history, completed work, exception class and private
traceback under `empirical_failure`. Publication validates actual stored QC,
manifest, lifecycle, exposure and evidence files; it never reconstructs or
rebinds them.

## Population and frozen summaries

After every pre-access gate passes, verify canonical population SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`, exactly
7,227 states, and historical alias-order counts
`885,801,952,877,861,629,764,734,724`. Project target fields out before
analytical records exist. Identity fields serve membership and duplicate checks
only; discard event references afterwards. Preserve canonical geometry and
anonymous state-local ordinals. No clipping, exclusion, repair or refitting.

For each candidate and combination, retain receiver endpoint field and the
length-normalized segment-average field over `t in [0,1]`. Retain
union-minus-maximum distributions and absolute `1e-12` numerical-equality
frequency; Session 13 nearest distances, `d2-d1`/`d3-d1`, fractional top-1/2/3
membership, nearest/support overlap and multi-edge correspondence; all three
candidate-pair signed differences, within-state Spearman and strict agreement,
strict reversal, tie creation, tie removal and both-tied counts; endpoint versus
segment ordering; receiver/corridor role divergence and ordering on opposing
raw-distance pairs; and inherited across-state overlap/redundancy associations.

Use Spearman only, exact-tie average ranks and absolute `1e-12` block-anchor
ordering. All-zero support is ownerless; zero-strength fractions and constant or
insufficient correlations are unavailable. Weight equally match to state to
edge, averaging eligible pairs within state first. Report every match, means,
extrema, inverse-ECDF 5/25/50/75/95 percentiles, explicit denominators,
represented matches and unavailable reasons. Session 13 gap percentiles remain
distributions of state-average gaps. No bins, matching, regressions, significance
tests, practical thresholds, composite score or post-exposure metric.

## Visual, outputs and stop rules

Before data access, render the inherited synthetic geometry twice as a
byte-identical 1800x800 SVG: three equal-aspect union panels, shared `[0,1]`
scale and anonymous labels. Show `SYNTHETIC`, `CARRIER-ORIGIN GEOMETRIC OCCLUSION
HYPOTHESIS`, `NOT VALIDATED COVER SHADOW` and `NOT SUPPRESSION`. Full-frame QA
must confirm unclipped, nonoverlapping text and readable consistent panels. No
empirical rendering or animation.

Successful closure under `outputs/continuous_occlusion_retry_14r6/` contains
exactly thirteen public artifacts: `candidate_summary.json`,
`structural_correspondence.csv`, `receiver_segment_divergence.csv`,
`candidate_pair_comparison.csv`, `union_vs_max.csv`, `multi_edge_summary.csv`,
`match_summary.csv`, `synthetic_stress_summary.json`, `lifecycle_summary.json`,
`exposure_summary.json`, `qc.json`, `manifest.json` and
`synthetic_field_comparison.svg`. Pre-access authority is separate; geometry,
journals, traces and state-level records stay ignored. Frozen success/failure
schemas reject unexpected fields, nonfinite values, count disagreement and hash
mismatch. Failure closure marks incomplete scientific artifacts unavailable.

Preserve three commits: protocol; tested implementation plus observed synthetic
acceptance and approved visual; closed results/report and append-only research
log. Green three-job CI is mandatory on the tested checkpoint before the single
empirical launch and again after closure. Any post-access defect stops the phase
without repair, omission or restart.

Scientific A requires complementary structure and a coherent advantage for one
directional candidate; B means both remain plausible; C means little additional
description; D means blocked or invalid. Readiness 1 permits recommending only a
separately governed behavioral test of one frozen field; 2 retains descriptive
use with more discrimination; 3 requires revision or blocker diagnosis; 4 does
not justify continuation. Information differences, correlation or visual appeal
alone cannot select a candidate. Accessibility remains **PROXY ONLY** and
suppression **NOT SUPPORTABLE**.

The report maps all 43 requested handoff items and gives exactly one next action:
behavioral validation beyond M1 for A/readiness 1, bounded candidate
discrimination for B, retention of simpler geometry for C, or diagnosis of the
observed blocker for D. It executes none.
