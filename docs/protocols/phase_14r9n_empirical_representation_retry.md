# Phase 14R9N — Fresh empirical continuous-occlusion representation retry

**Frozen 2026-09-19 before implementation or development access.** Starting
authority is clean synchronized commit
`f308aa6072486a56738378722d59fa96d0de47a7`; annotated `v0.1.0` remains
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

## Authority and question

R9N asks whether either frozen directional carrier-origin field adds
complementary structural description beyond isotropic proximity and warrants a
later behavioral receiver-choice study. This is one label-free development
representation run. It is not behavioral validation.

R9J supplies the linear publication validator (PA), R9K supplies the repaired
structural comparator (A/readiness 1), and R9M supplies offline checkpoint-CI
authority plus durable traceback binding (A/readiness 1). Their protocol,
report, and manifest SHA-256 values are frozen as follows:

- R9J: protocol `8c205e768840260dc81ea7bb000d8edaa143de75eb0158d3a80bb7f3e43de9dd`,
  report `7b1f4a2ddd0c4b8dc3030aa156c7a4fe058298b0132a3934bf0a95c767af2465`,
  manifest `cbe421fe0fec781e2456e811806e437ad0dc711f958f05a0d9f1ed7d13602a96`.
- R9K: protocol `a065af8026f1e4e0da8387b0a3d354d8c24adc6447df83167b6922e1aab56f36`,
  report `3796f1e1403e7954e06a2a20213ee8cdf76c4b023dbc7f0e11d5e3f55e62519f`,
  manifest `bb50db6e665a13089d76079216bdd2a28596e98c7dbb97281807be39d81e6df5`.
- R9M: protocol `4c17117ae483ffddcaa4859de292cba795a8682754f15474df811b007c7b1e28`,
  report `995378793b2bcd20a553190c7227fed42de8655273de956c5739c303d331baf2`,
  manifest `222364223e3c6a9b4b9a91605794593d4ddacf2a42ba3cf509c2bd4ad0f41e26`.

R9L remains D/readiness 4 with zero empirical exposure. All historical files
and classifications remain immutable. No target, chosen receiver, outcome,
model, utility, share, protected detail, withheld match, pose, threat,
progression, sister-project data, API, dependency, certificate, tolerance,
field, or scientific redesign is authorized.

## Isolated orchestration and offline CI authority

The R9N runner exposes only `preflight`, `run`, and `publication-check`; only
`run` may authorize geometry. It uses the fresh ignored namespace beneath
`outputs/continuous_occlusion_empirical_retry_r9n/local/` and separate
pre-access, empirical-attempt, preparation, execution, and publication records.
No R9L marker or private evidence may be reused.

Before access, commit and push the tested R9N runner. Require terminal success
for `distribution`, `test (3.11)`, and `test (3.13)` on that exact commit. Then
capture exactly one authenticated, canonical R9M checkpoint-CI receipt. Its
bytes bind repository, commit, workflow identity and hash, run and job IDs,
terminal results, timestamps, protocol hash, runner hash, lockfile hash, and
provenance. Governed validation requires the create-once receipt and sidecar
hash to match exact local authority. It makes no network request. A missing,
altered, noncanonical, pending, failed, cancelled, wrong-commit, wrong-workflow,
or wrong-implementation receipt blocks before access. Optional live comparison
is diagnostic only and cannot change access authority.

Startup and dependency loading occur inside R9M's standard-library failure
boundary. The original traceback is synchronized before journal handling and
its SHA-256 is bound into immutable original and emergency records. When a
journal exists, the same traceback path is passed to R9J linear validation.
The outer record retains the precise stage while the compatibility journal uses
R9J's required `preparation` stage. Publication failure is recorded separately
without altering the original failure.

## Population and one execution

Unlock requires exact inherited and implementation hashes, committed sources,
the locked project environment, passing prerequisite tests, the frozen local CI
receipt, fresh markers, and canonical population SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`.
The population must contain 7,227 states with match counts 885, 801, 952, 877,
861, 629, 764, 734, and 724.

Preparation uses the unchanged lexical target-free projector. It records every
projection attempt and complete materialization receipt durably, then freezes
private prepared bytes and hashes before evaluation. Opened-state and
opened-edge counts derive from successful receipts. Unconfirmed receipts remain
uncertain. Geometric edges and candidate calls remain separate accounting
units.

Reserve exactly one R9N empirical attempt before possible geometry exposure.
Execute canonical match → state → candidate → receiver order for `isotropic`,
`expanding`, and `constant_width`. A complete run requires 7,227 completed
states, 72,270 completed geometric edges, 216,810 completed candidate calls,
and no uncertain exposure.

Preserve the field formulas and parameters, carrier-origin proxy, stable union,
maximum comparator, endpoint and length-normalized segment fields, joint
Simpson estimator, strict/repeat piecewise verifier, onset-aware certification,
switch and tie authority, degenerate-edge behavior, micro-residual routing,
direct and joint agreement gates, certificate matching and warning retention,
and R9K structural comparator. A certified contribution cannot waive a
whole-integral disagreement. No tuning is permitted.

The first numerical or execution failure stops the study. Preserve exact stage,
exception, traceback and hash, active context, warnings and certificates,
lifecycle and exposure, and completed work. Do not skip, repair, retry,
continue, reinterpret, or regenerate missing evidence. Partial scientific
evidence remains private and unavailable.

## Summaries, publication, and decision

Only complete execution permits the frozen candidate, receiver/segment,
union-minus-maximum, Session 13 correspondence, overlap/redundancy, multi-edge,
receiver/corridor, candidate-pair, ordering, and nine-match summaries. Use
Spearman only with inherited tie behavior. Weight equally by match → state →
edge. Reduce pair-derived continuous values to state means and ordering
indicators to state proportions before distribution summaries. Publish raw and
reduced counts, denominators, assessability, and unavailable reasons.

Use the reconciled nineteen filenames. Lifecycle, exposure, marker,
candidate-work, warning, certificate, and publication status belong in
`qc.json.progress_authority`; no twentieth lifecycle artifact is allowed. R9J's
single-pass immutable authority is the sole journal authority. Historical
prefix replay is prohibited. Publication validates persisted private and public
records without regenerating evidence. Failure packages mark all scientific
artifacts unavailable and publish no partial rows.

Choose exactly A for one complementary, coherent, match-stable directional
candidate warranting later behavioral validation; B for multiple plausible
directional candidates; C for little additional description beyond simpler
geometry; or D for blocked/invalid. Assign readiness independently: 1 for
behavioral validation of one frozen field, 2 for descriptor/visualization only,
3 for representation revision, or 4 for not justified. Do not force A or use
correlation, synthetic discrimination, or visual appeal alone. Only complete
A/readiness 1 evidence freezes the exact field specification.

Accessibility remains **PROXY ONLY** and suppression **NOT SUPPORTABLE**. Do not
claim causality, interception probability, defender value, player ranking, best
pass, tactical intent, or validated cover shadow.

## Tests, delivery, and stop rules

Before access, test actual runner entry, offline CI receipt validation,
prerequisites, imports, inherited hashes, direct-entry rejection, marker
collisions, interrupted receipts, uncertain exposure, candidate-versus-edge
accounting, deterministic order, target exclusion, R9K routing, all historical
gates, R9J success/failure publication, traceback binding, summary definitions,
ties, state-first reduction, unavailable groups, complete synthetic success,
and first-failure closure. Run focused and relevant suites, the full active
suite, tracked-only validation, compilation, schemas/hashes, privacy, links,
history, staged inspection, and `git diff --check` before the checkpoint commit.

Preserve three commits: `research: freeze R9N empirical retry`; tested R9N
orchestration and authority bindings; and `research: close R9N empirical retry`.
Push the tested checkpoint, require its CI, capture the receipt, and execute one
governed run. Afterward validate persisted evidence, close the report and
append-only log, push, require final CI, and verify clean synchronized
local/tracking/live heads and the unchanged tag.

If a pre-access gate fails, close D without geometry. If a post-access failure
occurs, preserve it and close D without repair or repetition. On complete
evidence, publish and classify once. Recommend exactly one next governed action:
behavioral validation beyond M1 for A/readiness 1, bounded candidate
discrimination for B, simpler geometry for C, or one bounded blocker diagnosis
for D. Execute no recommendation automatically.
