# Phase 14R9Z — Pair-authority and failure-evidence diagnosis

## Authority and purpose

This protocol authorizes one metadata-only diagnosis from synchronized commit
`f1ffefdf89dfd08da2fadfe5bc67fd80d08b4e80`. Session 14R9Y remains **D /
readiness 4**. It reopened one previously exposed state and edge, then stopped
at `ValueError: pair_authority` without creating a `TerminalCellAuthority`.
R9Z diagnoses the pair-schema contradiction and the missing traceback capture.
It implements neither repair and performs no empirical or numerical work.

The annotated `v0.1.0` tag must remain at
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. R9V–R9Y protocols, reports,
manifests and implementations are bound by the hashes recorded in the private
R9Z authority record. Historical files remain unchanged.

## Access boundary

The reviewer may read tracked source, tracked public artifacts, and only these
non-geometric R9Y private record classes: original failure, lineage receipt,
materialization receipt, checkpoint-CI receipts, marker records and the private
index metadata. It may validate hashes named by those records without opening
the indexed empirical payloads.

The selected-edge contents, boundary capture, reference-cell payloads, prepared
population, provider data, raw geometry, coefficient values, field evaluators,
coefficient bounds, refinement, terminal classification and scientific
summaries are forbidden. R9Z records zero reopened states and zero reopened
edges. Missing authority blocks rather than expanding the allowlist.

## Pair-authority diagnosis

The diagnosis traces R9X's frozen minimum schema through version-1
`create_authority`, `load_authority`, `classify_terminal`, R9V's
`PairAuthority.equal_on`, and the R9Y acquisition call. It keeps distinct:

- two pair coefficient references required by the frozen schema;
- exact symbolic coefficient identity;
- branch-local mathematical equality;
- tolerance-only production co-ownership.

The retained exception proves only that the two coefficient-authority hashes
differed. It cannot establish their values, why they differed, tolerance-only
equality or terminal-cell topology. Synthetic exact-rational fixtures must show
that duplicate coefficients satisfy identity, distinct functions can be equal
on a common inactive branch, ordered distinct references serialize
deterministically, tolerance proximity is not mathematical equality, and
collapsing distinct references loses authority.

The prospective repair specification is frozen as diagnosis output only. It
must preserve two ordered coefficient references, add explicit independently
validated cell-local pair-equality authority, distinguish symbolic identity,
common-branch equality and unresolved/tolerance-only relations, and retain both
functions in later analysis unless identity is proven.

## Failure-evidence diagnosis

The review traces the R9Y authority-construction exception through its `except`
block and terminal exit. It tests whether blocked QC existed only in memory,
whether the exception was re-raised before synchronized traceback persistence,
and whether the hardened failure controller was absent. The historical exact
traceback remains unavailable and must not be recreated.

Synthetic failures at lineage review, materialization, coefficient derivation,
authority construction, validation and publication validate a prospective
contract requiring capture before closure, one immutable traceback hash,
independent publication-failure evidence and a valid blocked package. The
contract is evidence only; R9Z does not change R9Y or the failure controller.

## Evidence, decisions and delivery

The isolated standard-library runner exposes only `preflight`, `review` and
`publication-check`. It uses create-once private review and closure records.
Public output is exactly eight files under
`outputs/continuous_occlusion_pair_authority_diagnosis/`:

- `diagnosis_contract.json`
- `pair_semantics.json`
- `schema_implementation_trace.json`
- `synthetic_pair_controls.csv`
- `failure_capture_trace.json`
- `negative_controls.csv`
- `qc.json`
- `manifest.json`

Public evidence contains only schemas, categorical findings, flags, counts and
hashes. Synthetic coefficients remain test-only. Publication checking validates
persisted bytes and cannot rerun the review.

Classification is **A/readiness 1** only when both defects are established, all
synthetic and access controls pass, and one bounded repair contract is complete.
**B/readiness 2** means pair semantics are established but failure evidence is
incomplete. **C/readiness 2** means failure evidence is established but pair
semantics remain unresolved. **D/readiness 4** means execution, integrity or
evidence is insufficient.

The planned commits are protocol, tested diagnosis tooling, and closed evidence.
Commit 2 must pass distribution, Python 3.11 and Python 3.13 CI before the one
governed metadata review. Closure must pass final CI, privacy, schema/hash,
tracked-only, compilation, historical-preservation and diff checks.

On A/readiness 1, recommend exactly: **separately govern one bounded repair of
the TerminalCellAuthority pair-equality schema and failure-traceback capture,
with zero empirical reopening, then stop before another acquisition.** Do not
implement that repair or repeat R9Y.
