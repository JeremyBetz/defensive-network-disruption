# Session 14am — Fresh Single-Edge Constant-Width Comparator Diagnosis

Planning: ON
Turbo: OFF
Model: Astra Medium

## 1. Authority and prospective freeze

Start from clean, synchronized `94092788770b32638ea4dc748c70e2a593436ed1`; reverify local, tracking and live GitHub heads. Preserve `v0.1.0` at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

Commit `docs/protocols/phase_14am_constant_width_comparator_diagnosis.md` before numerical probes or renewed geometry access. Bind R8, 14ak and 14al protocols, implementations, manifests, retained failures, input hashes and environment.

Preserve these qualifications:

- R8 remains D/readiness 3; 14ak remains invalid without usable numerical interpretation.
- Missing historical estimates, adaptive subdivisions and component timings remain unavailable. New measurements are diagnostic reproductions.
- The approximately 55-minute delay is not established as quadrature time.
- Retain 14ak’s corrections to R8’s protocol identifier and prepared-file hash.
- R8 uses onset-aware certification; 14ak used an older entrypoint. Reproduce R8’s actual path.
- The historical “unsplit” comparator already splits at canonical onsets; it omits additional switch/tie boundaries.

Authorize only zero-based state **4**, receiver **7**, candidate **constant_width**. Preserve historical code, artifacts, numerical contracts, claims and released software. No other geometry, completed scientific summaries, targets, models, shares or provider acquisition.

## 2. Isolated implementation and pre-access acceptance

Add an internal diagnostic adapter and runner exposing `preflight`, `diagnose`, and `publication-check`, using `uv run --locked python`.

Before access:

- Construct the complete actual diagnostic payload, including envelope, certified structures, owner witnesses, integral intervals, convergence, runtime and journal records.
- Use the unchanged 14al serializer. Project unsupported owner-witness objects through explicit primitive fields without arbitrary object serialization.
- Require deterministic bytes, exact float preservation, durable create-once writing and validated manifest/hash closure.
- Test persisted success/failure packages against missing evidence, changed hashes, false acceptance, serialization failure and cross-file disagreement.
- Test selective single-edge access with forbidden-value sentinels.
- Test timeout handling, stage persistence, marker collisions and emergency preservation independent of journal replay.

Commit tested tooling before execution. A pre-access defect blocks geometry access.

The new execution uses an exclusive attempt marker, synchronized access journal, stage-by-stage private evidence and independently writable immutable emergency records. A marker prevents repetition but grants no access authority.

## 3. One bounded numerical diagnosis

### Exact reproduction requirement

Hash complete retained input files without decoding them. Selectively reconstruct only the authorized edge and verify its canonical/prepared geometry and serialization. Record attempted and successful access separately.

Reproduction requires:

- R8’s actual production/verifier call path;
- the same structural authority and comparator roles;
- the actual gate inputs;
- the same `GateFailure: piecewise_unsplit` failure.

Similar disagreement obtained through another path is diagnostic evidence only and does not constitute reproduction. If exact reproduction fails, close unresolved without another edge or retry.

Use observation hooks without replacing historical functions. Capture the unchanged production estimator once, including its visited vectors and stopping evidence. Preserve the original gate failure before supplementary diagnostics.

### Frozen numerical paths

- **Production:** joint individual/union/maximum Simpson vector at `256,512,1024,2048,4096,8192,16384`; accept the first finer vector when every component changes by at most `1e-7`. Its estimate remains provisional if full verification fails.
- **Piecewise verifier:** bounded-residual intervals and adaptive quadrature over accepted canonical structural partitions; strict `1e-13`, repeat `1e-11`, `limit=1000`.
- **Onset-only comparator:** adaptive maximum over endpoints and canonical onsets, using `1e-13`, `limit=1000` and `math.fsum`.

Adaptive stopping uses subdivisions, not the Simpson ladder. Record stopping and agreement timing exactly.

Preserve all interval-aware gates: strict/repeat and onset-only `1e-10`, direct Simpson `1e-9`, and joint maximum `1e-6`. Use the original positive structural-piece count for residual allocation; never recompute eligibility separately for isolated pieces.

### Supplementary convergence and reference authority

Calculate uniform-grid and structural-piece Simpson curves only at the frozen `256–16384` ladder. Retain estimates, successive differences, actual evaluations, per-piece contributions and timing privately.

The **16,384 structural-piece Simpson result** earns the name **highest-resolution internal diagnostic reference** only when:

1. Its structural partition passes independent validation.
2. The absolute change from 8,192 to 16,384 is at most **`1e-10`**, frozen prospectively as a numerical convergence criterion.

Other-path agreement does not determine reference eligibility. Compare adaptive and production evidence against this reference afterwards. If either prerequisite fails, report the estimate but mark reference status unavailable. It is never mathematical ground truth.

The **65,536 direct-Simpson result** serves only the inherited independent-verification gate. It cannot replace the 16,384 reference, alter adaptive stopping or become a production estimator.

Replay the actual gate from captured adaptive inputs. Keep supplementary Simpson counterfactual comparisons distinct from the historical decision; do not invent a historical gate resolution.

### Partition, onset and runtime diagnostics

Independently verify domain coverage, ordered structural boundaries, preserved tie endpoints, positive-width pieces, total width, routing and certified ownership. An invalid partition stops numerical interpretation.

At every relevant onset inspect only its immediate predecessor, onset and successor floats. Compare individual/maximum values with the independent scalar oracle. Analytical continuity, finite-step slopes and tolerance-based owner blocks remain distinct.

Record:

- Exclusive compute time.
- Nested/inclusive time.
- Waiting and I/O time.
- Unattributed overhead.

Reconcile exclusive stage durations, waiting/I/O and overhead approximately to governed wall time. Never add overlapping inclusive timers or force unexplained time into a numerical stage. Historical unmeasured durations remain unavailable.

Retain **ten minutes per adaptive/replay operation and sixty minutes total**. A timeout preserves completed evidence and stops execution. It establishes a feasibility problem within this budget, not numerical inaccuracy or a proved runtime for the full population.

Run frozen synthetic controls before access. Document any newly proposed topology-derived fixture without executing or tuning it after diagnosis.

## 4. Journal-publication diagnosis

Verify the preserved R8 journal’s complete hash chain and inspect only metadata and lifecycle information needed for failure mechanics. Do not open scientific products.

Use frozen valid synthetic journals with `16,32,64,128,256,512,1024` states and ten edges per state, plus one provider-free fixture matching the retained historical event-count shape. Preserve any rejection of the historical shape rather than silently repairing it.

Instrument unchanged replay behavior for record/byte counts, replay calls, cumulative prefix processing, reads, parsing, hashes, validation passes, elapsed time and available memory.

Base complexity conclusions primarily on **operation counts**. Explicitly test whether repeated prefix processing produces triangular work. Timing scaling is supporting evidence only; extrapolation beyond tested sizes cannot prove runtime or complexity.

Distinguish repeated reconstruction, repeated parsing/hashing and duplicate failure handling. Assess streaming validation, authenticated incremental summaries, checkpoints plus suffix replay and indexed validation conceptually. Implement no scalability repair.

Verify original/emergency failure references and traceback integrity. Emergency evidence remains distinct from normal publication acceptance.

## 5. Evidence, decisions and delivery

Produce the **18 specified artifacts** under `outputs/session14am_constant_width_comparator_diagnosis/`, with strict success, partial-failure and invalid schemas frozen before execution.

Public files contain sanitized findings, counts, flags, timings and hashes. Exact geometry, estimates, contributions, roots, owners, neighborhoods and traces remain private and hash-bound. Explicitly mark private or unavailable quantities.

Validate actual persisted files, schemas, cross-file relationships and hashes before interpretation. Publication checks never regenerate missing evidence.

Classify numerical **A–I**, readiness **1–4**, publication **P1–P5**, and execution validity independently. Multiple independently demonstrated issues outrank a single-cause classification. Unavailable evidence cannot establish a healthy path.

Preserve three commits:

1. Frozen protocol.
2. Tested diagnostics.
3. Closed evidence, report mapping all **42 handoff items**, and one append-only research-log entry.

Run focused, relevant and full tests, compilation, privacy/access guards, links, historical-byte checks, staged inspection and `git diff --check`. Report run/pass/skip counts separately. Push, require green Python 3.11, Python 3.13 and distribution CI, and verify clean synchronized heads and the unchanged release tag.

Recommend exactly one next action:

- Numerical A/B/C with readiness 1/2 and publication P1/P2: a separately governed bounded repair addressing the demonstrated numerical and publication defects.
- D/E/F/G: numerical design review.
- H: one narrowly specified evidence action.
- I: repair the execution defect that blocked diagnosis.

Execute no recommendation and do not resume the empirical representation study.
