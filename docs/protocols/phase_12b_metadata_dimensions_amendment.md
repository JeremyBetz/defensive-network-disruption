# Phase 12b — Minimal development dimension authority

This prospective access amendment authorizes only root pitch_length and
pitch_width from the nine already-spent development match metadata objects at
SkillCorner commit `02a396ffd09b283c9f092fdedeff11da6d535b66`.
Use the exact development membership in the historical Session 8 source; no
reserved or withheld identity may enter the allowlist. Public outputs contain no
provider IDs or dimension mappings. Detailed provenance stays ignored.

Resolve commit/tree/path/blob metadata with bounded GitHub API requests, checking
exact identities and object type/size. Prefer intact existing Session 2 metadata
bytes; otherwise fetch only the verified Git metadata blob. No twenty-match mixed
manifest; no tracking, events, targets, rosters or other product parsing.
Opaque source bytes may exist in ignored storage. A selective JSON scanner may
traverse syntax but must materialize only the two permitted numeric values;
other values must never enter records/logs/exceptions. Reject duplicate keys,
invalid JSON, missing/boolean/string/nonfinite/nonpositive dimensions.

Reject path escapes, symlinks, unauthorized products, redirects/substitutions,
oversized envelopes and integrity/schema failures. API envelope maximum 4 MB;
metadata maximum 1 MB. Allow initial request and two retries only for timeout,
reset/dropped connection, interrupted stream, HTTP 500/502/503/504, waiting one
then two seconds. Each attempt is separately logged; bytes are discarded on
transport failure. No integrity, clean truncation, TLS or schema retries.

Keep mapping and receipts in ignored local storage and append access entries
immediately, including stage, alias, protocol/implementation and status. Verify
all nine before population preparation. Publish only counts, hashes and states.
No new scientific rule or repair may follow exposure. A blocker preserves the
partial record and closes Session 12b without computing horizons.
