# Phase 12b — Normalized progression redistribution

Prospective authority, 2026-09-10. Start: `9c48bf5b3146222e27e9eeb151af448e6beeb882`.
Preserve release and all historical authorities. This protocol implements the
user-approved Session 12b plan, with the separate metadata amendment below.

## Formula and interpretation

Freeze V(x',y')=1/2+x'/L, with actual verified match length L, centred metric
attack-oriented x, and native y unchanged. Range [0,1]; finite y is required but
ignored by the surface. Any carrier/receiver x outside [-L/2,L/2] stops the entire
comparison, with no tolerance, clipping, exclusion or extrapolation.

Conceptually compare only u and u*(1-|2v-1|) at synthetic u,v in {0,0.5,1}.
Select u: the alternative imposes an unsupported lateral penalty. No empirical
formula selection, centrality, external surface, M2, fitting or new dependency.
External xT remains deferred under Phase 12a; do not contact organizers.

Delta V_j=V(r_j)-V(c); H_m=sum(p_mj*delta V_j); S=H_1-H_0.
This is model-weighted goalward displacement divided by length. Signed changes
are retained. Verify S=sum((p1-p0)*V(receiver)) and
S=sum((p1-p0)*x_receiver)/L and contributions (p1-p0)*delta V.
Use math.fsum in canonical candidate order and identity tolerance
64*float64_epsilon*max(1,sum(abs(summands)),abs(compared totals)).
M0/M1 differ in shared coefficients; use defense-conditioned model comparison.
No calibrated threat, probability, causal effect, attribution, value, intent,
pressing trap, predictive validation or practitioner evidence is established.

## Inputs, preparation and execution

Canonical population SHA-256:
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`.
Final model SHA-256:
`0383561e45bcd9f8d84eccc7e9733b2409536f61c39193fa87d08c36c6f6fd65`.
Use the exact nine development identities/counts in committed Session 8 source;
7,227 states. Targets are discarded before analytical objects. Identity only
supports membership and duplication checks. No population regeneration or raw
tracking/events, reserved/withheld/pose, empirical rendering or temporal recovery.

Commit protocol/amendment, then tested implementation before metadata access.
Verify dimensions; prepare canonical geometry without utilities/shares/horizons.
Commit coordinate authority before analysis. Each command verifies prerequisite
hashes; analyze requires clean committed state and an exclusive persistent marker.
No automatic boundary crossing. On any defect preserve evidence and stop; no
empirical repair/rerun. Failure closure must work without analysis outputs.

## Summaries and tests

D=effective_count_M1-effective_count_M0. Signs for D and S: negative below -1e-12,
positive above 1e-12, otherwise numerical zero (not practical equivalence).
Report all nine cross-tab cells. States equal within match, then nine matches
weighted equally. For H0,H1,S,D report means, inverse-CDF median and 5/25/75/95
percentiles, extrema and signs. Count proportions use raw denominators per match;
macro proportions are equal-match weighted, not pooled. Report per-match Pearson
S versus D (constant means unavailable). Only strata: three D signs, with S
summaries weighted over represented matches; empty strata unavailable.
Compare D concentration summaries to Session 8 using 64-epsilon relative checks.
No significance tests, regressions, extra metrics/bins or outcome joins.

Test metadata projection/integrity/access, geometry bounds, monotonicity/lateral
invariance, unchanged M0/M1, identities and signed decompositions, nine categories,
weighting/empty groups, sentinel targets, exclusive state, strict schemas/hashes,
preparation cannot score and analysis cannot acquire/fit. Full active synthetic
suite and compilation; report passes/skips separately. Validate/hash before
interpretation. Preserve old files; logs/review append-only; claim ledger unchanged.

## Outputs and closure

Runner: preflight, verify-dimensions, prepare, analyze, render-synthetic,
publication-check. Namespace outputs/internal_threat_baseline: authority,
horizon_summary.json, match_summary.csv, concentration_value_typology.csv, qc.json,
manifest.json and synthetic_progression_redistribution.svg. Details/ledger/markers
under ignored local/. Raw metadata under ignored data/session_12b/.

Synthetic SVG: 100x60 pitch, carrier (-20,0), A/B/C (-10,-15),(0,15),(20,0);
shares (.5,.3,.2) and (.2,.3,.5), explicitly illustrative not fitted. Width encodes
share, color destination value, labels signed changes and horizons. Label
SYNTHETIC / SPATIAL VALUE PROXY / NOT CALIBRATED xT. Render twice identically.
On a prerequisite blocker, do not proceed to rendering or empirical commands.

Report all 24 handoff items, unavailable where blocked. Scientific A/B/C/D is
respectively complementary formal description, limited context, little benefit,
or blocked/invalid. Software 1/2/3/4: ready internal interface, needs revision,
keep internal, invalid. No practical threshold forces an interpretation.
Accessibility PROXY ONLY; suppression NOT SUPPORTABLE. One later recommendation:
fixed-baseline sensitivity if useful, formal redundancy if little, bounded
prerequisite/integrity review if blocked. Commit results separately, push/sync,
preserve v0.1.0, stop. No release or execution of the recommendation.
