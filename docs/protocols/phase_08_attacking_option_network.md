# Phase 08 — Local attacking-option networks

Prospectively frozen from clean local/live remote
`54b56a9900b96a1be0e9e51087a978ffeef6d4f1`.

## Question and scope

Do local carrier-centred option distributions provide complementary formal
descriptions beyond the leading option shares? A directed star contains no
information beyond its complete edge vector. This is descriptive compression
in spent development data, not predictive replication or practitioner validation.
M0/M1 only. C09/C10 stay supported within scope, C01/C02 in progress;
accessibility PROXY ONLY, suppression NOT SUPPORTABLE. No claim promotion.

Use the canonical 7,227-observation nine-match development population at
`outputs/receiver_ranking_m0_m1/local/population.jsonl`, SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`.
Use unchanged final models in `outputs/reserved_evaluation/final_development_models.json`,
SHA-256 `0383561e45bcd9f8d84eccc7e9733b2409536f61c39193fa87d08c36c6f6fd65`.
Exact development membership and per-match counts inherit Phase 07's committed
diagnostic authority and protocol. Reject every other identity before computation.
Hash the entire canonical population before parsing. Inspect schema/membership
and geometric fields only; discard target fields from application records.
No raw products, reserved details, withheld/pose, temporal reconstruction,
empirical rendering, fitting, M2 computation or new provider requests.

## Frozen mathematics

Reuse unchanged M0/M1 feature construction and the final standardized-matrix
utility operation. Temperature-one stable softmax defines model-implied
receiver-option shares. No calibration, smoothing, cutoff or total-mass metric.
Rank with the existing absolute 1e-12 block-high utility tie rule, even where
shares underflow. All candidates stay aligned between models. Differences also
contain shared-coefficient changes; they are not causal defender effects.

For each model/state retain top-one share, top-two cumulative share, entropy
H=-sum(p log p), normalized entropy H/log(n), effective count exp(H), and utility
range. Zero terms contribute zero; singleton normalized entropy is zero, top-two
is one. Also summarize M1 minus M0 for these quantities, top-set changes, and
within-choice pair changes split into reversal/tie creation/tie removal.
No HHI, additional distribution distances, outcomes, labels, new bins, tests of
significance, regression, alternative transformation or practical thresholds.

For each match and equal-match macro, provide mean, minimum, maximum and
5/25/50/75/95 weighted inverse-CDF percentiles. States are equally weighted
within matches; match macro assigns equal mass to each of nine matches.
Pair-change fractions are averaged by state then match, not pooled pairs.
Empty pair sets are unassessable. Report per-match Pearson correlations between
effective count and top-one, top-two and utility range for both models; zero
variance means null/unassessable. Correlation is not predictive-value evidence.

Synthetic distributions fixed before empirical work: uniform four options,
singleton, concentrated (0.97,0.01,0.01,0.01), tied (0.4,0.4,0.1,0.1), and
tail contrast (0.4,0.3,0.15,0.1,0.05) versus
(0.4,0.3,0.2,0.075,0.025). The latter share the same strict ordering and leading
shares but different entropy. Synthetic examples establish mathematics only.

## Software, visual and commands

Freeze `outputs/attacking_option_network/software_contract.json` with this
protocol. Experimental immutable OptionState/OptionNetwork, explicit frozen
model argument, neutral names, no automatic loading. Kloppy 3.19.0 adapter takes
explicit player selections and verified metric coordinate context, x'=s*x/y'=y.
Canonical carrier identity remains null. Other identifiers are caller-supplied.
No silent eligibility inference or new shared frame framework.

Use existing NumPy and pure helpers; no dependency/lock changes. Optional
Kloppy import belongs in adapter only. Defer mplsoccer (real pitch dimensions
absent), matplotvideo (no video/temporal data), pandas and graph libraries.
Append version/license/capability decisions to the existing library review.

Runner: preflight, analyze, render-synthetic, publication-check. Protocol and
contract commit first; implementation/tests/environment authority commit next;
analyze once on clean committed implementation with exclusive persistent marker
and ignored append-only access ledger. No automatic crossing of commit gates.
Failure preserves records, logs a sanitized failure, and blocks rerun. No
scientific or implementation correction after empirical exposure in this run.

Outputs: software_contract.json, implementation_authority.json,
network_summary.json, m0_m1_network_comparison.csv, qc.json, manifest.json,
synthetic_options.svg under outputs/attacking_option_network. Detailed outputs
and execution/access records stay in ignored local/. CSV uses LF. Strict schemas
and finite values; hashes validated before aggregate closure/results display.
Manifest binds inputs/protocol/contract/implementation/environment/output bytes;
report records manifest hash without recursive hashing.

One public synthetic side-by-side SVG only: carrier (0,0), candidates
(12,8),(22,0),(10,-12),(30,15), defenders (8,3),(16,-2),(24,10).
Use frozen M0/M1 models, identical positions, equal scale, anonymous labels,
width=0.5+8*share, metre scale/attacking arrow, no pitch or measured ball. Synthetic
geometry is marked explicitly and is never empirical evidence. No animation.

## Decisions and stop

Report network A/B/C/D separately from execution validity: A complementary
formal description beyond leading shares, B mainly communication design with
analyst usefulness untested, C limited/redundant description, D unjustified or
obscuring abstraction. No claim of new information beyond complete edges.
Software 1 ready for provider-independent core, 2 narrower geometry API,
3 one architectural revision needed, 4 not ready. No automatic threshold.

Create docs/session_08_attacking_option_network_report.md mapping the 23
requested handoff items; append research-log and reuse-review entries only.
Verify focused/full synthetic tests, compilation, exact historical preservation,
model/population hashes, adapter semantics, firewalls, schema/hash consistency,
publication safety, staged content and diff checks before result commit/push.
Report passed and skipped counts. Recommend exactly one separately governed
direction and stop; no package hardening, network expansion or new evaluation.
