# Phase 14z — CI artifact transport and reproducibility completion

Status: **FROZEN BEFORE IMPLEMENTATION OR NUMERICAL EXECUTION**  
Date: 2026-09-11

Session 14z begins from synchronized `938683d6f13e5299128ce30aa1d05e6bdddc6c96`; `v0.1.0` remains peeled to `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. Session 14y remains historically **H — UNRESOLVED**, readiness 4. Its repaired switch projection and successful local 108-vector/366-component reproduction remain authoritative.

## Question and boundary

This phase repairs only the transport of the existing Session 14y Python 3.13 diagnostic evidence, then performs one fresh governed cross-platform comparison. The prior workflow emitted a 314,834-byte JSON record as one base64 log line. GitHub Actions completed successfully but omitted that line from its downloadable log, so the CI record could not be authenticated or compared.

No field formula, joint Simpson computation, switching logic, micro-interval routing, residual contract, historical vector, equality assertion, numerical tolerance, dependency, lock, released API, or Session 14R code may change. No empirical state, target, outcome, model, option share, provider product, protected/reserved/withheld data, pose, xT, progression, or Session 14R partial scientific output may be accessed.

## Frozen diagnostic and transport

The computation is the accepted Session 14y diagnostic: 108 frozen fixture/candidate vectors, 366 ordered components, every `256`–`16384` Simpson ladder value, accepted resolutions, convergence deltas, float64 values and IEEE-754 bits, signed zero, switches, partitions, tie enclosures, `owners_before`, `owners_at`, `owners_after`, micro-interval routing, residual bounds, and sanitized environment metadata.

The workflow runs on Python 3.13 using the repository's pinned checkout, Python and uv setup actions and locked all-extras installation. It executes the diagnostic exactly once and writes canonical sorted, compact, finite JSON with one trailing LF to `session14z_ci_diagnostic.json`. It computes SHA-256 over those exact bytes and prints only a concise record containing schema version, byte size, hash, and artifact name. Stdout/base64 is not an authoritative or fallback transport.

The deterministic GitHub Actions artifact name is `session14z-python313-vector-diagnostic`. The supported pinned upload-artifact action uploads only `session14z_ci_diagnostic.json`, after successful creation and hashing, using default retention. The file contains synthetic diagnostic evidence only and no secrets.

After one manual dispatch, retrieve that artifact exactly once. Require the expected artifact and internal filename, a single regular file, and exact equality between the CI-declared SHA-256 and the locally computed hash before parsing or comparison. A missing artifact, malformed archive, duplicate/unexpected file, unsafe path, or hash mismatch stops the phase. The retrieved bytes are never edited.

## Prospective execution

Before dispatch, tests must prove deterministic file serialization and hashing, exact byte roundtrip, supported size, safe artifact path, corrupted-byte rejection, unchanged Session 14y schema/component ordering/routing, signed-zero and ULP handling, and privacy. Workflow inspection must establish Python 3.13 only, pinned setup, locked installation, one diagnostic invocation, upload after creation, and a concise hash declaration. Ordinary CI remains unchanged.

Run one local diagnostic using the unchanged computation. Require its record to reproduce Session 14y's local authority: 108 vectors, 366 bitwise-equal historical components, and accepted-resolution counts `55@512`, `13@1024`, `30@2048`, `8@4096`, `2@8192`. Unexpected difference stops the phase before dispatch.

After committing and pushing tested transport tooling, dispatch the governed workflow once, wait for completion, retrieve its artifact once, verify its declared hash, and compare it with the local record. No second dispatch or transport fallback is authorized.

## Comparison and existing authority

Algorithmic reproducibility requires identical component order, accepted Simpson resolution, partitions, switching structure, mapped ownership records, tie enclosures, micro-interval routing, and residual rules. Any difference fails the algorithmic verdict and cannot be reduced to floating drift.

For all 366 accepted components report historical, local and CI values; exact equality; absolute and defined relative difference; finite-number ULP distance; and signed-zero differences (`+0`/`-0` have zero numerical distance). At each ladder resolution report differing-component count and maximum absolute difference, and identify the first divergence. Judge numerical acceptability only under the existing `1e-7` controlled-Simpson convergence and `1e-6` production/reference authority. No replacement tolerance is selected.

Only when algorithm paths, accepted resolutions, routing and partitions agree and all numerical differences remain within existing authority may the report assess whether Python dictionary equality imposes an unnecessary cross-platform bitwise property. Small differences alone do not establish that conclusion.

## Decisions and stop rules

Choose exactly one classification, retaining Session 14x/14y precedence:

- **A — BITWISE DRIFT ONLY; ALGORITHM AND NUMERICAL RESULT REPRODUCIBLE**.
- **B — CROSS-PLATFORM FLOATING DRIFT WITHIN EXISTING NUMERICAL AUTHORITY**.
- **C — HISTORICAL EXACT-EQUALITY CONTRACT IS OVERSTRICT**.
- **D — DEPENDENCY / ENVIRONMENT DRIFT CHANGES ALGORITHM PATH**.
- **E — ROUTING / PARTITION / STOPPING-RESOLUTION DIFFERENCE**.
- **F — MATERIAL PRODUCTION NUMERICAL DIFFERENCE**.
- **G — MULTIPLE ISSUES**.
- **H — UNRESOLVED**.

Material defects and multiple-material-issue G take precedence. C requires matched algorithmic behavior and numerical results inside existing authority. Readiness is separately 1 (bounded equivalence-contract repair), 2 (environment/dependency pinning repair), 3 (production numerical review), or 4 (more evidence).

Any governed local failure prevents dispatch. Any workflow, retrieval, integrity, or comparison failure is preserved and closes without a second execution. This phase does not repair historical tests, vectors, tolerances, dependencies, production numerics, or Session 14R.

## Outputs and closure

Publish exactly the requested files under `outputs/cross_platform_vector_reproducibility_14z/`: `transport_contract.json`, `local_diagnostic.json`, `ci_diagnostic.json`, `environment_comparison.json`, `vector_comparison.csv`, `resolution_comparison.csv`, `routing_comparison.json`, `equality_contract_review.json`, `qc.json`, and `manifest.json`. The manifest binds the run ID, artifact name, declared and downloaded hashes, protocol, implementation, environment, historical authorities, and output hashes. Public evidence contains only synthetic fixture names and sanitized environment details.

Commit chronology is protocol; tested transport and diagnostic tooling; then closed evidence/report and append-only research-log entry. Run focused, relevant and full local tests, compilation, workflow/schema checks, hashes, privacy/publication checks, links, history preservation, staged inspection and `git diff --check`; report ordinary CI separately from the governed diagnostic workflow.

If A/B/C and readiness 1 are earned, recommend exactly: **separately govern a cross-platform historical-vector equivalence-contract repair, then authorize a fresh Session 14R retry if that repair passes.** Execute no recommendation automatically.
