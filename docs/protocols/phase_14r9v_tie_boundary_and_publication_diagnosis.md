# Phase 14R9V — Tie-boundary maximality and immutable failure-publication diagnosis

**Frozen 2026-09-21 before implementation or retained access.** Starting
authority is clean synchronized commit
`d1fec793d63370f6e9ed687cbcba83519459b798`; annotated `v0.1.0` peels to
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. R9U remains
**D — blocked/invalid, readiness 4**.

## Scope and authority

R9V diagnoses two independent R9U blockers: the retained constant-width
`tie_boundary_not_maximal` failure at zero-based state 10, receiver 2, and the
subsequent `immutable_record_exists` failure after successful R9J linear review.
Only that already exposed edge and candidate are authorized. No population,
other edge or candidate, target, outcome, model, share, pose, protected data,
scientific summary, numerical repair, certificate, tolerance, dependency or
public API is authorized.

R9J through R9T authority is inherited exactly as bound in Phase 14R9U. R9U is
bound by protocol SHA-256
`46542fa8be7e2c8f609f07aa329f3ca1edab113afa200ee98e898fcc52a6a16b`,
report `7adc513a9953441ed1d0b9db29f5a126c7a261d7dd01a0444df0c0cd2dd56ff5`,
implementation `9b7806e5bb90534cbb5923648330d2592059f009aa253031d3483574a9f6cee7`,
prepared population `15a809060f0ba4a8cb3790011dab8008db3dad6e931c287ccb395f61ef1530b0`,
journal `ef3906cc5de9b1c15166720045fe4c5775a271866d0dfc8233786cd91e38ac26`,
linear authority `60f6cb33a6fb92b2c48f849fd933e2e9eed60855bd99c800a4a90dd50cc4ede2`,
original failure `987abb1af84b473e80108ac9699749cac70b16214f542389f5a28eaa24565bdb`,
numerical traceback `421b9e65bd918992f01a442a5818b36d375b3a8d31bb91384eaf2d0f4cf2729e`,
publication failure `006471a1c32c02290bdee6e748d816c38427917049eac5e01b322a3ac5348d93`
and publication traceback
`d59619f1fb3aad1a81deb69d5226fa2e7f7d66502ad93f150193d8c0f2858833`.
The nine committed R9U public artifacts retain their committed hashes. R9U has
no manifest; that absence is historical evidence and no substitute may be made.

## Retained numerical diagnosis

An isolated runner exposes only `preflight`, `diagnose` and
`publication-check`. Separate create-once markers govern retained-journal review,
numerical diagnosis and publication controls. R9J reviews the 33,223-record R9U
journal once and must confirm its chain, terminal context, traceback and the
state-10/receiver-2 materialization receipt. The complete prepared file is
hashed without general decoding; selection is lexical and retains only the
authorized carrier, receiver, ordered defenders and authority identity.

The unchanged R9T route is invoked once and must reproduce
`VerificationError: tie_boundary_not_maximal` at owner certification. A
non-mutating observer captures the failing `_certify_boundary` frame and its
already evaluated rows without adding field calls. A different outcome closes
the numerical branch NF.

The frozen source meaning is that the exactly equal pair must both belong to
the inherited absolute-`1e-12` global maximum-owner block at a positive inside
witness. This check does not itself prove maximal geometric extent. Exact pair
equality, tolerance ownership, global maximality and interval extent remain
separate evidence.

An independent constant-width reference uses exact binary64-to-rational inputs,
the existing fixed `2^-256` outward arithmetic, certified square-root and
exponential bounds, and exact symbolic identity. It compares the pair against
every defender across the proposed interval and neighboring structural regions.
Deterministic bisection stops at depth 80, 65,536 cells, 600 seconds per
operation or 3,600 seconds total. Limits yield unresolved evidence.

Twelve provider-free controls cover maximal and truncated exact intervals,
point ties, switch and onset endpoints, three-way ties, tolerance-only ties,
duplicate functions, third-defender dominance, tiny intervals, malformed
ordering and oscillatory binary64 interaction. Numerical classification is
exactly NA, NB, NC, ND, NE or NF. R9V may document one prospective numerical
repair but may not implement it.

## Publication ownership diagnosis and repair

Planning inspection identifies the route to verify: `TerminalClosure.fail`
creates `local/linear_authority.json`, then R9U's publisher calls
`retain_authority()` and attempts to create that path again. R9V must confirm
this using the retained traceback and synthetic reproduction.

Prospectively, terminal closure is the sole owner of the immutable linear
authority. Publishers receive a content-bound persisted-authority descriptor;
private and public writers own only their package files; validators only read;
emergency and publication-failure records retain separate owners. A new internal
adapter implements this contract without editing historical R9U or R9O files.

Controls cover success, pre-access failure, numerical failure, publication
initialization failure, publisher failure, repeated closure, pre-existing
authority, tampering and private/public collision. They require one R9J review,
no prefix replay, read-only validation and preservation of the original failure.
Only complete equivalence permits a separate publication-fix commit and PA.

## Evidence, validation and stop

Public output is exactly eleven files under
`outputs/continuous_occlusion_tie_boundary_publication_diagnosis/`:
`authority.json`, `numerical_reproduction.json`, `tie_invariant.json`,
`tie_maximality.json`, `independent_reference.json`,
`synthetic_tie_controls.csv`, `publication_collision.json`,
`publication_controls.csv`, `publication_repair.json`, `qc.json` and
`manifest.json`. Exact geometry, identities, values, coefficients, bounds,
owners, receipts and traces remain ignored and hash-bound.

Commit chronology is protocol; tested diagnostic tooling; a publication-only
repair if justified; and closure. All prospective code is committed, pushed and
green on distribution, Python 3.11 and Python 3.13 before retained access, with
an offline R9M receipt for the exact checkpoint. Run focused tie and publication
tests, R9Q/R9R/R9T/R9K/R9J/R9M/R9U regressions, relevant Session 14 tests, full
and tracked-only suites, compilation, schema/hash, privacy, link, historical-byte,
staged and diff checks.

Return numerical NA–NF, publication PA–PD, execution validity and readiness
1–4 independently. A diagnosed but unrepaired numerical defect normally leaves
readiness 3 even with PA. Recommend exactly one bounded next governed action.
Do not execute a numerical repair or another empirical study.
