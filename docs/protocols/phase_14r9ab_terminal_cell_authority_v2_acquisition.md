# Phase 14R9AB — Terminal-cell authority v2 acquisition

## Authority and purpose

This protocol authorizes one bounded acquisition from clean synchronized commit
`2f487e7bfca338bff78deb845c757a3ad4927186`. The annotated `v0.1.0`
tag must remain at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
Session 14R9AA closed **A / readiness 1 / execution valid**, with zero empirical
access, after validating a version-2 `TerminalCellAuthority` for distinct tied
functions and a capture-first failure boundary.

R9AB may reopen only the retained R9V selected-edge copy and may create exactly
one private version-2 authority for its single unresolved constant-width leaf.
It records one previously exposed state and one previously exposed edge reopened,
with zero new population exposure. It may not access the prepared population,
provider source, another state, edge, receiver or candidate, targets, outcomes,
models, shares or scientific summaries. It performs no field evaluation,
terminal bound, refinement, owner certification, localization, maximality
analysis or TA/TB/TC/TD/TE classification.

All historical files and classifications remain unchanged. In particular, the
historical R9Y traceback remains unavailable and version-1 authority bytes and
behavior are preserved.

## Frozen lineage and schema

The retained lineage is rooted at R9V private-index SHA-256
`2abe5f916265275a94c157bd1a93c55f96d352863cbdce618a40418af3a2ebc6`.
Within it, `boundary_capture.json` is
`30a8363749bb07053cdd35f82248535ebc1909441274a9da572ba684de7f597b`
and `selected_edge.json` is
`04bfd87303522ced4968d7953228d2bc8a289a2fab28d823cfd1c7deff8aa51f`.
The ordered 81-cell partition has canonical SHA-256
`91af2d5a17291223c49e2b361c4091b67047424e70d52cc305c64c49f78c2e26`.
Its sole unresolved leaf is ordinal 36, depth 80, with pair status `equal` and
maximum status `unresolved`.

The R9X protocol, retention contract, minimum schema and manifest have SHA-256
values `5cf0461d9ec16d75f3b763a100e21aede23387a0f8242d159b4761d61f1d47c0`,
`775b891e304920522fbc0f7427d41df6a3116c35406f635660f273956217aaaf`,
`c8154e4072298d274e11282b72a913284adcaeda7154fdd9d214c5ba4d28cc91`
and `9c87d0217a0152a535890c573c5aa22498295f20dc922c0044c0d3e570fae567`.
The unchanged R9X authority implementation has SHA-256
`ab272ec304602ab0931fd23b4ecf74cf7479f8ee076d60b954824090525ae097`
and constant-width formula authority
`031e01934e5a5954a9ef3f2ac4eff536072ccca26db56efbe7d47cd8913ffdc6`.

The R9Y protocol, blocked package manifest, historical failure and acquisition
implementation have SHA-256 values
`342b45dc6c8382e780b4b6fdcb4dc3fd7759e9bec59d5f4fceecc5fa14d116ed`,
`d0254407d973eca7a5b2fbe1bf34c15a9ef0529c190672947bb2130f92c985a0`,
`06571618df9faa884bb008aa61bf6cfc611c269ac9611847248cb04a5f2afd10`
and `409554e382e85198f6f5e4a04ff75c8a31a40e681ee741c13fb6cc32d0713b1a`.
The R9Z protocol and manifest are bound at
`c3ab50099536267d82538d444bb0814c80d8cc7e0847f18998e0e9622cb76d88`
and `3bc702538cae9a28cf739f5fa0a9e9d307fa9e45ee6fccea4c46e8ee6d19c456`.

The R9AA protocol, report, manifest, v2 authority implementation and failure
boundary have SHA-256 values
`67ab53650bf1c483cea322156b74313806df173b2f914dba697198c6c662aba4`,
`91be03a8b9d5c8a68c4d868aa5a52d115b8d2b5a9090dac4371aaacab9db499d`,
`d919a4095b25882c8ba6c49a4091e325a7b227dcc3a69d1a740c7c9a33e4035d`,
`306d542c812bc1f72c84ae1171bfe11c55adafb52e8cb5e391568ad0b6d63431`
and `d60a6cd5cf87903857cc06b686bfaa247b139588d8465a342605d259f1fb94fb`.

The R9V reference implementation, owner verifier and onset-owner authority are
bound at `07697bfa5329193cb5f6cc1f632f1128654a04115fa48c2a8fcfc5656553168f`,
`417233c6baf694b35d4ce835fe1164ed9730a54df81e2cb8b6deb06911a330cd`
and `d62dc611a759b03c68098c7628568d3f9e78fd59d995a4f555dd6ed325171b97`.
R9J linear validation is bound at
`a242cb0921d1fd390cd2b74c9d15a0fdff3e2b82e46000dc8be0cbbba6d6c139`.
The protocol also binds the exact R9J/R9M and single-writer implementations in
the pre-access private authority inventory; any drift blocks access.

## Tie relation and authority construction

The version-2 serialization is integer schema version `2` and
`canonical-json-v2`. It contains exact cell bounds, constant-width formula
authority, canonical exact `q`, `dot` and `cross2` coefficient records, ordered
`pair_left_ref` and `pair_right_ref`, every unique competitor reference, source
authority, a separately hash-closed tie authority and overall provenance.

The relation is prospectively frozen as `common_inactive_branch`. This follows
from three retained facts: R9V recorded the unresolved leaf as `equal`; its
hash-bound `PairAuthority.equal_on` permits that status only for symbolic
identity or both functions proven zero across the cell; and R9Y's retained
`pair_authority` failure establishes distinct coefficient hashes. The older R9V
public flag named `symbolic_identity` was mechanically derived from the presence
of equal cells, not from the `identical` predicate. It remains preserved as an
inaccurate historical summary label and is not authority for R9AB.

If the two newly derived ordered references are equal, the retained equal-cell
record is absent or changed, or the common inactive-branch implication cannot
be reproduced solely from the frozen lineage, acquisition stops. R9AB may not
fall back to `symbolic_identity` or `tolerance_certified`.

The tie hashes are defined exactly:

- `boundary_authority_sha256` is the raw retained boundary-capture hash above.
- `interval_authority_sha256` is the canonical SHA-256 of schema version,
  ordinal, depth, exact derived lower and upper bounds, the outer-interval
  source hash and the ordered-partition hash.
- `structural_tie_lineage_sha256` is the canonical SHA-256 of schema version,
  the retained equal-cell record, the ordered pair indices, the R9V reference
  implementation hash and the R9V private-index hash.
- `tolerance_authority_sha256` is the canonical SHA-256 of schema version,
  the literal `1e-12` maximum-owner rule and the two frozen owner-source hashes.

The selected-edge copy is read only after lineage validation and a synchronized
access-attempt record. The acquisition derives exact rational coefficients for
both ordered pair members and every other defender. Coefficient records and
competitor references are deduplicated only by exact canonical coefficient
hash. An ignored derivation receipt binds the source defender count, ordered
pair positions, excluded pair count, derived competitor positions, unique
coefficient count and unique competitor count without identities or raw
coordinates. Omitting any non-pair defender blocks.

The unresolved bounds are derived only from the retained outer interval and the
validated ordered depth sequence through the unchanged R9X
`derive_leaf_bounds` contract. The authority is written once and accepted only
when the R9AA compatibility loader validates it and serialize-load-serialize
produces byte-identical canonical content.

## Lifecycle, failure and publication

The isolated runner exposes only `preflight`, `acquire` and
`publication-check`. Fresh review, access-attempt, materialization, authority,
publication and closure markers are create-once and do not grant authority.

R9AB uses an acquisition-specific linear journal with only these event classes:
lineage review, access attempt, materialization, authority persistence,
structural validation and terminal success or failure. Its validator reuses the
R9J canonical encoding, sequence, hash-chain and single-pass design, plus the
R9V immutable single-writer descriptor. It emits no numerical-stage, candidate
evaluation or scientific-completion event. One review creates one immutable
content-bound closure authority, which is reused by private and public package
validation without replay.

Failures use R9AA's sealed capture-first boundary. The original traceback must
be synchronized and validated before blocked-QC or publication. Secondary QC or
publication failures are written separately and cannot alter original evidence.
Any governed failure stops without another read, repair or rerun.

Public output is exactly seven files under
`outputs/continuous_occlusion_terminal_cell_authority_v2_acquisition/`:

- `acquisition_contract.json`
- `lineage_validation.json`
- `authority_capture.json`
- `pair_authority_validation.json`
- `sufficiency_validation.json`
- `qc.json`
- `manifest.json`

Public evidence contains hashes, versions, counts, flags, exposure totals,
classification and readiness only. Exact bounds, coefficients, pair relation
details, empirical ordinals, source geometry, access records and tracebacks
remain ignored and hash-bound. Publication checking validates persisted bytes
and never reconstructs evidence or rereads the selected edge.

## Acceptance, decisions and delivery

Before retained access, focused R9AB tests must cover exact lineage and each
hash mismatch; partition order and coverage; unresolved-cell identity; exact
synthetic coefficient and bound derivation; canonical fractions; ordered
distinct pair references; common inactive-branch proof; complete competitor
coverage and deduplication; provenance; deterministic bytes; compatibility
loading; round trips; interrupted materialization; uncertain exposure; journal
sequence and chain; single-pass authority reuse; capture-first failure ordering;
publication failures; marker collisions; rerun rejection; and tripwires for all
forbidden access, numerical, bound, refinement and classification routes.

R9AA, R9X, R9Y, R9Z, R9M, R9J and publication regressions, the full active and
clean tracked-only suites, compilation, schema/hash and finite-JSON checks,
privacy, links, historical bytes, staged inspection and `git diff --check` must
pass. The tested implementation commit must be pushed and pass distribution,
Python 3.11 and Python 3.13 CI before one immutable R9M checkpoint receipt can
authorize access. A bounded pre-access correction is a separate commit followed
by complete checkpoint revalidation; shared history is never rewritten.

Classification **A / readiness 1** requires one canonical v2 authority, valid
distinct ordered pair references, complete competitor authority, valid
common-inactive-branch lineage, byte-identical loader round trip, exact reopened
exposure, zero prohibited computation and green delivery. **B / readiness 2**
means capture with one unresolved structural validation. **C / readiness 3**
means the required coefficient or tie authority cannot be derived within scope.
**D / readiness 4** means execution, integrity, publication or delivery is
invalid or blocked.

The principal commits are:

1. `research: freeze v2 terminal-cell authority acquisition`
2. `research: capture v2 terminal-cell authority`
3. `research: close v2 terminal-cell authority acquisition`

After a successful acquisition, recommend exactly: **separately govern the
terminal-cell independent bound using only the persisted TerminalCellAuthority
v2, with zero empirical reopening.** Do not execute that bound automatically.
