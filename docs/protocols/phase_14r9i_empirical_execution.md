# Phase 14R9I — continuous occlusion empirical representation run

Frozen 2026-09-16 from clean synchronized
`7246f3d8638b1d15b4d55c2432695fcce38f754c`. Live GitHub `main` matched that
commit and annotated `v0.1.0` peeled to
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

## Question, authority, and boundaries

Execute one label-free descriptive study on the already-spent nine-match
development population. Determine whether the frozen expanding or
constant-width carrier-origin field adds structural description beyond the
isotropic proximity baseline and whether one warrants later behavioral
receiver-choice validation.

Session 14R9H established **A — runner authority context correctly wired /
readiness 1**. The unchanged numerical evaluator validates the computation
geometry and joins alias, origin, receiver, and ordered defenders before the
maximum-verifier authority builder. R9I uses that route without a new context
layer or audit. Historical R9G remains **D — invalid / blocked, readiness 4**.

R9I binds exact committed authority for R9 synthetic acceptance, stress and
visual evidence; R9a publication reconciliation; R9F's hash-only portable
fixture; R9H runtime context; Session 14as certificate semantics; Session 14ar's
registered certificate; and the unchanged field, integration, certification,
projection, lifecycle, aggregation and publication implementations. A hash
mismatch or unauthorized implementation change blocks access.

No target, chosen receiver, rank, outcome, model output, option share, protected
detail, withheld match, pose, xT, progression, sister-project data, empirical
visualization, or behavioral validation may enter this phase. The carrier
position is a proxy for ball origin, not a measured ball position.
Accessibility remains **PROXY ONLY** and suppression remains **NOT SUPPORTABLE**.

## Pre-access and execution authority

The isolated runner exposes `preflight`, `run`, and `publication-check`; only
`run` can authorize geometry. Startup and imports remain inside a
standard-library failure boundary. Original exceptions and private tracebacks
must survive failed journal replay or publication.

The runner verifies the committed protocol, implementation, environment,
lockfile, inherited manifests, exact R9F fixture bytes, approved R9 SVG, and
fresh marker state. It runs and validates a fixed discoverable R9I prerequisite
test command with the locked interpreter. Green Python 3.11, Python 3.13, and
distribution CI for the exact implementation commit are required before
unlock. Full-suite and clean tracked-checkout runs are validation replays, not
new governed acceptance.

Use fresh create-once pre-access, empirical-attempt, preparation, and execution
markers in `outputs/continuous_occlusion_empirical_retry_r9i/local/`. Reserve
the attempt before possible geometry exposure. A marker never grants access.
Unlock is derived from the required hashes, implementation freshness,
environment, tests, CI, population authority, target guards, and fresh markers;
there is no manual override.

The approved R9 acceptance remains 108 cases, 366 references, and 399 mapped
permutations with exact canonical structure, component order, accepted
resolution, and the frozen finite-float equivalence rule. R9I does not rerun
that governed acceptance or rerender the inherited 1800×800 SVG.

## Population, projection, and lifecycle

After authorization, require canonical population SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`,
exactly 7,227 states, and match counts `885, 801, 952, 877, 861, 629, 764,
734, 724`. Discover membership and receiver ordinals lexically, skip targets
before analytical records exist, and persist a projection attempt followed by
a synchronized materialization receipt listing every opened edge. Freeze the
target-free prepared bytes and hash before evaluation.

A state opens when its successful materialization receipt is durable. An edge
opens when projected edge geometry is durably receipted. Candidate evaluation
starts are separate work counters. Candidate calls never create geometric
edges. Unconfirmed receipts preserve exposure uncertainty. Complete an edge
only after all three candidates and required checks and summaries succeed;
complete a state only after every required edge and state summary is retained.

Execute canonical file order, then candidate order `isotropic`, `expanding`,
`constant_width`, then canonical receiver order. Public lifecycle authority is
derived from the hash-chained journal and appears only in
`qc.json.progress_authority`. It records attempt and unlock state, preparation,
opened/completed states and edges, candidate work, unresolved exposure,
terminal state and publication state. Detailed records remain ignored.

## Numerical and certificate contract

Preserve the isotropic, expanding, and constant-width formulas with `sigma=2 m`,
onset `1 m`, expansion angle `10 degrees`, float64 arithmetic, and the `1e-9 m`
origin–defender exclusion. Stable bounded union is primary; maximum is the
comparator.

The joint vector uses Simpson intervals `256, 512, 1024, 2048, 4096, 8192,
16384` and accepts the first finer vector for which every component changes by
at most `1e-7`. Preserve onset-aware certification, canonical partitions,
certified tie endpoints, inherited degenerate-edge behavior, and micro-residual
eligibility `width <= 1e-12/N`. Preserve all strict/repeat, onset-only,
direct-Simpson, joint-maximum and applicable historical split-Simpson gates.

Runtime certificate requests derive their authority from the active validated
geometry and structural record. Exact certificate matching retains the adaptive
estimate and warning. The registered certificate covers only its exact terminal
contribution, uses the frozen outward aggregation rule, and cannot waive a
whole-integral disagreement. No automatic certificate generation or authority
broadening is permitted. Unmatched warnings, failed roots, invalid partitions,
nonfinite values, nondeterminism, or failed agreement stop the run.

## Frozen summaries and aggregation

For every candidate under union and maximum, `receiver_field` is the endpoint
value and `segment_integrated_field` is the length-normalized average over
`t in [0,1]`. Report `union_minus_max = union - maximum`, using absolute
`1e-12` only for numerical equality.

Retain Session 13 receiver- and segment-nearest distances, `d2-d1` and `d3-d1`,
fractional top-1/2/3 involvement, support overlap, nearest-role divergence and
multi-edge involvement. Candidate comparisons use second-minus-first values for
isotropic→expanding, isotropic→constant-width and
expanding→constant-width at corresponding receivers. Candidate and
receiver-versus-corridor ordering uses strict agreement, strict reversal, tie
creation, tie removal and both tied under the inherited `1e-12` block-anchor
rule. The inherited equal-minimum fixture is synthetic capacity evidence only.

Use Spearman only with exact-tie average ranks. Constant or insufficient groups
are unavailable. Weight equal match → state → edge. Pair-derived continuous
observations reduce to state means and ordering indicators to state proportions
before extrema and inverse-ECDF percentiles. Report raw source counts,
published observation counts, assessability, all nine matches, represented
matches and equal-match summaries. No threshold, weighted winner score,
Pearson, regression, p-value, or new metric is authorized.

## Failure, publication, and decisions

The first numerical or execution failure stops the complete run, including a
recurrence of a historical failure. Preserve active match/state/edge/candidate,
stage, warning and certificate status, actual and uncertain exposure, completed
work, original exception, and private traceback. Do not omit work, repair,
restart, or interpret partial scientific output.

The public namespace is
`outputs/continuous_occlusion_empirical_retry_r9i/` with exactly the inherited
nineteen filenames: `preaccess_contract.json`,
`retained_observation_certificate_gate.json`, `runner_failure_oracles.json`,
`runner_success_oracle.json`, `synthetic_acceptance.json`,
`evidence_type_summary.json`, `visual_qa.json`, `candidate_summary.json`,
`structural_correspondence.csv`, `candidate_pair_comparison.csv`,
`receiver_corridor_ordering.csv`, `union_vs_max.csv`,
`overlap_redundancy.csv`, `multi_edge_summary.csv`, `match_summary.csv`,
`synthetic_stress_summary.json`, `qc.json`, `manifest.json`, and
`synthetic_field_comparison.svg`. Failure publication marks unexecuted
scientific artifacts unavailable. There is no public lifecycle or compatibility
artifact.

Publication validation reads actual persisted files, replays journal authority,
recomputes lifecycle and exposure hashes, verifies private bindings and never
regenerates missing evidence. Validate the package before interpretation and
again after hash closure.

Choose exactly one classification: **A** one directional candidate warrants
later behavioral validation; **B** multiple directional candidates remain
plausible; **C** directional fields add little beyond simple geometry; **D**
invalid or blocked. Choose readiness independently: **1** ready for behavioral
receiver-choice validation of one frozen field; **2** geometric descriptor or
visualization only; **3** representation needs revision; **4** not justified.

Assess interpretability, smoothness, carrier-relative direction,
multi-defender composition, multi-edge behavior, Session 13 correspondence,
equal-minimum discrimination, distinction from isotropic proximity, software
simplicity and visualization usefulness without weights or new thresholds.
Only complete A/readiness-1 evidence freezes a candidate specification.

## Execution and closure

Preserve three commits: protocol; tested R9I orchestration and authority
bindings; then closed outputs, report, and append-only research-log entry. Push
the tested checkpoint and require green CI before one empirical attempt. After
closure run focused, relevant, full, tracked-only, compilation, schema/hash,
finite-value, privacy/access, link, history, staged-file and diff checks; push
and require final green CI.

If pre-access fails, preserve zero-access evidence and stop. If the empirical
run fails, preserve exposure and stop without repair or rerun. The report maps
all 35 requested handoff items. Recommend exactly one result-dependent next
action: behavioral receiver-choice validation beyond M1 for A/readiness 1;
bounded candidate discrimination for B; retention of simpler geometry for C;
or one bounded blocker diagnosis for D. Execute no recommendation.
