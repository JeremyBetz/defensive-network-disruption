# Phase 14R9G — empirical execution after CI-portability repair

Frozen 2026-09-16 from clean synchronized
`c54d20121ea6107728f82759d82a2444be67ce6a`. The annotated `v0.1.0` tag
peels to `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

## Question, authority, and limits

This is one fresh label-free execution on the already-spent nine-match
development population. It asks whether the frozen expanding or constant-width
carrier-origin fields provide complementary structural description beyond the
frozen isotropic baseline and whether one warrants later behavioral validation.
It preserves Session 14R9E as **D — blocked / readiness 4** and Session 14R9F as
**B — minimal sanitized committed authority fixture required and valid /
readiness 1**.

R9G binds the committed Session 14R9 synthetic acceptance, stress fixture and
visual evidence; the R9a nineteen-artifact publication validator; the R9F
CI-portable hash-only authority fixture; the Session 14ar certificate registry;
and the unchanged field, certification, integration, lifecycle, projection and
aggregation implementations. Hash mismatch, unauthorized implementation drift,
failed prerequisites, failed validation, absent required CI, or a nonfresh
marker blocks access.

No new numerical acceptance, certificate, tolerance, field, dependency, root
export, claim, or release is authorized. No target, chosen receiver, target
rank, outcome, model value, option share, provider acquisition, protected
detail, withheld match, pose, xT, progression, empirical visualization, or
behavioral validation may enter this phase. Accessibility remains **PROXY
ONLY** and suppression remains **NOT SUPPORTABLE**.

## Frozen pre-access contract

The isolated runner exposes `preflight`, `run`, and `publication-check`; only
`run` can authorize development geometry. Startup uses a standard-library
failure boundary before importing project numerical dependencies. The locked
project interpreter produces and validates a fixed discoverable R9G test
record. Independent emergency evidence must retain import, prerequisite,
marker, callback, and publication failures even when journal replay or normal
publication is unavailable.

The runner verifies exact committed hashes for all inherited evidence and
implementation authorities. It validates the R9F fixture's exact bytes, schema,
source hashes, field-selection specification, and non-reconstructive allowlist,
without reading ignored historical geometry. The fixture supports provenance;
it is not geometry. Runtime certificate requests derive their own selected
geometry and structural semantic fingerprints. Copying retained identifiers or
using synthetic provenance cannot manufacture a match.

The approved R9 SVG is inherited byte-for-byte. No render route exists. Full
local and clean-checkout validation and green Python 3.11, Python 3.13, and
distribution CI for the exact tested implementation are required before access.
These are validation replays and do not create a new governed synthetic or
scientific result.

Fresh create-once pre-access, empirical-attempt, preparation, and execution
markers are required. A marker reserves work and never grants access. Expected
negative-control rejection is a passing test; any unexpected governed gate
failure closes R9G without repair or rerun.

## Population, access, and lifecycle

After authorization, require canonical population SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`,
exactly 7,227 states, and match counts `885, 801, 952, 877, 861, 629, 764,
734, 724`. Discover membership and receiver ordinals lexically, skip targets
before analytical values exist, and persist a projection attempt followed by a
synchronized complete materialization receipt. Freeze target-free prepared
bytes and their hash before evaluation.

Access and work use distinct counters. A state is opened by a unique successful
state-materialization receipt. Every receiver edge in that receipt is opened at
projection materialization, even if evaluation never starts. Edge evaluation
starts are counted separately. Candidate calls do not create additional
geometric edges. Preparation failure preserves projected exposure. Unconfirmed
receipts preserve uncertainty and cannot be reported as zero exposure.

Evaluation order is canonical state → candidate → receiver. A geometric edge
completes only after all three candidate calls and required numerical checks
succeed. A state completes only after all required edges and summaries are
durably retained. Public `qc.json.progress_authority` reports attempt, unlock,
preparation, opened and completed state/edge counts, candidate work, unresolved
exposure, terminal status and publication status, derived from the durable
journal. Detailed journal, snapshots, prepared records, geometry and partial
science remain private and ignored.

## Frozen numerical contract

Candidates are isotropic proximity, smooth expanding directional, and smooth
constant-width directional fields. Preserve `sigma=2 m`, onset `1 m`, expansion
angle `10°`, float64 arithmetic, the `1e-9 m` origin–defender exclusion, and the
carrier-position proxy for ball origin. Stable bounded union is primary and
maximum is the comparator.

The joint vector uses Simpson intervals `256, 512, 1024, 2048, 4096, 8192,
16384` and accepts the first finer vector for which every governed component
changes by at most `1e-7`. Preserve onset-aware certification, exact canonical
partitions, certified tie endpoints, inherited degenerate-edge behavior, and
micro-residual eligibility `width <= 1e-12/N`. Preserve strict/repeat,
onset-only, direct-Simpson, joint-maximum, and applicable historical
split-Simpson gates.

The independent-certificate route applies only to an exact eligible warning and
an exactly matching registered authority at the adaptive-result boundary. It
retains the adaptive estimate and warning, uses conservative outward interval
aggregation once, and cannot waive whole-integral disagreement. Any unmatched
warning, failed root, malformed certification, invalid partition, nonfinite
result, nondeterminism, or failed comparison stops the complete run.

## Descriptive analysis and aggregation

For every candidate under union and maximum, retain `receiver_field` as the
endpoint value and `segment_integrated_field` as the length-normalized average
over `t in [0,1]`. Report union minus maximum and numerical equality only at
absolute `1e-12`.

Retain Session 13 receiver- and segment-nearest distances, `d2-d1` and `d3-d1`,
fractional top-1/2/3 involvement, support overlap, nearest-role divergence and
multi-edge involvement. Candidate-pair comparisons use second-minus-first
values for isotropic→expanding, isotropic→constant-width, and
expanding→constant-width. Receiver-versus-corridor and candidate ordering use
strict agreement, strict reversal, tie creation, tie removal and both tied with
the inherited `1e-12` block-anchor rule. The inherited equal-minimum synthetic
result is formal-capacity evidence only.

Use Spearman only with exact-tie average ranks. Constant or insufficient groups
are unavailable. Weight equal match → state → edge. Pair-derived continuous
distributions reduce to state means and ordering indicators to state
proportions before extrema and inverse-ECDF percentiles. Keep source counts
separate from published observations. Report every match, the equal-match
macro, represented-match counts, denominators and unavailable reasons. No
threshold, composite winner score, Pearson correlation, regression or p-value
is authorized.

## Failure, publication, and decision

The first empirical or execution failure stops all work. Preserve active
match/state/edge/candidate, numerical stage, warning and certificate status,
actual and uncertain exposure, completed work, original exception and private
traceback. Do not exclude an edge, restart, repair, or interpret incomplete
science.

The public namespace is `outputs/continuous_occlusion_empirical_retry_r9g/`
with exactly the existing R9a nineteen filenames. Lifecycle authority appears
only in `qc.json.progress_authority`; there is no public
`lifecycle_summary.json`. Complete, blocked, and failure schemas are frozen in
the tested implementation. Publication validation reads actual persisted files,
replays journal authority, verifies private bindings, and never reconstructs
missing evidence. Persisted evidence must validate before a success receipt is
issued; the resulting closed package is then hash-verified again.

Choose one scientific classification: **A** one directional candidate warrants
later behavioral validation; **B** multiple directional candidates remain
plausible; **C** directional fields add little beyond simple geometry; **D**
invalid or blocked. Choose readiness independently: **1** ready for behavioral
receiver-choice validation of one frozen field; **2** ready only as geometric
descriptor/visualization; **3** representation needs revision; **4** not
justified. Assess interpretability, smoothness, carrier-relative direction,
multi-defender composition, multi-edge behavior, Session 13 correspondence,
equal-minimum discrimination, baseline distinction, software simplicity and
visual usefulness without weights. Only complete A/readiness-1 evidence freezes
a candidate.

## Execution and closure

Commit chronology is protocol; tested implementation and authority bindings;
then one empirical attempt followed by closed evidence, report and append-only
research-log entry. Push the tested checkpoint and require green CI before the
attempt. After the attempt, validate focused, relevant, full, compilation,
schemas/hashes, privacy/access, links, history, staged files and diff checks;
push closure and require final green CI. Preserve clean synchronized local,
tracking and live heads and the unchanged release tag.

If pre-access fails, preserve zero-access evidence and stop. If empirical work
fails, preserve exposure and stop. No governed rerun is authorized. Recommend
exactly one next action: behavioral validation beyond M1 for A/readiness 1;
bounded candidate discrimination for B; simpler geometry for C; or one bounded
blocker diagnosis for D. Execute no recommendation.
