# Phase 14R9H — runner authority-context wiring diagnosis and repair

Frozen 2026-09-16 from clean synchronized
`6385792269c9beb034722d5f01bd74ea391971fd`. Live GitHub `main` matched that
commit, and annotated `v0.1.0` peeled to
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

## Question and authority

Determine whether the actual empirical orchestration path supplies the complete
runtime geometry required by the exact certificate-authority matcher. Session
14R9G remains **D — invalid / blocked, readiness 4**. Its pre-access conclusion
was based on the identifier-only metadata supplied by `calculate_edge`; static
inspection now shows that the lower-level evaluator validates the numerical
geometry and constructs a geometry-complete `runtime_context` before invoking
the independent maximum verifier. That later join is a planning-time finding,
not an accepted result. This phase tests the complete route without assuming
either loss or correctness.

This is synthetic orchestration and retained-observation work only. It opens no
development geometry, runs no empirical analysis, inspects no partial scientific
output, and changes no field, tolerance, integration, certificate, public API,
dependency, claim, or release contract. Empirical access must remain zero states
and zero edges. All historical files and classifications remain unchanged.

## Frozen authority flow

Trace the actual route
`summarize_state -> calculate_edge -> evaluate_edge -> evaluate ->
validate_geometry/points -> runtime_context -> independent_maximum -> _piece ->
_runtime_request`.

The prospective contract distinguishes orchestration identity from certificate
authority. `alias`, state ordinal, and edge ordinal identify active work. The
selected-geometry fingerprint uses `alias` plus the validated float64 origin,
receiver, and defender sequence. Candidate, frozen parameters, maximum
combination, interval endpoint bits, canonical partitions, onsets, and switches
come from the active numerical evaluation. State and edge ordinals are not added
to the certificate fingerprint. Defender order is the row order returned by the
unchanged `validate_geometry`; no sorting or second geometry path is permitted.

The authoritative geometry source is the geometry already supplied to and
validated by the field computation. The certificate layer may not reopen a
provider file, load prepared geometry, reconstruct values from identifiers, or
substitute retained Session 14ar geometry. Test observers may record synthetic
values at the evaluator, context join, and authority builder only when they
delegate to unchanged functions and cannot alter the result.

If the complete route already carries identical canonical geometry, make no
cosmetic production refactor. If a unique loss is demonstrated, the only
authorized repair is an internal immutable context constructed from the already
validated origin, receiver, and defenders and forwarded through existing
interfaces. Any need for another geometry loader, new authority fields, fuzzy
matching, certificate changes, or numerical changes blocks the phase.

## Frozen oracles

Use provider-free synthetic geometry with synthetic identifiers to exercise the
future empirical call chain. Require the authority builder to observe the same
canonical float64 origin, receiver, and ordered defenders as numerical
evaluation. Derive the runtime fingerprint and show that the downstream
authority boundary receives it; synthetic provenance never becomes empirical
certificate authority.

Mutate origin, receiver, and one defender independently. Each change must alter
the selected-geometry fingerprint. Hold alias, state, and edge fixed while
changing geometry and require exact matching to fail. Exercise defender
permutations according to the existing order-sensitive selected-geometry
contract; do not invent normalized ordering.

Separately exercise the committed Session 14ar retained observation and
registered certificate at the already approved adaptive-result boundary. This
must preserve the warning, exact request, successful certificate status, and
readiness contribution without reconstructing empirical geometry. An unmatched
synthetic or authority-mutated request must retain the warning, fail exact
lookup, remain blocking, and produce false readiness. The two layers together
test the actual runner-to-builder join and the retained exact-match behavior;
neither layer alone establishes empirical execution.

Add guards proving the maximum verifier and certificate layer read no provider
or prepared geometry and use no ignored/private fallback. Inject context,
startup, marker, and publication failures through the isolated runner. Preserve
the original exception, traceback, zero access, invalid readiness, valid failure
evidence, and rerun rejection. All tests must pass in a tracked-only checkout.

## Execution, evidence, and decision

The isolated runner exposes `preflight`, `audit`, and `publication-check`.
`audit` claims a create-once marker and executes exactly one synthetic
end-to-end authority-context acceptance after the tested tooling commit. An
unexpected post-exposure failure is preserved without repair or rerun.

Create exactly eight public artifacts under
`outputs/continuous_occlusion_runner_authority_context/`:
`authority_flow.json`, `context_contract.json`, `mutation_oracles.csv`,
`retained_certificate_orchestration.json`, `unmatched_warning_oracle.json`,
`publication_regression.json`, `qc.json`, and `manifest.json`. Public evidence
contains synthetic facts, hashes, counts, booleans, and sanitized interface
names only. No reconstructive empirical value is permitted.

Choose exactly one classification: **A** runner authority context correctly
wired and fresh empirical retry authorized; **B** one further bounded repair;
**C** preparation cannot supply authority safely; **D** multiple architectural
issues; **E** unresolved; or **F** invalid/execution failure. Choose readiness
independently from 1 ready, 2 ready after small repair, 3 architecture revision,
or 4 not ready. A/readiness 1 requires complete context at the actual builder,
identity between computation and authority geometry, successful mutation and
false-match controls, retained-certificate success, unmatched-warning blocking,
no geometry reopening, valid failure publication, tracked-only portability, and
green required CI.

Preserve three commits: protocol; tested tooling and any strictly necessary
bounded forwarding repair; then closed evidence, report, and append-only
research-log entry. Run focused, relevant, full, tracked-only, compilation,
schema/hash, finite-value, privacy, link, history, staged, and diff checks. Push
and require Python 3.11, Python 3.13, and distribution CI. If A/readiness 1 is
earned, recommend only a separately governed fresh empirical R9 execution using
the verified runtime authority-context wiring and existing numerical/publication
authority. Do not execute it.
