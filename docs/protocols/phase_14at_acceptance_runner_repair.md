# Phase 14at — Governed acceptance-runner repair and synthetic contract acceptance

Date: 2026-09-15. Planning: OFF; Turbo: ON; Model: Sol Light. Starting
authority: `c4078ac585fa293b695b4f1112c7716fc380c7b2`. Local, tracking, and
live GitHub `main` were clean and synchronized before mutation. Preserve
annotated `v0.1.0` at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

## Question and scope

Session 14as closed **F — invalid / execution failure; readiness 4** because
its governed runner loaded
`defensive_network_disruption.validation.onset_owner_acceptance` from its
source path with `importlib.util.spec_from_file_location`. That standalone
module had no package parent, so Python rejected the module's relative import
with `ImportError: attempted relative import with no known parent package`.
The failure occurred before governed synthetic results were persisted.

This phase repairs only that execution context and the runner's success and
failure publication. It preserves Session 14as as invalid and changes no
certificate mathematics, warning identity, authority match, tolerance,
containment, quadrature, residual, aggregation, readiness, field, dependency,
version, released API, or historical artifact. It accesses no empirical data,
geometry, model, target, share, outcome, provider product, or partial scientific
output. Governed evidence must report zero empirical states and edges.

## Package loading and runner contract

The approved mechanism is a normal installed-package import of
`defensive_network_disruption.validation.onset_owner_acceptance`, executed by
the locked project interpreter. Direct source-file loading, synthetic package
names, repository-specific `sys.path` mutation, and copied authority code are
forbidden in the governed route. A test-only engineering oracle must retain the
historical direct-file failure.

Provide one internal runner with `preflight`, `accept`, and
`publication-check`. Its single acceptance path must preflight repository and
retained authority, load Session 14ar's committed certificate, import the
synthetic authority through package context, execute the unchanged Session 14as
contract, persist progress, publish either a strict success or strict failure
package, and stop. The runner exposes no empirical route.

Before the governed execution, test this same runner in editable/local,
ordinary test, and module-invocation contexts. Inject package import,
certificate authority, synthetic regression, and publication failures through
the runner. A valid failure package preserves active stage, exception class and
message, traceback, completed obligations, false readiness, marker state, and
zero empirical access. The historical ImportError or a controlled exact
equivalent must be caught and validated. Success publication must persist and
independently validate the certificate regression, positive and negative
oracles, 108/366/399 results, readiness, QC, and manifest.

Claim a persistent exclusive marker before governed exposure. A second run is
rejected; neither success nor failure may be overwritten. Tests use isolated
temporary markers. Any failure after governed exposure is preserved without
repair or rerun.

## Immutable certificate and numerical authority

The sole eligible warning remains SciPy
`scipy.integrate._quadpack_py.IntegrationWarning` with message SHA-256
`3f2b9b58ff1c98b7c27fff799d18b42a6dd19e50c2b5f6b60a0b80bfb2798ab3`.
Session 14ar remains the only registered certificate. Do not generate or
register another certificate or rerun its Taylor derivation.

Preserve exact certificate serialization and authority matching, realized
binary64 width `<= 1e-10`, closed-enclosure containment, the four piece
branches, retained adaptive estimate, and conservative aggregation using
`math.fsum` plus exactly one final outward `nextafter` per side. Preserve the
Session 14as immutable readiness object without a wrapper override.

Pre-exposure validation must show identity for the Session 14ar regression,
positive and negative controls, ordinary quadrature, micro-residual behavior,
aggregation, certificate serialization, and readiness derivation. Replay the
accepted synthetic authority with exact canonical structure, component order,
accepted resolution, and current finite-float equivalence. Require 108/108
cases, 366/366 references, and 399/399 mapped permutations. A semantic or
numerical difference stops the phase before governed execution.

## Single acceptance, outputs, and decisions

After all package-context, direct-file negative, publication, numerical,
compilation, and diff gates pass, execute exactly one governed synthetic
acceptance. It must persist Session 14ar regression success, all positive
controls, all negative controls blocking, 108/366/399 completion, derived
readiness true, zero blocking warnings, zero empirical access, and valid success
publication. No empirical loader is reachable.

Create exactly these public files under
`outputs/continuous_occlusion_acceptance_runner_repair/`:

- `runner_contract.json`;
- `package_context_regression.json`;
- `publication_oracles.json`;
- `certificate_regression.json`;
- `synthetic_reference_regression.csv`;
- `readiness.json`;
- `qc.json`;
- `manifest.json`.

Detailed traces and execution records remain ignored and private. Public files
use strict finite schemas, atomic writes, and manifest-bound hashes.

Classify exactly one of: **A — governed acceptance runner repaired; certificate
contract accepted**; **B — runner repaired but end-to-end acceptance still
blocked**; **C — publication/failure-handling defect remains**; **D — package
context repair changes numerical behavior**; **E — multiple issues**; or **F —
invalid/execution failure**. Assign Session 14R readiness independently from 1
fresh separately governed retry, 2 one bounded wiring/publication repair, 3
verifier/production revision, or 4 not ready. Do not force A.

Preserve exactly three commits: this protocol; tested runner/publication repair;
closed evidence/report/log. Run focused, relevant, and full tests, compilation,
108/366/399 regressions, schemas, hashes, success/failure validators, privacy,
links, history checks, staged inspection, `git diff --check`, and CI. Push and
verify synchronized heads and the release tag.

No claim-ledger change or scientific result is permitted. A/readiness 1 supports
exactly: **separately govern a fresh Session 14R empirical representation retry
under the accepted independent-certificate verifier contract.** Do not execute
it.
