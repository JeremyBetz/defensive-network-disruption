# Phase 12a — Threat prerequisites

## Prospective authority

Start from clean synchronized `5b72645a1a6d10b94f4f747e7a34978e98d6e7ba`.
Preserve release `v0.1.0` at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
Commit this protocol before collecting new audit evidence. The user authorizes
a documentation/provenance audit, not empirical Session 12 execution.

## Permitted inspection

Read public methodology, official competition rules, package source and licenses,
and existing committed project provenance and population-building source code.
Review socceraction.xthreat and its documented published 12-by-8 baseline first;
do not search alternative surfaces to obtain a favorable eligibility finding.
Record immutable package revision, dependency constraints, source methodology,
surface training provenance, separate code/surface licensing, grid indexing,
orientation and dimensional assumptions. Public documentation access is allowed;
do not fetch the numerical surface JSON or install/import the package.

Trace project origin, units, direction and dimensions through committed code and
documentation only. Use git-tracked file lists for searches. Do not read ignored
population rows, raw metadata/events/tracking, mixed-content manifests, protected
detail, withheld or pose data. No provider acquisition, fitting, threat lookup,
empirical horizon, synthetic numerical execution or public API implementation.

## Decision rules

Library reuse permission does not establish permission to use external learned
parameters. Seek explicit competition evidence; unavailable clarification means
unresolved, not prohibited. Separate surface redistribution permission from code
licensing. Coordinate mapping requires evidenced origin, axis direction, actual
dimensions and surface orientation; do not infer dimensions from extrema or use
the synthetic pitch as empirical authority. Identify the smallest later
development-only metadata projection if required, without performing it.

Return READY FOR SESSION 12 PROTOCOL only when licensing, eligibility, provenance
and coordinate mapping are established. Otherwise use BLOCKED — ELIGIBILITY,
BLOCKED — COORDINATES, or BLOCKED — MULTIPLE PREREQUISITES as applicable.
Do not assign empirical scientific/software classifications.

## Formal deductions and outputs

Document signed destination changes, horizon and receiver decomposition, carrier
baseline cancellation, changed shared model coefficients, and conditional-choice
versus possession-value semantics algebraically only. Leave mutually exclusive
case definitions and numerical tolerances to a later empirical protocol.

Create `docs/session_12a_threat_prerequisite_audit.md` and
`outputs/threat_weighted_option_network/prerequisite_summary.json`. Append bounded
entries to the research log and library review. Public records contain provenance,
hashes and gate states, no provider identities/mappings or reconstructive records.
Draft one organizer question in the report; do not send it. All requested
empirical report items must say not executed in prerequisite audit.

## Verification and stop

Check citation support, revisions/hashes, license distinctions, local links,
publication safety, append-only histories, unchanged historical files and tag,
staged contents and git diff --check. Skip software tests: no software change.
Commit the audit separately, push and verify clean synchronized heads. Recommend
exactly one next action determined by the unresolved gate. No automatic further
science, messaging, surface acquisition or release is authorized.
