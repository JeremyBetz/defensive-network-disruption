# Phase 14ak — Constant-width comparator and failure-publication diagnosis

**Frozen:** 2026-09-13  
**Starting authority:** `0ace3b79d18cdfa9b2f3dd26a7bf1d201cdbfa1f`  
**Scope:** one retained development edge plus synthetic publication diagnostics

## Question and retained authority

Session 14ak asks why the Session 14R8 constant-width independent-maximum
comparison failed `piecewise_unsplit`, whether the production estimate remains
numerically healthy, and why normal failure publication was interrupted while
replaying the large journal. It is a diagnostic session, not a continuation of
the representation study.

The only authorized empirical geometry is zero-based state ordinal `4`, receiver
ordinal `7`, candidate `constant_width`, selected from the preserved R8 prepared
population. No other receiver, completed R8 state summary, target, outcome,
model, option share, protected product, pose, xT or progression value may enter
an analytical object. Complete source files may be hashed without decoding.

The retained original failure is `GateFailure: piecewise_unsplit` at numerical
stage `onset_adaptive`. Historical exact comparator estimates and stage timings
were not retained and remain unavailable. Values measured here are explicitly
diagnostic reproductions. The R8 report remains immutable but contains two
transcription errors: the verified R8 protocol commit is
`e96f6968d52986db1f45cead2c5adc55289f080c`, and the verified prepared-file
SHA-256 is
`15a809060f0ba4a8cb3790011dab8008db3dad6e931c287ccb395f61ef1530b0`.
Its approximately 55-minute delay is not established as quadrature time.

Bind before access the R8 protocol and implementation, canonical and prepared
population hashes, original and emergency failure hashes, complete journal hash
and head, lockfile, field implementation, controlled Simpson implementation,
onset-aware certification, canonical partition construction, bounded-residual
verification and publication replay code. If the retained edge or failure
authority is not unique, stop unresolved.

## Numerical contract

Preserve the constant-width field, float64 behavior, ownership semantics,
canonical boundaries, tolerances, integration implementations and work limits.
No field, partition, gate or estimator repair is authorized.

Distinguish three paths:

1. **Production:** the unchanged joint-vector Simpson ladder
   `256,512,1024,2048,4096,8192,16384`, accepting the first finer vector for
   which every component changes by at most `1e-7`. Its maximum is provisional
   until the independent verifier passes.
2. **Piecewise:** bounded-residual intervals plus strict `1e-13` and repeat
   `1e-11` adaptive quadrature over accepted canonical structural pieces.
3. **Onset-only comparator:** the same maximum integrand and adaptive family,
   partitioned only at domain endpoints and canonical directional onsets. The
   historical name `unsplit` means unsplit with respect to switches and tie
   boundaries, not one quadrature call over `[0,1]`.

The unchanged gate requires point-to-piecewise-interval distance at most
`1e-10`. Direct 65,536-interval Simpson retains its `1e-9` gate, and the joint
maximum retains `1e-6`. Supplementary uniform and independently allocated
piecewise Simpson curves use nominal intervals `256` through `16,384`; they are
diagnostics, not adaptive settings or truth. The strict/repeat adaptive results
and the highest authorized converged piecewise evidence form the internal
comparison authority. No higher grid or new quadrature family is introduced.

For the authorized edge, verify partition coverage, ordering, canonical
endpoints, onset multiplicity, switches, ties, owner semantics, residual routing
and width sum. Inspect only exact predecessor/onset/successor floats and bounded
finite-step neighborhoods. Analytical continuity and independent scalar-oracle
agreement are separate from slope and higher-derivative diagnostics.

Each adaptive or replay operation has a ten-minute wall-clock limit; the entire
governed diagnosis has a sixty-minute wall-clock limit. A timeout preserves the
stage and available evidence, marks dependent results unavailable and ends the
one execution. It is not evidence of numerical inaccuracy and cannot trigger a
retry.

## Publication-replay contract

The R8 append-only journal remains authoritative and byte-identical. Verify its
hash chain and bounded metadata without reading scientific summaries. Static
source inspection distinguishes parsing, hashing, prefix replay, lifecycle
reduction and duplicate failure handling.

Measure the unchanged replay behavior on prospectively frozen synthetic journals
with `16,32,64,128,256,512,1024` states and ten edges per state, plus one
synthetic event-shape fixture matching the retained journal's public event
counts. Record count, encoded size, operation count, elapsed time and peak memory
when available. Synthetic keys and payloads contain no empirical identifiers or
geometry. Do not replay a fabricated scientific result.

Assess single-pass streaming replay, authenticated checkpoints, checkpoint plus
suffix replay and incrementally authenticated summaries for correctness,
compatibility, auditability and fail-closed behavior. Implement none. The new
diagnostic uses an independent emergency writer so its original exception,
traceback hash, stage, access receipt and available snapshot survive even if
normal replay fails. An exclusive persistent marker prevents concurrency and
rerun.

## Evidence, privacy and decisions

Exact empirical coordinates, estimates, field values, owners, roots, float bits,
per-piece contributions and onset neighborhoods remain ignored and hash-bound.
Public artifacts contain neutral ordinals, aggregate counts, classifications,
agreement flags, sanitized runtime summaries and evidence fingerprints. A
topology-derived synthetic fixture may be documented after diagnosis but is not
executed in this session.

Choose exactly one numerical result: A production/piecewise healthy and onset
comparator under-resolved; B production/piecewise healthy and agreement contract
too strict for the topology; C production partition or piecewise defect; D both
paths unstable; E stopping or adaptive-routing defect; F multiple issues; G
unresolved; H invalid execution. Choose readiness independently: 1 bounded
comparator/gate repair, 2 small implementation repair, 3 numerical design
revision, or 4 more evidence.

Choose publication P1 large-journal replay defect with bounded repair ready; P2
streaming/checkpoint architecture needed; P3 inconsistent failure record; P4
multiple issues; or P5 unresolved. A retry may be recommended only after a
separate repair succeeds and no production-field defect remains.

## Execution and closure

Before access, test selective edge projection with forbidden-value sentinels,
partition and convergence logging, timeouts, journal integrity, synthetic replay
scaling, emergency preservation, cross-file rejection and automatic-rerun
prevention. Commit tested tooling before the one governed diagnosis.

Publish the fourteen named files under
`outputs/session14_constant_width_comparator_diagnosis/`, strict success/failure
schemas, a report mapping all forty handoff items and one append-only research-log
entry. Close and hash evidence before interpretation. Any post-access defect
stops without repair or rerun. Run focused, relevant and full tests, compilation,
privacy, links, historical-byte checks, staged review and `git diff --check`.
Preserve separate protocol, tested-tooling and closure commits; push, require all
three CI jobs, verify synchronized heads and the unchanged release tag, recommend
exactly one bounded next action, and stop.
