# Phase 07b — Formal diagnostic closure without human review

Status: prospectively frozen after the Phase 07a withdrawal and before final
Session 7 classification. Starting checkpoint:
`2e43368eecb4ccea38d14afb2fc7f3b615ab38f8`.

## Purpose and authority

This continuation closes Session 7 using only the already committed, closed
aggregate diagnostics produced under
[Phase 07](phase_07_construct_validity_diagnostics.md). It follows and is
bounded by the later
[Phase 07a amendment](phase_07a_human_review_withdrawal.md). It does not revive
the withdrawn human-review path, authorize a new diagnostic run, or change any
existing protocol, output, population, model, selection, or rendered artifact.

The question is:

> Does the frozen defensive-geometry receiver-ranking representation exhibit
> prespecified structural behavior coherent enough to serve as a bounded
> attacking-connection or receiver-option primitive?

This is a development-only formal construct-diagnostic and model-behavior audit.
Development target comparisons are **IN-SAMPLE DIAGNOSTICS** because the final
models were trained on the same nine matches. They are not independent
predictive validation, practitioner validation, accessibility ground truth, or
suppression validation.

## Frozen evidence

The only outcome-bearing evidence permitted for interpretation is the committed
public aggregate package in `outputs/construct_validity_diagnostics/`, including
its feature disagreement, utility-component, M2-role, stratified, selection,
quality-control, and manifest artifacts. The package remains byte-identical.
The final model authority has SHA-256
`0383561e45bcd9f8d84eccc7e9733b2409536f61c39193fa87d08c36c6f6fd65`.
The development population authority has SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`
and contains 7,227 observations from the nine spent development matches.

Public Session 6e results may be cited only as the already closed predictive
authority for C09 and C10. No reserved row, passage, per-attempt score, rank, or
other protected detail may be opened. The withheld match and pose data remain
unopened. Raw provider products, ignored Session 7 diagnostic rows, case
mappings, diagrams, forms, and response files may not be opened for this
closure.

No fitting, preprocessing, population regeneration, feature calculation,
alternate coefficient, model change, new bin, new threshold, case selection,
passage rendering, review response, or human or assistant judgment is allowed.
The historical `record-review`, `reveal-passages`, and human-dependent
`close-review` routes remain unauthorized.

## Frozen interpretation framework

Interpret the existing diagnostic families together:

1. M1 feature disagreement: whether receiver-nearest and finite-segment
   defender geometry produce distinct within-choice orderings, and how the
   frozen M1 ordering behaves in those states.
2. Utility decomposition: attacking, receiver-nearest, segment-nearest, and M2
   attenuation fitted utility components, including shared-coefficient changes.
   Use no causal or defender-credit language.
3. M2 role: ordering and top-set changes, target-rank movement, unchanged-order
   states, and the frozen label-independent non-nearest-defender strata.
4. Geometric stratification: only the prospectively frozen feature bins and
   direction categories, with empty strata reported rather than treated as
   zero.
5. Formal failures: only the already selected algorithmic failure quota and
   aggregate worsening distributions. Do not inspect or narrate cases.

Choose exactly one primary classification:

- **A — GEOMETRIC CONSTRUCT COHERENCE SUPPORTED WITHIN DEVELOPMENT
  DIAGNOSTICS:** stable, distinct defensive relationships, M2 behavior coherent
  with distributed proximity, broadly coherent formal checks, and no
  fundamental contradiction. This still does not validate accessibility.
- **B — USEFUL RECEIVER-SELECTION GEOMETRY, BUT ACCESSIBILITY INTERPRETATION
  REMAINS WEAK:** coherent and interpretable geometry whose diagnostics cannot
  distinguish accessibility from correlated tactical or receiver-selection
  structure.
- **C — MIXED CONSTRUCT COHERENCE:** meaningful diagnostic conflicts or failure
  modes materially limit interpretation.
- **D — ACCESSIBILITY-LIKE INTERPRETATION NOT SUPPORTED:** formal behavior
  materially contradicts the intended construct.

Choose M2 separately:

1. Distributed-defender term has clear construct/practical geometric meaning.
2. Distributed-defender term has limited or mixed interpretability.
3. Distributed-defender term is primarily a small predictive refinement.
4. Diagnostic execution invalid.

Also choose one software-readiness result: **YES**, **YES, BUT NARROWLY**, or
**NO**; and one network-readiness result: **YES**, **YES, WITH RESTRICTIONS**,
**NO — RESOLVE A SPECIFIC EDGE ISSUE FIRST**, or **NO — NARROW PROJECT TO
RECEIVER-SELECTION GEOMETRY**. These decisions concern semantics and future
research readiness, not new predictive performance.

## Claim limits and alternative explanations

C09 and C10 remain supported within their existing scope. C01 and C02 remain in
progress. C03–C08 remain untested. Accessibility remains **PROXY ONLY** and
suppression **NOT SUPPORTABLE**. Do not claim causal effects, best-pass judgment,
player attribution or value, practitioner validation, or validated network
disruption.

The report must preserve defenders reacting to latent attacking structure,
team/player tactical tendencies, receiver quality and role, omitted carrier
orientation, progression preference, provider target-generation semantics,
latent game state, and jointly determined attacking and defensive geometry as
unresolved alternatives.

## Deliverables, verification, and stop

Create:

- `docs/session_07_construct_validity_diagnostic_report.md`;
- `outputs/construct_validity_diagnostics/formal_closure.json`;
- `outputs/construct_validity_diagnostics/formal_closure_manifest.json`;
- one append-only research-log entry;
- a bounded update to `AGENTS.md` that records Session 7 as closed.

The closure JSON contains only aggregate findings, classifications, readiness
decisions, claim boundaries, and authority hashes. Its manifest binds the new
protocol, preserved diagnostic package, closure JSON, report, and environment.
Record the manifest's own SHA-256 in the report rather than recursively within
the manifest.

Before committing, verify starting and historical authorities, existing output
hashes, output schemas, publication safety, absence of protected identifiers and
private paths, Markdown links, compilation, focused Session 7 tests, the full
active software suite, changed-artifact scope, append-only history, staged
contents, and `git diff --check`. Report passed and skipped tests separately.

Commit this protocol before classification. Then create and validate the closure
package in one later result commit. Push and verify clean synchronized local,
tracking, and live remote heads. Recommend exactly one separately governed next
direction and stop. Do not begin a network, package, model, withheld, pose, or
Session 8 execution.
