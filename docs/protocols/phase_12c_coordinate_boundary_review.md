# Phase 12c — Coordinate boundary occurrence review

Prospective protocol, 2026-09-10. Start from clean synchronized
`ca17380b7f987d33d8841aa2493897e311f98436`; preserve v0.1.0 at
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa` and every historical authority.
Session 12b remains blocked/closed. Question: what explains its twelve canonical
carrier/candidate coordinate occurrences outside verified longitudinal bounds?
No model loading, target/outcome inspection, utilities, shares, progression,
policy implementation, empirical rendering, external xT or new payload acquisition.

## Stages and exact reproduction

Commit protocol, then tested isolated implementation before any new values.
Identify using canonical population SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`
and dimension-file SHA-256
`ded8721ad91f18b98c09488852481aaadfe3098d7b5f2039f0531e6e4f923c69`.
Verify closed 12b authority/QC/manifest hashes before parsing. Require 7,227
states and exact Session 8 per-match counts. Select only carrier/candidate points
satisfying abs(x')>L/2; reproduce per-match counts 2,1,3,2,1,1,2,0,0 in ascending
historical development alias order, exactly 12 in total. Otherwise stop.
Do not regenerate population. Freeze ignored row-ordinal/role/candidate-ordinal
occurrences and publish only neutral occurrence authority/counts/hashes. Commit
this authority before review. No deeper source values during identify.

## Permitted source and projection

Use only existing local metadata, event and tracking files for the seven affected
development matches at SkillCorner commit
`02a396ffd09b283c9f092fdedeff11da6d535b66`. Reject all other match/products before
opening. Verify commit/tree/path/Git blob and actual LFS pointer, then whole local
byte SHA/size before parsing. Bounded GitHub object metadata/pointer requests only;
no payload download, mixed twenty-match manifest, redirects or substituted pin.
Missing/unverified products stop that operation; never repair or download.
Reuse the established bounded transport-only policy for metadata requests.

Canonical fields: match/event recovery keys, role, candidate identity if needed,
offending x'/y'. Targets and defender coordinates are not projected. Dimensions:
existing mapping, length/width and identity receipts. Exact matching event fields:
event_id, period, time_end, player_id, player_in_possession_id only. Scan event IDs
to find unique exact records; never filter or rebuild pass attempts using outcome.
Metadata: home/away IDs, home_team_side; relevant carrier/offender player IDs,
team IDs and playing_time.by_period name/start_frame/end_frame. Reverify source
pitch_length/pitch_width against frozen mapping. Tracking index: frame, period,
timestamp only. Samples: offending player ID, x, y, is_detected only at authorized
frames. No ball, other coordinates, target, pass_outcome, score or model feature.
Unselected values may be traversed as syntax but not materialized into retained
application records, diagnostics or exceptions. Test sentinels. Details stay ignored.

## Recovery and diagnostics

Event lookup must be unique. Primary carrier ID wins, fallback only if empty;
conflicting populated references stop as unresolved provenance. Use frozen integer
microsecond clocks, latest strictly prior same-period frame, age <=100 ms. Verify
period/carrier-team direction, exact x'=s*x and y'=y, relevant active intervals,
source dimensions and mapping without guesses, scaling, default or tolerance.
Transformation/join defect selects C and stops further provider-value inspection.
A sign reflection preserves abs(x); sign alone cannot produce the violation.

For each selected player inspect decision frame and at most two immediately
adjacent same-period tracking frames on each side, <=200 ms each side. No window
extension, interpolation, carrying identities, velocity or other player/ball.
Detection: literal true detected, false extrapolated, null/missing unavailable;
malformed values stop. No extrapolation causality inferred from status alone.

Compute excess abs(x')-L/2; minimum/median/maximum, counts by role, boundary side,
pitch-length class, alias and detection, and excess by status. Use y/width only
for simultaneous lateral crossing. Do not infer role, technical-area behavior or
intent. Deduplicate player/frame keys, count repeats and overlapping windows.
Observed local runs require consecutive same-period index positions for the same
player outside the same boundary. Missing samples break observed runs. Mark runs
at window edges censored; never call these complete physical excursions.
No acceptable tolerance, clipping or policy is selected.

## Evidence and interpretation

Review only committed coordinate documents and pinned provider README. Distinguish
explicit semantics from inference; silence is unavailable, no expanded search.
Primary result: A legitimate support only with documented semantics plus behavior;
B extrapolation-related only with specific mechanism evidence beyond false flags;
C demonstrated project transformation/join defect; D affirmative isolated anomaly;
E multiple evidenced mechanisms; F unresolved otherwise. Mixed verified/unresolved
cases normally remain F. Remedy readiness: 1 bounded-policy design justified;
2 provider clarification required; 3 defect repair first; 4 not ready/reconsider.
Coordinate support and value-function domain need not coincide. Discuss future
policies conceptually only. No renewed Session 12b or changed claim ledger.

## Software, tests and closure

Separate runner: preflight, identify, review, publication-check. Hash/clean commit
gates, exclusive stage markers, append-only ignored access ledger; review requires
committed occurrence authority. Preserve partial failures without rerun/repair.
Public namespace outputs/coordinate_boundary_review: occurrence_authority.json,
occurrence_summary.json, match_summary.csv, qc.json, manifest.json. Only aggregate
records; local identities, occurrences, samples and timestamps remain ignored.
Strict schema/hash/cross-file/publication checks before closing outputs.

Test production selection, counts, excess, transformation, dimensions, timing,
carrier rules, projection, statuses, bounded neighbors, runs/censoring, source
integrity, symlinks, reserved rejection, no model routes, markers and failures.
Run focused/full active synthetic tests, compilation, links, hashes, append-only
preservation, staged review and git diff --check. Report passes/skips separately.
The real 12-count is checked only after commits; not a pre-protocol test.
Write docs/session_12c_coordinate_boundary_review.md covering all 24 handoff items,
including unavailable values. Append bounded research/library entries. Commit
closure, push/sync main, verify unchanged tag, stop. Recommend exactly one next
action from policy design, provider clarification, repair or reconsideration;
do not execute it. Accessibility PROXY ONLY; suppression NOT SUPPORTABLE.
