# Phase 14R9E — Empirical execution under reconciled publication authority

Frozen 2026-09-16 from clean synchronized
`fc7a4da62a64e597acbeccd790f825b6fd35e26c`. Preserve annotated `v0.1.0`
at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`, every historical Session
14 artifact and classification, the released API, dependencies, README, and
claim ledger.

## Question, population, and non-claims

This label-free development study asks whether the frozen expanding or
constant-width carrier-origin directional field adds complementary structural
description beyond isotropic proximity and whether one warrants separately
governed behavioral receiver-choice validation. The origin is a
**carrier-position proxy for ball origin**, not an observed ball location.
Accessibility remains **PROXY ONLY** and suppression **NOT SUPPORTABLE**.

The authorized population is the already-spent nine-match development geometry:
SHA-256 `cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`,
7,227 states, and match counts `885,801,952,877,861,629,764,734,724`. Targets,
chosen-receiver labels, ranks, outcomes, M0/M1/M2 utilities or shares, protected
or withheld detail, pose, xT, progression, sister-project data, provider
acquisition, and empirical visualization are forbidden.

## Inherited authority and prospective qualifications

R9 remains **D — BLOCKED / readiness 3** and R9a remains **C — checker /
representation abstraction mismatch / readiness 1**. R9E reuses, by exact
committed hashes, R9's retained-observation certificate gate, runner controls,
108/366/399 synthetic acceptance, stress evidence, visual approval, and
supporting manifest. It reuses R9a's nineteen-artifact publication representation.
It performs no new governed numerical acceptance and does not rerender the
visual. Historical numerical tests may execute as validation-only in local and
ordinary CI suites and must be reported as such.

Two gaps are repaired prospectively inside R9E. R9's retained certificate gate
was separate from its empirical maximum path, so R9E composes the existing
certificate verifier at the adaptive-result boundary. R9a reconciled the public
representation but did not replay the access journal or recompute lifecycle
hashes, so R9E validates persisted journal, snapshot, private evidence, and
derived counters before accepting publication. Historical files remain unchanged.

## Pre-access contract

The isolated runner exposes only `preflight`, `run`, and `publication-check`.
Only `run` may authorize geometry. It uses `uv run --locked python`, the existing
governed launcher, and normal installed-package imports. Startup, marker,
prerequisite, callback, and publication failures preserve an independent
exception and traceback even if normal replay fails.

Pre-access readiness is a derived conjunction requiring:

- exact hashes for the R9 protocol, runner, numerical modules, fixtures,
  certificate registry, seven inherited authority artifacts, manifest, and SVG;
- exact hashes for the R9a protocol, validator, reconciliation evidence, and
  manifest;
- unchanged package version, lockfile, public API, and implementation bindings;
- successful synthetic tests of runtime certificate matching, unmatched-warning
  blocking, journal replay, lifecycle derivation, failure closure, and complete
  publication; and
- absent fresh R9E attempt, preparation, and execution markers.

Any mismatch or substantive pre-access failure closes R9E blocked. There is no
manual override, repair, or governed retry. Documentation-only history does not
invalidate numerical authority unless a bound hash changes.

Runtime certificate requests derive their interval identity, endpoint bits,
field family, parameters, combination, integrand fingerprint, structural
authority, method, provenance, and tolerance from current execution. Retained
identifiers cannot be copied to manufacture a match. Exact registered matches
preserve the warning and adaptive estimate and certify only that contribution.
No certificate is generated automatically. Unmatched warnings block. A certified
contribution never waives a whole-integral agreement gate.

## Numerical and execution contract

Preserve isotropic Gaussian proximity, smooth expanding directionality, and
smooth constant-width directionality with `sigma=2 m`, onset `a=1 m`, expansion
half-angle `10 degrees`, float64 arithmetic, the inherited constant-width scale,
and the `1e-9 m` origin–defender exclusion. Stable bounded union is primary and
maximum is a comparator.

Production remains the joint Simpson vector at
`256,512,1024,2048,4096,8192,16384`, accepting the first finer vector only when
every governed component changes by at most `1e-7`. Preserve onset-aware owner
certification, canonical onsets and switches, certified tie endpoints,
degenerate-edge endpoints, micro-residual eligibility `width <= 1e-12/N`,
outward interval aggregation, and strict/repeat, onset-only, direct-Simpson, and
joint-maximum agreement gates. Historical split-Simpson checks remain limited to
the four synthetic fixtures.

After authorization, lexically discover membership and receiver ordinals while
skipping forbidden fields. Persist projection attempts and complete materialized
edge receipts before later work; unresolved receipt writes remain uncertain.
Freeze private prepared bytes and hashes before field evaluation. Execute in the
authoritative state → candidate → receiver order. Count each geometric edge once
and the three candidate calls separately. An edge completes only after all three
candidates and required checks; a state completes only after every edge and its
summaries are retained.

Any numerical or execution failure stops the entire run. Preserve active
match/state/edge/candidate, stage, warning and certificate status, counters,
exposure, traceback, and private partial work. Do not omit an edge, continue to a
later state, restart, or interpret incomplete science.

## Summaries and aggregation

Publish endpoint `receiver_field` and length-normalized
`segment_integrated_field` over `t in [0,1]` for every candidate under union and
maximum; union-minus-maximum with absolute `1e-12` equality; frozen Session 13
nearest distances, `d2-d1`/`d3-d1`, fractional top-1/2/3 involvement, support
overlap, nearest-role divergence, and multi-edge correspondence; all three
candidate-pair second-minus-first differences and ordering categories; and
endpoint-versus-segment ordering. Equal-minimum evidence remains synthetic.

Use Spearman only, exact-tie average ranks, and `1e-12` block-anchor ordering.
Constant or insufficient groups are unavailable. Use equal match → state → edge
weighting. Pair-derived continuous values reduce to state means and ordering
indicators to state proportions before extrema and inverse-ECDF percentiles.
Keep raw source counts distinct from published observation counts and report all
nine matches plus equal-match macro results and represented-match counts.

## Lifecycle and publication

Fresh attempt, preparation, and execution markers are create-once and grant no
access authority. The synchronized hash-chained journal is authoritative for
access attempts and successful materialization. `states_opened` is the count of
unique successful state-materialization receipts. `edges_opened` is the count of
unique materialized receiver-edge receipts. Lifecycle records govern evaluation
and completion; candidate-call records govern candidate work. These counters
cannot be supplied independently.

The public namespace is
`outputs/continuous_occlusion_empirical_retry_r9e/` and contains exactly R9's
nineteen filenames. `preaccess_contract.json` is the authority summary; there is
no `authority.json`, `lifecycle_summary.json`, or twentieth compatibility file.
Lifecycle, exposure, and candidate work live in `qc.json.progress_authority`.
Detailed journal, snapshot, preparation, identities, geometry, numerical traces,
and state-level records remain ignored and hash-bound.

R9a validates the nineteen-file representation. R9E additionally replays the
persisted journal, recomputes its chain and canonical snapshot hash, derives all
counters, validates private evidence, and compares the result with QC and
manifest. Success requires authorization, nonempty complete work, all discovered
states and edges completed, all three candidate calls per edge, zero failures,
zero unresolved receipts or edges, no active context, all scientific artifacts,
and a validator-derived receipt. Blocked/failure packages mark scientific files
unavailable. Publication checking validates existing bytes and never regenerates
missing evidence.

## Decisions and closure

Choose scientific A for one defensible directional candidate, B for multiple
plausible candidates, C for little additional description, or D for blocked or
invalid. Choose readiness independently: 1 behavioral validation of one frozen
field, 2 descriptor/visualization only, 3 representation revision, or 4 not
justified. Assess the ten inherited dimensions without weights; correlation,
guaranteed difference, or visual appeal alone cannot select a candidate.

Preserve exactly three commits: this protocol; tested R9E wiring and inherited
authority bindings; and closed outputs/report/log. Push the tested checkpoint
and require green Python 3.11, Python 3.13, and distribution CI before the one
empirical execution. Run focused, relevant, and full validation, compilation,
schemas/hashes, privacy/access checks, links, history preservation, staged
inspection, and diff checks. Leave README and claim ledger unchanged.

Recommend exactly one next action dictated by complete evidence: behavioral
validation beyond M1 for A/readiness 1, bounded candidate discrimination for B,
simpler geometry for C, or one bounded blocker diagnosis for D. Execute none.
