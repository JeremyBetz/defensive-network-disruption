# Phase 14R8 — Continuous occlusion empirical representation retry

**Frozen:** 2026-09-13

**Starting authority:** `e47ab1524206a5d42c3c6106b1955913b4435120`

**Scope:** label-free, development-only structural representation study

## Question and authority

Session 14R8 asks whether either frozen carrier-origin directional field adds
complementary structural description beyond isotropic defender proximity and
whether one warrants a later, separately governed behavioral validation. The
carrier coordinate is a proxy for ball origin; measured ball position and
colocation are unavailable.

The authority chain comprises Session 13 and the accepted numerical,
certification, canonicalization, launch, lifecycle, exposure, publication and
aggregation work through Session 14aj. Session 14aa's failed execution remains
historically qualified. Canonical structure remains distinct from raw solver
provenance. All stopped Session 14 empirical attempts remain unchanged.

R7 preserved equal-state means for pair-derived families but calculated their
extrema and inverse-ECDF percentiles over individual pair values. Session 14aj
established a standalone state-first aggregation helper. R8 must demonstrate
that helper's integration into collection, schema validation and publication;
the 14aj result alone is not end-to-end execution authority.

No target, outcome, fitted model, utility, option share, provider acquisition,
protected, formerly reserved or withheld detail, pose, xT, progression,
empirical image, behavioral validation or claim promotion is authorized.
Historical implementations and outputs, README, claim ledger, dependencies,
released API and `v0.1.0` remain unchanged.

## Field and numerical contract

The candidates remain isotropic Gaussian proximity, expanding directional and
constant-width directional fields with `sigma=2 m`, onset `a=1 m`, expansion
half-angle `10 degrees`, float64 arithmetic and the `1e-9 m` origin-defender
exclusion. Stable bounded union is primary and maximum is a comparator. Neither
is a probability, suppression measure, attribution or value.

Every nondegenerate edge uses the accepted onset-aware owner certification,
canonical onsets and switches, preserved certified tie endpoints and bounded
residual intervals. Exact onset/switch coincidence and the absence of an
interior representable witness fail closed. Degenerate edges retain inherited
endpoint behavior.

The joint Simpson ladder is `256,512,1024,2048,4096,8192,16384`; accept the
first finer vector whose every component changes by no more than `1e-7` and
fail at exhaustion. For `N` positive-width pieces, widths at most `1e-12/N`
retain rigorous residual intervals. Preserve the interval-aware strict/repeat,
onset-adaptive, direct-Simpson and joint-maximum gates. Historical split-Simpson
checks apply only to the four frozen synthetic failures.

Pre-access acceptance replays 108 cases, 366 references and 399 mapped
permutations. Canonical structure, component order and accepted resolution are
exact. Corresponding finite final components use only
`abs(a-b) <= 64*epsilon64*max(1,abs(a),abs(b))`; nonfinite values fail and
signed-zero bits remain diagnostic. Empirical edges receive no factorial
permutations or invented reference vectors.

## Launch, exposure and lifecycle

The isolated runner exposes `preflight`, `verify-production`,
`render-synthetic`, `run` and `publication-check`. Only `run` may authorize
development geometry. The sole governed form is `uv run --locked python`.
One top-level path verifies the environment and implementation, produces and
validates the fixed discoverable prerequisite record, verifies committed
synthetic and visual authority plus green Python 3.11, Python 3.13 and
distribution CI for the exact checkpoint, and then enters the guarded callback.

The exclusive attempt marker and synchronized hash-chained journal are created
before geometry can be opened. Lexical discovery obtains membership and
receiver ordinals without materializing coordinates or targets. A durable
projection attempt precedes the restricted projector; a durable receipt listing
every materialized edge precedes registration or evaluation. Unconfirmed
materialization remains uncertain exposure and cannot become confirmed zero.

One carrier-to-receiver connection is one geometric edge. Its three candidate
calculations are separate field calls. An edge completes after all candidates,
numerical checks and retained summaries succeed; a state completes after all
required edges and state summaries succeed. Persist active context and actual
opened, started and completed counts. Failure preserves partial work, exposure,
the original exception and private traceback and stops without repair, omission
or automatic rerun.

## Population and summaries

After all pre-access gates, verify canonical SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`,
7,227 states and alias-order counts `885,801,952,877,861,629,764,734,724`.
Skip targets before analytical records exist. Identity fields serve integrity
only and event references are discarded. Freeze private prepared bytes and
hashes before field evaluation. No clipping, exclusion, repair or refitting.

Retain endpoint and length-normalized segment-average fields for all candidates
and both combinations; union-minus-maximum and `1e-12` numerical equality;
Session 13 distance, gap, fractional top-1/2/3, nearest/support overlap and
multi-edge families; all candidate-pair differences, Spearman and five ordering
categories; endpoint-versus-segment ordering; receiver/corridor divergence; and
the inherited across-state overlap/redundancy associations.

Use Spearman only, exact-tie average ranks and inherited `1e-12` block-anchor
ordering. All-zero support is ownerless. Empty, constant or insufficient groups
remain unavailable with explicit denominators and reasons.

The frozen family registry governs collection, validation and publication:

- aligned candidate-field differences and pairwise support Jaccard reduce to
  one `state_pair_mean` per eligible state;
- ordering indicators reduce to one `state_pair_proportion` per eligible state;
- within-state Spearman remains `state`, and across-state Spearman remains
  `match`;
- non-pair edge summaries retain equal match-to-state-to-edge weighting.

Candidate differences pair candidate values at the same receiver; they do not
create another receiver-pair selection. Raw source-pair counts remain distinct
from published state observations. Match summaries weight assessable states
equally. Macro summaries weight represented matches equally. Pair-derived
extrema and inverse-ECDF 5/25/50/75/95 percentiles operate on state summaries.
Entirely unavailable categorical families retain their declared unit.

## Visual, evidence and stop rules

Before access, render twice a byte-identical 1800x800 SVG using the inherited
synthetic geometry, three equal-aspect panels and a shared `[0,1]` scale. It must
show `SYNTHETIC`, `CARRIER-ORIGIN GEOMETRIC OCCLUSION HYPOTHESIS`,
`NOT VALIDATED COVER SHADOW` and `NOT SUPPRESSION`. Full-frame visual QA is
required. No empirical visual is authorized.

Successful closure under `outputs/continuous_occlusion_retry_14r8/` contains
exactly fourteen public files: `candidate_summary.json`,
`structural_correspondence.csv`, `receiver_segment_divergence.csv`,
`candidate_pair_comparison.csv`, `union_vs_max.csv`, `multi_edge_summary.csv`,
`match_summary.csv`, `synthetic_stress_summary.json`, `lifecycle_summary.json`,
`exposure_summary.json`, `pair_aggregation_summary.json`, `qc.json`,
`manifest.json` and `synthetic_field_comparison.svg`. Pre-access authority is
separate; geometry, identities, journals, roots, traces and state records remain
ignored.

Success requires 7,227 completed states, all required edges and field calls,
complete summaries, strict schemas, finite values, consistent denominators,
cross-file lifecycle/exposure agreement and existing hash verification.
Publication checking never regenerates or rebinds evidence. Failure packages
mark incomplete scientific files unavailable and publish no fabricated rows.

Preserve three commits: this protocol; tested implementation with synthetic
acceptance and approved visual; closed evidence/report and append-only research
log. Green three-job CI is mandatory at the tested checkpoint before the single
empirical run and after closure. Any new core defect blocks R8 rather than
silently expanding its repair.

Scientific A requires complementary structure and a coherent, match-stable
advantage for one directional candidate; B retains multiple plausible
candidates; C retains simpler geometry; D is blocked or invalid. Readiness is
independent: 1 permits only a separately governed behavioral hypothesis test,
2 permits descriptive use, 3 requires discrimination or repair and 4 does not
justify continuation. Correlation, guaranteed information differences, visual
appeal or saturation alone cannot select a candidate. Accessibility remains
**PROXY ONLY** and suppression **NOT SUPPORTABLE**.

The final report maps all 50 requested handoff items and recommends exactly one
next action dictated by the observed classification. No recommendation is
executed in this session.
