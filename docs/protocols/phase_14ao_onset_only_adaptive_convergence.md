# Phase 14ao — Onset-only adaptive convergence evidence study

Date: 2026-09-14. Planning: ON; Turbo: OFF; Model: Astra Medium.
Starting authority: `72321255e5c31fc5946d5469717eeb5d17e30dce`.
Local/tracking/live heads were clean and synchronized before mutation. Preserve
`v0.1.0` at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

## Question and boundary

Collect only missing onset-only convergence evidence for zero-based state 4,
receiver 7, constant_width. No other geometry, targets, outcomes, models, shares,
scientific summaries, provider acquisition, switch detection, production or
piecewise integration, reference-eligibility recomputation, or journal benchmark.
Preserve all historical source, outputs, classifications, claims and released APIs.
No repair or Session 14R retry. 14an remains H/readiness 4/publication P1.

Commit this protocol before numerical execution or renewed geometry access;
commit tested tooling before the single governed diagnosis. Only preflight,
diagnose and publication-check are exposed, using `uv run --locked python`.

## Retained authority and selective access

Hash-bind 14am and 14an manifests and their private indexes before selective
parsing. Require 14an reference_eligibility eligible=true; do not recalculate it.
Retrieve the 14am piecewise record at intervals=16384 (estimate only), the
onset_adaptive estimate, and strict lower/upper bounds. Retrieve structure.onsets
only as type and first_post_branch, the serialized canonical property of
CertifiedOnset. If repeated structure records exist, their selected onset lists
must agree exactly. Never decode envelope, switches, witnesses or tie fields.
Retained raw solver provenance remains separate and unopened. No onset generation.

Require canonical population SHA-256
`cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`
and R8 prepared SHA-256
`15a809060f0ba4a8cb3790011dab8008db3dad6e931c287ccb395f61ef1530b0`.
Hash complete files without decoding; lexically select only row 4 receiver 7
using the historical single-edge projector. Verify selected canonical/prepared
serialization. Two product receipts represent one unique state/edge; attempts
and successful materialization are distinct. Failed receipts remain uncertain.

## Frozen numerical adapter

Use unchanged constant-width field/scalar float64 maximum evaluation, endpoints,
canonical-onset splits and math.fsum. Use SciPy quad(full_output=1) only to expose
terminal diagnostics; do not change the algorithm or add switch/tie boundaries.
One refinement axis: epsabs=epsrel at
`1e-13, 5e-14, 2e-14, 1e-14, 5e-15, 2e-15`; limit=1000 for every piece/level.
The first level is the historical anchor. No extra levels or altered partitions.

Treat returned termination messages as the same blocking condition as emitted
IntegrationWarning; retain them before stopping. All warnings, nonfinite values
and unexpected exceptions stop later levels. Never treat full_output warning
suppression as normal success. Keep estimates/errors/first-last initialized
alist/blist/rlist/elist values, callback count/order hash, neval and last private.
Final panels are not individually proven locally accurate. Subdivision operations
are sum(last-1) under this finite QAGS path, distinct from final panel count.
Rejected-panel and running global-estimate history are unavailable, not invented.

Onset localization uses only final panels touching an interior retained onset
exactly. Retain their count and reported-error fraction privately; no new spatial
neighborhood or field probes. Compare to the retained reference and separately
retained piecewise bounds. Those diagnostic comparisons do not rerun the original
production gate. Relative differences are available only when abs(reference) is
greater than 64*float64 epsilon. All exact estimates/differences remain private.

Anchor requirements: reference discrepancy exceeds 1e-10, retained piecewise
comparison fails its 1e-10 threshold, and the onset-only total differs from the
retained onset-only total by at most 1e-10. Otherwise stop H without later levels.

Each entire level (all onset pieces) has a 600-second maximum; total governed
execution including controls/persistence has 3600 seconds. A warning or timeout
preserves partial and completed records, marks later levels unavailable, stops,
and cannot alone establish numerical inaccuracy. Exclusive persistent marker;
no automatic retry, second edge, omitted level or post-access implementation fix.

## Prospective classifications

G=1e-10; I_i is a completed estimate; D_i=abs(I_i-I_(i-1)); E_i=abs(I_i-R).
All six levels must finish normally before A or B. Added work requires a changed
trace and an increase in evaluation or subdivision count.

A: added work demonstrates either both final successive D exceed G and both E
decrease, or the disagreeing anchor moves inside G with a successive change
exceeding G and a reduced reference discrepancy. Readiness 1 for this bounded
onset convergence target.

Qualified B: the last three estimates span at most G/10, all E exceed G, and both
final transitions have additional work and changed traces. B establishes only
stabilized disagreement under the tested refinement, not healthy accuracy or a
proved gate defect. Stable quadrature bias remains an alternative. As explicitly
selected by the user, B has readiness 4 and supports only contract review.

C requires an independently demonstrated stopping/orchestration defect, not a
long runtime or warning; readiness 2. D is invalidated retained authority,
readiness 3 with separate execution-invalid status. E requires multiple
independently demonstrated numerical issues; readiness follows the combination.
H/readiness 4 covers all insufficient, mixed, warning/timeout-limited evidence.
No classification from disagreement alone. Publication P1 is historical only.
Missing eligibility -> H/no access; contradictory or changed authority -> D.
Unexpected implementation/execution failure -> H and execution-invalid, preserved.

## Pre-access tests and persistence

Provider-free controls: f(t)=1, t**3 and abs(t-0.371), over [0,1] with a split
at 0.25 where needed. Compare diagnostic and historical plain quad call estimates,
reported errors and callback order exactly at identical settings. A kink with
limit=1 is a deliberate warning control. Freeze all controls before empirical
execution; no topology-derived fixture after access. Test scope guards, forbidden
sentinels, initialized-array slicing, counts, partial levels, deterministic bytes,
marker collision, timeouts, independent emergency writing, missing/changed evidence,
false classification acceptance and full small success/partial/invalid packages.
Fabricated classification records do not generate numerical evidence.

Use existing explicit primitive serializer and create-once synchronized writes.
Maintain a small hash-chained access/stage journal; never replay the original R8
journal. Preserve the original exception independently of replay and publication.
Validate persisted outputs, private evidence and existing hashes; no rebinding.

## Public schemas, outputs and closure

Exactly ten files under outputs/session14ao_onset_only_adaptive_convergence/:
retained_authority.json, refinement_protocol.json, refinement_results.csv,
subdivision_summary.csv, error_estimate_summary.csv, reference_comparison.csv,
timeout_summary.json, classification.json, qc.json, manifest.json.
Detailed stage records, traces, coordinates, exact estimates, errors and retained
onsets are ignored/private/hash-bound. CSVs have six fixed level rows and LF
endings; unavailable later rows are explicit. JSON has finite values and exact
schema_version=1 field sets, frozen in the tested implementation before access.
Results expose status, count, timing, comparison flags and evidence hashes only.
QC includes complete/partial/invalid status, execution validity, candidate,
levels attempted/completed, unique state/edge opens, exposure uncertainty,
classification/readiness and journal/private-index hashes. Manifest binds every
public file, inputs, protocol, implementation and environment. No success claim
from an incomplete package. Partial evidence is a valid stopped diagnosis.

Close evidence before report interpretation. Write docs/session_14ao_onset_only_adaptive_convergence.md
mapping the supplied 33 items and append one bounded research-log entry.
Exactly three commits: protocol; tested tooling; closed evidence/report/log.
Run focused/relevant/full tests, compilation, schema/hash/privacy/access checks,
links, historical-byte and staged checks, diff checks. Synthetic regression tests
are validation-only and not adjudication evidence. Push and require green ordinary
Python 3.11, Python 3.13 and distribution CI; verify synchronized heads/tag.

One subsequent recommendation only: A bounded convergence/stopping repair;
qualified B bounded agreement-contract review; C bounded runtime repair;
D/E/H one narrow authority/design/missing-evidence action. Execute none.

## Documentary/input bindings

- `AGENTS.md`: `269f22fa10845e267b4cd4dc02cca48ed94ea42db0fca594e7e67409122b0501`
- `docs/research_governance.md`: `c5c37b6e8f4dcf6799570c15fdb351dfeeb76847e2d74bc23371c1b0af8150a2`
- `docs/research_log.md`: `3e5c738610369e2748884d41787bd485a886dc3fb52b534fa7c83c643896c00d`
- `docs/session_14am_constant_width_comparator_diagnosis.md`: `f75659988ab5825e0983f2744674a48f72bf9bd852badde267ae1cc849c70ec3`
- `docs/session_14an_retained_evidence_adjudication.md`: `eae579e7a815ce57f340c079301d2478b7279763ace2b2305a4bfcf49400d5c8`
- `docs/protocols/phase_14am_constant_width_comparator_diagnosis.md`: `b3ceeb52bf6727ae4e2220b49427b744f4916adbdeecd34d7103ce7f86dfcfdd`
- `docs/protocols/phase_14an_retained_evidence_adjudication.md`: `ee2cc896051b9af072f678919869dbeefce4730dee662220aecf5eeb0dbabec6`
- `outputs/session14am_constant_width_comparator_diagnosis/manifest.json`: `235585718c4fe6a1eb2c48ac655d95a84f92fe7b342d2e486abfb3542425193e`
- `outputs/session14an_retained_evidence_adjudication/manifest.json`: `7abbca8d205fb09ac67db3bb40e008241217cce75b8b20a054f99b1961b40459`
- `outputs/session14an_retained_evidence_adjudication/reference_eligibility.json`: `1856ff417b6d725daa64cc8647d63b3da48a133add2e4bdcc0f7096cc207ac80`
- `src/defensive_network_disruption/geometry/production_verification.py`: `6d4eeddf0c7537ecbea027e1c308e4f573b2cf0f4bb71a0b3b839e2382f75a6c`
- `src/defensive_network_disruption/geometry/representation_retry.py`: `96dbdfe6030582e69f837a1343fb1e68952ed34992c957f243507bdc5b2c2f99`
- `src/defensive_network_disruption/geometry/diagnostic_serialization.py`: `e1627687cc2af03785c373b78d30c09ae12e4d24473e0a2df8e26c7f80020557`
- `src/defensive_network_disruption/validation/empirical_switch_diagnosis.py`: `55bc652c3e0980424e2c58fcad5ce7e6c8eb76fc3e164274485186dd11338b98`
- `src/defensive_network_disruption/geometry/root_partition_determinism.py`: `8709706de798277c172d5f4a874856fad1ebb4321caf1f004be9e24ba622f0af`
- `uv.lock`: `c9e4da0781770b9e72b8a5b10a7c514f2259b7e63dc1b67604ee481934db0a0c`
