# Session 14am — Fresh single-edge constant-width comparator diagnosis

Date: 2026-09-14. **Numerical H — UNRESOLVED / readiness 4; publication P1.**
Execution was valid but incomplete under the frozen operation budget. The exact
R8 failure was reproduced. The historical-size journal replay subsequently
reached its 600-second limit, and the runner preserved the evidence and stopped.
The 3,600-second global limit was not reached. No repair, second edge, second
empirical execution, or scientific-summary inspection followed.

## What the closed evidence establishes

The observer entered R8's unchanged `evaluate_edge()` / `evaluate()` route,
including onset-aware certification and the actual `piecewise_unsplit` gate.
It captured the gate inputs and original `GateFailure` before supplementary
work. This is an actual-path reproduction, not a similar disagreement obtained
through the obsolete 14ak entrypoint.

The partition audit passed: eight canonical onsets, one certified switch, no
tie intervals, ten positive-width pieces, and no bounded-residual pieces. Domain
coverage, total width, retained structural boundaries, and certification
witness semantics passed the implemented checks. These checks establish the
observed structural record; they do not prove globally complete finite-grid
switch detection or owner constancy at every real point inside each piece.

Joint Simpson converged at 2,048 intervals. Its result remains **provisional**
because full verification failed. The 16,384 structural-piece Simpson result
met both prospectively frozen reference conditions: the partition passed and
the 8,192-to-16,384 change was at most `1e-10`. Agreement with other methods was
not used to determine that eligibility. Strict/repeat agreement, the inherited
direct-Simpson gate, the joint-maximum gate, and the production/piecewise
comparisons with this internal reference passed. The onset-only adaptive result
did not agree with the eligible reference within `1e-10`.

These are useful completed numerical findings, preserved in
[production-path evidence](../outputs/session14am_constant_width_comparator_diagnosis/production_path.json)
and [gate evidence](../outputs/session14am_constant_width_comparator_diagnosis/agreement_gate_replay.json).
They do not override the frozen runner's overall **H** closure: its timeout
handler records an incomplete diagnosis as H/readiness 4 even when preceding
numerical checks completed. No final A classification or retry authority is
claimed. A disagreement alone does not identify exactly which omitted feature
caused the onset-only adaptive estimate to miss the reference.

The historical "unsplit" comparator already partitions at canonical onsets.
It omits the additional switch/tie boundaries. Strict and repeat piecewise
quadrature use `1e-13` and `1e-11`; the onset-only comparator uses `1e-13`, all
with `limit=1000` and `math.fsum` accumulation. Adaptive subdivision is separate
from the Simpson ladder. The 65,536 direct grid was used only for its inherited
verification gate, never as the diagnostic reference or production estimator.
No tolerance, residual allocation, field formula, or production calculation
changed.

## Publication cost and timing

Seven valid synthetic preparation journals, each with ten edges per state,
completed at 16 through 1,024 states. The actual replay processed every lifecycle
prefix. For 66, 130, 258, 514, 1,026, 2,050, and 4,098 records, the measured
cumulative prefix visits were respectively 2,211; 8,515; 33,411; 132,355; 526,851;
2,102,275; and 8,398,851. Each equals `n(n+1)/2`. This establishes triangular
record reconstruction work for this path. It is not a fitted timing curve or a
claim that every internal operation has precisely quadratic total cost.

The preserved R8 journal passed its full hash-chain read: 30,882 records and
6,533,975 bytes. Its provider-free event-shape replay timed out after 600.005
seconds, during prefix 6,118, with 18,718,021 submitted prefix-record visits.
The last prefix was interrupted: that count is submitted work, not proof that
every record in the last call finished. Final acceptance/rejection of the
complete historical shape remains unavailable. No runtime was extrapolated
beyond the tested range. Publication classification **P1** is supported by the
completed operation-count controls; the full-size completion requirement was
not met.

The instrumented in-memory replay performs repeated reconstruction, without
file reads, JSON parsing, or chain hashing inside that measured routine. Those
zero fields do not describe the surrounding file-authority and publication
wrappers. The diagnostic separately read and authenticated the complete
historical journal. Historical source also shows duplicate failure handling;
its original elapsed cost remains unavailable. The retained original exception,
traceback, and publication exception remain bound in private evidence. Emergency
preservation is distinct from successful normal R8 publication.

Governed wall time through evidence collection was 878.334 seconds: 873.728
seconds of process CPU excluding measured evidence-persistence CPU, 0.040 seconds
of measured persistence wall time, and a 4.566-second unattributed remainder.
Waiting was not separately instrumented and is not silently labelled numerical
compute. Nested profiler timers are retained separately and are not added to
this reconciliation. Concurrent synthetic tests may affect waiting; these are
diagnostic timings, not a performance benchmark. Final package-writing time is
outside that collection timer.

Within the actual numerical route, nonoverlapping recorded stage durations
were approximately 0.104 seconds for joint Simpson, 42.262 seconds for owner
certification (including repeated canonical construction), 0.018 seconds for
strict piecewise, 0.018 seconds for repeat piecewise, and 0.017 seconds for
onset-only adaptive work. Nested adaptive/certification timings are inclusive
and must not be summed with these stage durations. The historical roughly
55-minute delay cannot be assigned to quadrature from these newly measured
values.

Streaming validation, authenticated incremental summaries, checkpoints with
suffix replay, and indexed validation remain conceptual options. Any later
proposal must retain the complete append-only authority, exact exposure,
active failure context, and fail-closed validation. None was implemented here.

## Evidence and qualifications

The [manifest](../outputs/session14am_constant_width_comparator_diagnosis/manifest.json)
binds all 18 public files, the protocol, implementation, environment, historical
source authority, and private evidence index. Its SHA-256 is
`235585718c4fe6a1eb2c48ac655d95a84f92fe7b342d2e486abfb3542425193e`.
The private index SHA-256 is
`a91f6095c24c1198e323e26e338a2b6a4199288862d0dc1743509cf432dde81a`.
Exact geometry, field totals, contributions, roots, bits, owners, and neighborhoods
remain private. Public convergence CSVs are availability/fingerprint records;
they are not released empirical convergence values.

Private reproduction/timing evidence is bound by
`037ac87bd9ac0143ac377e6e7f16d590479231230d591b37c9132abaadfc4cc6`;
partial timeout evidence by
`f5f20c298711459b2f3c071b2c15d6220b12201047f5bd82041b7db744d85978`;
and preserved original/emergency evidence by
`63b942208f89685f5dec53338faf39ee790b18af480b1de15f90bcbb2692fba1`.

R8 remains D/readiness 3. Session 14ak remains invalid without usable numerical
interpretation; its transient values were not used. Its correction to R8's
protocol commit (`e96f6968d52986db1f45cead2c5adc55289f080c`) and the prepared-file
hash ending `...1530b0` is retained. Historical estimates, subdivisions, and
component timings remain unavailable. The observer retained new adaptive return
estimates/error reports, but not QUADPACK subdivision arrays; no extra adaptive
run was introduced to recover them.

All three onset-neighbor samples for each canonical onset passed the inherited
independent scalar-oracle comparison. Exact samples and finite-step slopes are
private. Analytical continuity follows the frozen formulas and finite maximum;
finite slopes and tolerance-based owner sets are not independent continuity
proofs or exact-equality claims. No topology-derived synthetic fixture was
executed or tuned after exposure. The timeout left the later public
`synthetic_reproduction`, publication-options, and emergency-check records
explicitly unavailable; the original/emergency facts above come from the
already hash-bound retained record, not replacement output files.

## Forty-two-item handoff

1. Starting HEAD: `94092788770b32638ea4dc748c70e2a593436ed1`; clean local/tracking/live agreement verified before mutation.
2. Ending HEAD: the closure commit containing this report; exact hash and live synchronization are supplied in the final delivery receipt.
3. Protocol: [Phase 14am](protocols/phase_14am_constant_width_comparator_diagnosis.md), commit `2aff0c336e6828e30beb7bf697fce9325c035ffc`, SHA-256 `b3ceeb52bf6727ae4e2220b49427b744f4916adbdeecd34d7103ce7f86dfcfdd`. Tested implementation: `3ff864237b409205f5affad6de7a44b87a7fbc73`.
4. Serializer preflight: passed; deterministic complete synthetic payload SHA-256 `39906f9b0fb212ef003b862d02bc799f8a8bda5021a7c8fdbe78a260bd23f673`; create-once persistence and hash closure passed.
5. Retained authority: only zero-based state 4, receiver 7, `constant_width`; selected canonical/prepared serialization matched. Two source receipts represent one unique state and edge, with no uncertain exposure.
6. Exact failure: unchanged R8 path reproduced `GateFailure: piecewise_unsplit` with actual gate inputs captured before supplementary calculations.
7. Gates: strict/repeat and onset-only `1e-10`, direct `1e-9`, joint maximum `1e-6`; no altered settings or residual allocation.
8. Topology: eight onsets, one certified switch, no tie intervals. It is not an onset-only structural envelope.
9. Pieces: ten positive-width pieces, zero bounded-residual pieces.
10. Partition audit: all implemented checks passed, with finite-grid and per-piece-owner completeness limits stated above.
11. Production estimate: private and provisional; no fully accepted empirical field value is claimed.
12. Production resolution: 2,048 intervals, unchanged joint-vector stopping rule.
13. Piecewise curve: all seven nominal ladder levels recorded privately, with actual evaluations, contributions, and successive differences; final convergence criterion passed.
14. Uniform curve: seven supplementary levels recorded privately. The onset-only adaptive comparator is separate and has no Simpson-ladder stopping resolution.
15. Internal reference: 16,384 structural-piece estimate eligible independently of agreement with other paths; not mathematical truth.
16. Production/reference difference: private; inherited `1e-6` comparison passed.
17. Piecewise/reference difference: private; interval-aware `1e-10` comparison passed.
18. Onset-only/reference difference: private; `1e-10` comparison failed. This is supplementary to the actual gate failure.
19. Continuity: frozen analytical authority retained and adjacent-float scalar-oracle checks passed; no field discontinuity established.
20. Smoothness: finite-step slopes retained privately; no threshold, dense sweep, or causal explanation inferred from them.
21. Piece contributions: private, hash-bound across seven structural-piece levels; none omitted or treated as a new production estimator.
22. Gate replay: actual condition was false on captured strict interval and onset-only estimate; exact route and exception matched.
23. Stopping diagnosis: Simpson and adaptive stopping remain distinct. Missing adaptive subdivisions prevent a complete account of adaptive error estimation; no new adaptive settings selected.
24. Runtime: 878.334 seconds overall collection; historical-shape replay hit its 600-second operation cap. CPU, persistence, inclusive timers, and remainder are distinguished above.
25. New synthetic reproduction: none executed or tuned after exposure; later fixture documentation stage unavailable after timeout.
26. Numerical classification: **H — UNRESOLVED**, preserving the timeout closure and the separately recorded completed numerical findings.
27. Readiness: **4 — MORE EVIDENCE REQUIRED**; no empirical retry authority.
28. Historical journal: 30,882 records / 6,533,975 bytes; complete chain verified before replay profiling.
29. Complexity: exact triangular prefix visits observed on seven valid synthetic controls; full historical-shape replay incomplete.
30. Bottleneck: repeated lifecycle reconstruction is demonstrated. Original 55-minute attribution and complete historical-size elapsed time remain unavailable.
31. Emergency integrity: original gate, traceback, and publication exception retained and hash-bound. Normal R8 failure publication is not retroactively validated.
32. Publication: **P1 — LARGE-JOURNAL REPLAY IMPLEMENTATION DEFECT; BOUNDED REPAIR READY**, based on operation-count evidence, separately from numerical H/readiness 4.
33. Future publication shape: conceptual one-pass authenticated validation or checkpoints with suffix replay; no architecture selected or implemented as a repair here.
34. Scope: one unique retained state/edge/candidate; no additional geometry accessed.
35. R8 completed-state scientific values and partial scientific summaries: not inspected.
36. Targets, models, shares, outcomes, protected/withheld material, and provider acquisition: not accessed.
37. Tests: focused/relevant 55 run / 55 passed / 0 skipped; focused serializer/diagnostic recheck 24/24/0. Full-suite totals appear in validation below.
38. Python 3.11 CI: required on the closure commit; final delivery receipt records the observed result.
39. Python 3.13 CI: required on the closure commit; final delivery receipt records the observed result.
40. Distribution CI: required on the closure commit; final delivery receipt records the observed result.
41. Three commits: protocol, tested tooling, closed evidence/report/log. Push and exact synchronization/release results appear in the final delivery receipt.
42. Recommendation: **separately govern an evidence-only review of the retained 14am numerical checks and timed-out journal replay, without reopening geometry or rerunning integration.**

## Validation and preservation

The full committed-code suite passed: **768 run / 765 passed / 3 retained
skips**, in 1,354.041 seconds. An earlier full-suite process that
loaded pre-final-edit code was stopped and superseded by the committed-code
run; it is not counted as a passing full suite. The one governed diagnostic
was not restarted.

Compilation and the existing-output publication checker passed. All 745 files
present at the starting commit remained byte-identical before the required
append-only research-log entry. All 89 bound implementation/authority hashes, seven local links, and the
18-file public inventory passed validation. Privacy checks found no private
paths, credentials, identities, geometry records, or tracebacks in the new
public package. The historical research-log prefix is unchanged. Staged-file
inspection and diff checks passed: exactly the 18 public artifacts, this report,
and one append-only log entry are in the closure commit. No implementation was repaired after
access, and the evidence package was not rebound.

The claim ledger, README, release, dependencies, APIs, field mathematics and
historical outputs remain unchanged. Accessibility remains **PROXY ONLY** and
suppression **NOT SUPPORTABLE**. This numerical/tooling diagnosis makes no
cover-shadow, causal, attribution, value, or behavioral claim.
