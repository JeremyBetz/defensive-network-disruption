# Session 14R3 — Label-free continuous occlusion representation retry

## Decision and exposure boundary

**D — BLOCKED; readiness 3 — failure-accounting and publication checks require
bounded diagnosis.** No development population was opened, hashed or parsed.
Zero empirical states and zero empirical edges were prepared or evaluated.
The empirical `run` command was never invoked. No candidate was selected.

R3 successfully established a new governed synthetic acceptance and approved
visual. A subsequent pre-access review demonstrated a defect in the new
wrapper's preparation-failure accounting: after one synthetic row is prepared,
the expected population-count rejection passes `n=1` to a publisher parameter
named `states_completed`. QC consequently says one state completed, while its
own access record correctly says zero completed states and zero evaluated edges.
The publication validator accepts this disagreement because it checks the
fields separately but does not enforce their equality.

This is an R3 orchestration/evidence defect, not an observed defect in the
historical launcher, field formulas or numerical integration. The tested
checkpoint is preserved without an in-place repair or empirical launch. A green
software suite cannot override this demonstrated missing invariant.

The closed [manifest](../outputs/continuous_occlusion_retry_14r3/manifest.json)
SHA-256 is `860e7a8a066795bfd3fda94d9e3adb4b496ed950c73ef1711ae3cea729efd03b`.
It binds the successful synthetic authority, approved visual, supplemental
synthetic failure observation and zero-access blocked QC. It does not represent
the contradictory synthetic test QC as an actual empirical completion count.

## Thirty-six-item handoff

1. **Starting authority:** clean local HEAD, tracking main and live GitHub main
   matched `8da64387fe62d5bd15f0a347456252e931988945` before mutation.
2. **Release:** `v0.1.0` remains at
   `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`; no release operation occurred.
3. **Protocol:** [Phase 14R3](protocols/phase_14r3_continuous_occlusion_empirical_retry.md),
   commit `2539e15`, preceded numerical execution and all new implementation.
4. **Historical authority:** starting tracked files are preserved, apart from
   the authorized research-log append. Session 14aa retains its failure
   qualification; 14ab distinguishes canonical structure from raw solver
   provenance. Session 13 gap percentiles remain state-average-gap distributions.
5. **14ad qualification:** some historical published guard flags were fixed
   values and its audit runner lacked unexpected-failure closure. New callback
   spies and outer R3 closure were implemented without changing 14ad. The
   Phase 14f protocol/report discrepancy remains preserved, not harmonized.
6. **Question:** complementary directional structure beyond isotropic proximity
   remains the question. No empirical answer or later candidate specification
   was earned.
7. **Command surface:** preflight, verify-production, render-synthetic, run,
   publication-check. Only run offers an empirical route; no preparation or
   analysis CLI was exposed. Internal helpers require launch authorization.
8. **Environment:** governed commands used `uv run --locked python`; the new
   wrapper checks the project environment and binds its package fingerprint,
   protocol, implementation and unchanged lockfile. The historical launcher
   itself remains unchanged.
9. **Prerequisite production:** the same interpreter ran the fixed discoverable
   R3 enforcement suite. Six tests passed, with nonzero collection required.
   Its inherited producer identifier is retained for validator compatibility;
   the R3 command and implementation hashes distinguish this authority.
10. **Observed launch guards:** tests exercised startup, environment, producer,
    missing/empty/digest records, warning, downstream/readiness failure,
    context token/environment/digest, access-marker and launch-marker collisions,
    and access-callback failure. Prerequisite failures had zero callback spies;
    downstream failures prevented access; callback failure retained its actual
    invocation. Direct entry and repeat attempts were rejected in the tested
    paths. These findings are bounded by the new accounting defect below.
11. **Synthetic acceptance:** the new namespace's governed assembled path passed
    **108 cases, 366 references and 399 mapped permutations**, independently of
    historical PASS flags. The closed
    [production authority](../outputs/continuous_occlusion_retry_14r3/authority/production.json)
    records the observations and derived readiness.
12. **Numerical preservation:** the three frozen fields, sigma 2 m, onset 1 m,
    angle 10 degrees, 1e-9 origin exclusion and joint Simpson ladder 256–16384
    remain unchanged. Every-component convergence remains 1e-7.
13. **Structural and float contracts:** canonical structure, component order
    and accepted resolution stay exact. Finite final historical components use
    the unchanged 64-epsilon rule. Raw solver provenance is not substituted for
    canonical authority; signed-zero bits remain diagnostic.
14. **Independent maximum:** canonical partitions and certified endpoints reach
    the unchanged interval-aware verifier. Micro pieces retain residual
    intervals at width <= 1e-12/N. Strict/repeat, onset, direct and joint gates
    remain unchanged; the four historical split-Simpson checks remain synthetic.
15. **Synthetic versus empirical evidence:** all 366 reference comparisons and
    399 permutations are synthetic acceptance evidence. No empirical reference
    vector, permutation sweep or field evaluation occurred.
16. **Visual:** one new 1800x800
    [synthetic SVG](../outputs/continuous_occlusion_retry_14r3/synthetic_field_comparison.svg)
    was rendered twice with identical bytes. Full-frame inspection confirmed
    equal geometry, readable labels and shared scale, and no clipping. All four
    warning labels are visible. No historical visual was changed.
17. **Tested checkpoint:** `820bf404075bcf0aae1ec2328196b258de2ebd3e` contains
    implementation, tests, governed synthetic acceptance and visual approval.
    It was pushed for mandatory pre-access CI.
18. **Pre-access CI:** [run 34702848962](https://github.com/JeremyBetz/defensive-network-disruption/actions/runs/34702848962)
    is the tested-checkpoint workflow. Staged-review job results are
    recorded below; final completion is delivery evidence. CI did not authorize proceeding past the separately
    demonstrated accounting defect.
19. **Population:** the prescribed hash, 7,227-state count and nine match counts
    remained specifications; they were not reverified against real data in R3.
    No canonical or prepared empirical line was inspected.
20. **Projection:** synthetic sentinel tests exercised target skipping before
    records, geometry-only schema, unauthorized membership and path rejection.
    No target, event reference, model, share or outcome entered an R3 empirical
    record because no empirical records were created.
21. **Preparation authority:** no real prepared-population artifact exists.
    The one-row failure fixture was created in temporary storage, with a mocked
    projector returning fixed synthetic geometry and mocked hash checks.
22. **Actual access/completion:** zero real population opens, prepared states,
    completed states, started edges and completed edges. The published QC stage
    `run` names the withheld launch gate, not an executed empirical run. Closure
    used the existing failure publisher directly after the pre-access review.
23. **Observed blocker:** the supplemental
    [failure observation](../outputs/continuous_occlusion_retry_14r3/authority/preaccess_failure.json)
    records one synthetic prepared row, zero actual completed rows, and an
    accepted contradictory QC completion count of one. Both preparation failure
    publication and cross-field validation are affected.
24. **Endpoint/segment comparison:** empirical endpoint fields, normalized
    segment averages and their ordering comparison are unavailable.
25. **Union/maximum:** empirical distributions, numerical-equality frequencies
    and overlap accumulation are unavailable.
26. **Session 13 correspondences:** no new nearest-distance, gap, fractional
    top-k, support-overlap or multi-edge empirical finding. Historical Session
    13 results remain unchanged.
27. **Candidate pairs:** all three signed-difference, Spearman and five-category
    ordering comparisons remain empirically unavailable; no candidate wins.
28. **Receiver/corridor and across-state associations:** synthetic implementation
    checks only. Empirical opposing-pair correspondence and overlap/redundancy
    associations remain unavailable.
29. **Aggregation:** equal match/state/edge weighting, eligible-pair averaging,
    weighted inverse-ECDF summaries and unavailable groups were tested
    synthetically. All-nine-match consistency was not evaluated.
30. **Publication:** scientific result files are explicitly unavailable in the
    blocked manifest. No partial empirical comparison was published. Existing
    generated bytes were hash-closed, not manually edited or rebound.
31. **Scientific classification:** D means execution was blocked. It is not a
    negative finding about directional fields, continuity or cover shadows.
32. **Readiness and ten dimensions:** readiness 3 identifies an evidence-control
    blocker. The ten-dimension assessment below makes no numerical winner score
    or post-result threshold.
33. **Non-claims:** accessibility remains PROXY ONLY; suppression remains NOT
    SUPPORTABLE. No cover-shadow validity, causal attribution, defender value,
    pass success, tactical intent or practitioner usefulness is established.
34. **Validation:** focused, historical and full tests, compilation, local links,
    schema/hash publication, privacy, staged diff and history checks are
    reported below. Passing tests do not erase the supplemental failing
    consistency obligation. Counts overlap and are not independent evidence.
35. **Delivery:** three commits: protocol; tested implementation/acceptance;
    blocked closure/report and append-only log. The final commit, CI and
    synchronized ending HEAD are recorded in the delivery handoff to avoid a
    self-referential hash in this file.
36. **Exactly one next action:** separately govern a bounded diagnosis of R3's
    preparation-failure accounting and cross-field publication validation before
    another empirical launch. This recommendation is not executed.

## Interpretation and remaining limits

Interpretability, smoothness, carrier-relative behavior and composability have
the inherited formal definitions and synthetic checks, not new empirical support.
Multi-edge behavior and Session 13 correspondence are unavailable in R3.
Equal-minimum discrimination was exercised only in the frozen synthetic stress
cases. Distinction from isotropic is illustrated synthetically but cannot select
a candidate. Software simplicity is qualified by the new bookkeeping defect.
Visual usefulness passed engineering readability review; practitioner usefulness
remains untested. These are the ten planned dimensions, without weights or a
composite score.

Carrier positions remain uncertain proxies for ball origin. Shared-origin
geometry, fixed scales, persistent directional tails, union saturation and
multiplicity, finite-grid switching coverage, offline extrapolated tracking and
spent formerly reserved matches remain limitations. No protected, withheld,
pose, provider product, historical partial scientific summary, xT or progression
record was accessed. Claims, dependencies, released APIs and README are unchanged.

## Validation qualifications and prospective regression need

The first focused run reported 22 tests with one error: the new stage logger's
temporary directory did not exist in a synthetic summary test. Directory
creation and test isolation were corrected before governed acceptance. The
current focused suite then passed 27/27. The initial full run passed 552 of 555,
with three retained scaffold skips; it preceded one additional wrapper test.
The separate committed check record discloses that timing.

The later accounting probe initially omitted the directory normally created by
the launcher, so it stopped with FileNotFoundError before preparing a row. That
setup observation is retained privately and is not the defect evidence. With
the same synthetic fixture's launch directory present, the expected count gate
failed after one row, revealing the QC contradiction. Neither probe evaluated a
field or opened the canonical population. They were supplemental synthetic
tests, not empirical runs or repeats of governed numerical acceptance.

No corrective implementation change followed that observation. A future bounded
diagnosis should preserve this exact regression: one or more rows prepared,
population-count failure, zero numerical callbacks, and zero completed states.
It should require QC completion to equal the actual completion counter, separate
prepared counts, and reject contradictory count records. It must also test
failure publication after partial preparation and partial analysis, without
treating a passing suite as proof of untested invariants. No tolerance, field,
launcher primitive or scientific amendment is proposed here.

## Final verification record

Post-closure validation completed with **556 run, 553 passed, three retained
skips** in the full active suite. The focused R3 suite completed **27 run,
27 passed, zero skipped**; the relevant Session 14 suite completed **267 run,
267 passed, zero skipped**. These suites overlap. All commands exited zero.
They do not include the supplemental accounting observation as a passing
consistency test: that obligation demonstrably failed and is the reason for D.
The final full-suite log SHA-256 is
`fd155cba7aee36691013e96579c8f984f86f5da86f58df9933b0db31a4b03bdc`;
the complete log remains in ignored validation storage.

Compilation, local Markdown links, changed-artifact privacy review, historical
byte preservation, append-only log verification, staged inspection and diff
checks passed. The existing publication checker verified hashes and the blocked
schema. An additional direct check verified the actual closed QC counters agree
at zero, rather than relying on the known incomplete cross-field validator.
Seven scientific result files are explicitly unavailable. No generated evidence
was rebound after closure; implementation bytes match the tested checkpoint.

At report staging, tested-checkpoint CI had successful distribution and Python
3.11 jobs, with Python 3.13 still running. The final delivery verifies and reports
completion of that workflow and the separate closure-commit workflow. Remote CI
success does not retroactively authorize empirical access or remove the blocker.
The closed scientific package remains unchanged throughout delivery.
