# Session 14R — Continuous occlusion representation study

## Decision and 31-item evidence handoff

1. **D — BLOCKED.** The single empirical execution stopped on a numerical
   warning during independent maximum verification. The complete nine-match
   representation comparison is unavailable. This is an execution boundary,
   not evidence against either directional field and not a candidate selection.

2. **Starting authority.** Local, tracking and live `main` matched
   `3f58ede93143fda336e3e6515e15b0cc8c1a6fa9` before work. Annotated `v0.1.0`
   remains at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.

3. **Preservation.** Historical protocols, reports, outputs, field and
   verification implementations, Session 13 evidence, released exports,
   dependencies, lockfile, README, claim ledger and competition-authorship
   policy remain unchanged. The research log receives only the bounded entry
   for this closure. Session 13's published gap percentiles remain distributions
   of **state-average gaps**; they were not reinterpreted as edge-level quantiles.

4. **Prospective protocol.** The [frozen protocol](protocols/phase_14r_continuous_occlusion_retry.md)
   was committed as `fd05f5c714a5b3853b7e61c9e75309685ef47ed7` before new
   numerical execution or development access. Its SHA-256 is
   `bdf032e2f84563c5f1799b88aa28ceca867fb9fc8b02edd2cc25e244e5e1f698`.
   It fixes the three candidates, all summaries, numerical rules, synthetic
   visual, failure closure and interpretation dimensions.

5. **Tested implementation.** Commit
   `70681013b5f64be59819dd4e7c169b45cc12262c` contains the new
   [runner](../scripts/session_14r_occlusion_study.py),
   [geometry adapter](../src/defensive_network_disruption/geometry/representation_study.py),
   [selective projection](../src/defensive_network_disruption/data/representation_projection.py),
   [tests](../tests/test_session14r_study.py), synthetic acceptance and visual.
   No released API or historical numerical function was modified. The empirical
   adapter uses no OptionNetwork, fitted model or option shares.

6. **Synthetic acceptance.** The new namespace completed **108 cases, 366
   component references and 399 mapped permutation comparisons**. All six
   engineering checks passed. The valid pipeline control succeeded and all
   eleven negative injections blocked acceptance. Derived readiness passed.
   Maximum reference error was `4.1576548232002963e-08`. The old Session 14i
   marker was never reopened. Evidence is in
   [production authority](../outputs/continuous_occlusion_retry/authority/production.json).

7. **Numerical and environment authority.** The unchanged joint Simpson ladder
   is 256 through 16,384 intervals; the first finer vector requires every
   component change at most `1e-7`. The synthetic reference bound is `1e-6`.
   Empirical maximum verification consumes certified partitions and retains
   strict/repeated piecewise, onset-only adaptive and direct-grid checks. No
   historical empirical individual/union references or factorial empirical
   permutations were invented. Locked versions were Python 3.13.15, NumPy
   2.5.3, SciPy 1.18.1 and Matplotlib 3.11.1; the manifest binds the unchanged lock.

8. **Synthetic visual and pre-access corrections.** The
   [1800 × 800 SVG](../outputs/continuous_occlusion_retry/synthetic_field_comparison.svg)
   uses the frozen geometry, equal aspect and shared union-field scale. Each
   rendered version was generated twice with identical bytes. Full-frame QA
   corrected overlapping anonymous labels before access; a final renderer-only
   whitespace normalization satisfied the staged diff check. Earlier renders
   remain private. The production run preceded these permitted presentation
   fixes: [revision authority](../outputs/continuous_occlusion_retry/authority/visual_revision.json)
   binds the unchanged nonvisual runner syntax and historical runner bytes,
   while [visual approval](../outputs/continuous_occlusion_retry/authority/visual_approval.json)
   binds the final complete implementation. No numerical acceptance rerun or
   post-access code change occurred. Final SVG SHA-256:
   `dee4ec07bc364662bb4587049f63b5d0406a175dfe62793a40f8b1301154fbe6`.

9. **Population preparation.** All **7,227** canonical states passed structural
   preparation; historical alias-order counts were
   `885, 801, 952, 877, 861, 629, 764, 734, 724`. The canonical SHA-256 remained
   `cd706f9f4621efcf659fbe890d9a6a0ebfff97c3407095a6db5ea05044c1264d`
   before and after preparation. Membership, finite geometry, nonempty sets
   and origin–defender validity were checked without computing fields. There
   was no exclusion, clipping, population regeneration or membership repair.

10. **Prepared authority.** Commit
    `1ac194f2ecdefb90e7a6545a142ef408ce4e0a6f` froze
    [population authority](../outputs/continuous_occlusion_retry/authority/population.json)
    before analysis. Its SHA-256 is
    `08490684ab15b4d1485f01f8bde31b370fafa426ac4c5c8e1660f3fef3036c78`.
    The ignored geometry-only preparation is independently hash-bound.

11. **One empirical execution.** Analysis began only after clean committed
    prerequisites and an exclusive persistent marker. **One state completed**;
    verification stopped while processing the next state. Thus geometry and
    partial field calculations were exposed to the authorized process, even
    though no complete empirical aggregate package exists. Partial values
    remain ignored and were not used for interpretation. There was no second
    analysis execution, patch, tolerance change or corrective rerun.

12. **Exact observed blocker.** SciPy raised `IntegrationWarning` with:
    “Extremely bad integrand behavior occurs at some points of the integration
    interval.” The call chain was the new `evaluate_edge()` adapter, inherited
    `maximum_checks()`, inherited `adaptive_maximum()`, then scalar `quad` at
    the strict `1e-13` piecewise setting. Warnings were promoted to errors as
    required. The trace localizes the failed verification operation; it does
    **not** establish a discontinuous field, faulty quadrature, defective
    partition or a specific root cause. No further numerical probe was made.

13. **Failure closure.** The runner preserved the failure, private partial work
    and append-only ledger, then validated and closed a blocked package.
    [QC](../outputs/continuous_occlusion_retry/qc.json) explicitly records
    empirical access, `IntegrationWarning`, analysis stage and one completed
    state. [Manifest](../outputs/continuous_occlusion_retry/manifest.json)
    SHA-256 is
    `8a639f7a2b29a6aec026e176d80d8901b28839b0d2091dd0256751d77b6f70ed`.
    Publication validation checks existing hashes and never rebinds them.

14. **Artifact availability.** The final root contains the synthetic SVG,
    synthetic stress summary, QC and manifest. The manifest explicitly marks
    six files unavailable: `candidate_summary.json`,
    `structural_correspondence.csv`, `receiver_segment_divergence.csv`,
    `union_vs_max.csv`, `multi_edge_summary.csv`, and `match_summary.csv`.
    Pre-access and population records remain separate in `authority/`.
    Unavailable empirical files were not replaced by empty success artifacts.

15. **Field distributions.** Complete receiver-field, length-normalized
    segment-average and union-minus-maximum distributions are **unavailable**.
    No empirical candidate means, quantiles, extrema or field-strength claim
    is reported from the partial execution.

16. **Candidate correspondence.** All three candidate-pair difference
    distributions, Spearman summaries and agreement/reversal/tie-creation or
    removal counts are **unavailable**. Neither higher correlation nor
    guaranteed mathematical differences select a candidate.

17. **Receiver/corridor divergence.** Complete nearest-set intersection,
    equality and Jaccard summaries, opposing raw-distance pair counts and
    field-order alignment on those pairs are **unavailable**. Targets and
    behavioral labels were not joined to this geometry.

18. **Multi-edge and redundancy evidence.** Complete field-owner involvement,
    receiver-/segment-nearest overlap, top-1/2/3 fractional-strength bookkeeping,
    and across-state correlations with Session 13 descriptors are
    **unavailable**. Session 13's historical results remain valid within their
    original scope; this blocked execution does not extend them.

19. **Weighting and unavailable groups.** The implementation prospectively
    encodes equal match → state → edge weighting and eligible-pair averaging,
    inverse-ECDF quantiles, represented-match denominators and unavailable
    constant/empty comparisons. These passed synthetic tests. The required
    nine-match empirical aggregation did not complete, so no partial-match
    average or zero-filled unavailable group is presented as a result.

20. **Ten interpretation dimensions, without weights.** Interpretability:
    formulas specify different assumptions, with no empirical preference.
    Smoothness: synthetic verification passed, while empirical integration
    blocked. Carrier-relative behavior: declared directional construction
    only. Composability: synthetic union/maximum works, with empirical evidence
    unavailable. Multi-edge behavior: unavailable. Session 13 correspondence:
    unavailable. Equal-minimum discrimination: synthetic only, as below.
    Distinction from isotropic: formal/synthetic only. Software simplicity:
    internal adapter exists but empirical numerical acceptance remains blocked.
    Visual usefulness: synthetic full-frame QA passed; no practitioner evidence.
    These dimensions do not support A, B or C for the complete study.

21. **Equal-minimum synthetic stress.** The
    [stress summary](../outputs/continuous_occlusion_retry/synthetic_stress_summary.json)
    retains one-versus-three defender results under equal minima. Segment-union
    values for one/three defenders were approximately `0.151091/0.426817`
    (isotropic), `0.132928/0.458238` (expanding), and `0.105349/0.305836`
    (constant-width). This demonstrates formal multiplicity sensitivity under
    the frozen assumptions, not football importance or empirical prevalence.
    No empirical equal-minimum search was performed.

22. **Cover-shadow readiness.** **3 — needs revision / unresolved execution**,
    under the frozen protocol's separate readiness scale. No field is selected
    and no behavioral construct is frozen. The warning does not establish a
    deeper continuous-field defect or real cover-shadow validity.

23. **Access boundaries.** Only the authorized canonical development geometry
    was newly read. Targets are lexically skipped before analytical records;
    event keys support duplicate checks only and are absent from analysis.
    No fitted model, utility, option share, outcome, provider acquisition,
    reserved detail, withheld/pose data, xT or progression work was accessed or
    executed. No empirical diagram or temporal reconstruction was produced.

24. **Private records and failure enforcement.** Prepared states, mappings,
    partial calculations, traceback, stage markers and access records remain
    ignored. Actual failure closed the package; synthetic tests also exercise
    marker collision, immutable output hashes, failure publication and rejected
    repeat execution. The failed empirical command was not retried to test this.

25. **Tests.** Focused Session 14R: **23 run, 23 passed, 0 skipped**. Relevant
    Session 14-series suite: **139 run, 139 passed, 0 skipped**. Full active
    suite: **428 run, 425 passed, 3 retained skips**. The full suite was also
    repeated after empirical stop with the same passing totals. Tests use
    synthetic fixtures; their deliberate failure-case messages are not failed
    unit tests. Test success does not override the blocked empirical gate.

26. **Other validation.** Compilation, protocol/report links, publication
    success/failure schemas, finite/public-field checks, unchanged hashes,
    privacy scan, append-only log preservation, historical-file comparison,
    staged-content review and `git diff --check` were checked for closure.
    No generated result bytes were manually edited. CI and final remote
    synchronization are delivery checks reported in the final handoff after
    the closure commit; they cannot retroactively make this execution valid.

27. **Four-commit chronology.** Protocol → tested implementation with synthetic
    evidence and visual → prepared population authority → closed failure
    evidence, this report and append-only log. The three preceding full commit
    identities are listed above; the closure commit is the Git commit adding
    this report. Historical authorities were neither amended nor squashed.

28. **Delivery authority.** The final push preserves the release tag and checks
    local `HEAD`, tracking `origin/main` and live `main` equality, a clean tree,
    and CI status. Exact closure/ending HEAD and observed CI results belong to
    the accompanying delivery handoff, avoiding a self-referential commit hash
    in this document.

29. **Claim boundaries.** The claim ledger is unchanged. Accessibility remains
    **PROXY ONLY** and suppression **NOT SUPPORTABLE**. The fields establish no
    causality, interception, defender credit/value, tactical intent, pass
    success or best-pass judgment. No behavioral validation or claim promotion
    occurred.

30. **Interpretive limitations.** Origins are carrier-position proxies for
    ball origin; colocation is unverified. Shared-origin geometry, fixed design
    scales, persistent directional tails, union saturation and multiplicity
    assumptions remain unresolved modeling choices. Offline extrapolated
    tracking is not proven real-time availability. The ten formerly reserved
    matches are already spent and cannot be described as fresh validation.

31. **Exactly one next action.** Conduct a **separately governed bounded
    diagnosis of the independent-maximum integration warning**, preserving
    this failure and requiring new authority before any numerical repair or
    empirical retry. That diagnosis is recommended, not executed here.
