# Session 14R9E — Empirical execution under reconciled publication authority

## Decision

Session 14R9E closes **D — blocked or invalid / readiness 4**. The mandatory
pre-access CI checkpoint failed on both Python 3.11 and Python 3.13. The
empirical runner was therefore not invoked, no attempt or preparation marker
was created, and zero development states and edges were opened. Scientific
comparisons are unavailable.

The failure is localized to the new R9E test wiring. Three tests read ignored
private records from Sessions 14am and 14ao. Those records exist in the governed
local workspace but are absent from clean GitHub checkouts. Both CI Python jobs
therefore raised the same three `FileNotFoundError` errors. Distribution CI
passed. This does not invalidate R9's retained numerical acceptance, R9a's
publication-representation finding, the certificate authority, or any field
formula. It does show that R9E did not establish a portable committed authority
for its required pre-access checks.

## Frozen implementation

The prospective implementation remains committed and unexecuted against
development geometry. It composes the independent certificate verifier at the
adaptive-result boundary, derives runtime certificate requests from current
structure rather than retained identifiers, preserves ordinary and
micro-residual branches, and leaves whole-integral gates in force. Its
publication validator replays persisted journals and recomputes lifecycle and
exposure authority. These properties passed local focused tests, but the failed
cross-platform checkpoint prevents them from authorizing empirical access.

The public closure uses the frozen nineteen-file representation. Only `qc.json`
is available alongside `manifest.json`; all inherited authority, visual, and
scientific filenames are explicitly unavailable in this R9E namespace because
the runner never crossed its checkpoint. The R9a representation validator
accepts this blocked package. R9E journal replay is unavailable because no R9E
journal or lifecycle snapshot was initialized.

## Final handoff

1. **Starting HEAD:** `fc7a4da62a64e597acbeccd790f825b6fd35e26c`.
2. **Starting synchronization:** local, tracking, and live GitHub `main` matched before mutation.
3. **Release authority:** annotated `v0.1.0` remained at `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
4. **Protocol:** [`phase_14r9e_empirical_execution.md`](protocols/phase_14r9e_empirical_execution.md), commit `ecc84f2bf15ecaad6081c97a93728176c9cfbc6f`, SHA-256 `baaf970fadeef279e7370124cd05c269ea7a1b7186c4f9993544ad1433b5c67f`.
5. **Implementation checkpoint:** commit `c8a16dd3feb14cd62e89c22a060e46d01fbf6636`.
6. **Runner:** isolated commands are `preflight`, `run`, and `publication-check`; only `run` can authorize geometry.
7. **Certificate wiring:** implemented prospectively at the adaptive-result boundary with exact runtime-derived authority matching.
8. **Certificate scope:** a matching certificate can certify only its terminal contribution and cannot waive whole-integral disagreement.
9. **Ordinary pieces:** historical warning-free adaptive behavior is preserved.
10. **Micro-residual pieces:** inherited bounded residual intervals and routing are preserved.
11. **Production estimator:** the joint Simpson estimator, component order, ladder, and stopping rule are unchanged.
12. **Structural authority:** onset-aware certification, canonical partitions, tie endpoints, and degenerate-edge handling are unchanged.
13. **Publication wiring:** the prospective validator replays journals, recomputes snapshot hashes, derives counters, and compares private and public evidence.
14. **State exposure:** defined prospectively from unique successful state-materialization receipts.
15. **Edge accounting:** geometric edges remain distinct from candidate-field calls.
16. **Focused local tests:** 9 run, 9 passed, 0 skipped.
17. **Relevant local tests:** 48 run, 48 passed, 0 skipped; historical numerical replay was validation-only.
18. **Full local suite:** 955 run, 952 passed, 3 retained skips.
19. **Compilation:** passed for `src`, `scripts`, and `examples`.
20. **Preflight:** passed at committed implementation checkpoint `c8a16dd3feb14cd62e89c22a060e46d01fbf6636`.
21. **Distribution CI:** passed in GitHub Actions run `35096976539`.
22. **Python 3.11 CI:** 955 run, 949 passed, 3 skipped, 3 errors.
23. **Python 3.13 CI:** 955 run, 949 passed, 3 skipped, 3 errors.
24. **Exact CI blocker:** two tests could not read the ignored Session 14am selected-geometry record; one could not read the ignored Session 14ao warning-piece record.
25. **Failure semantics:** the checkpoint failure is an R9E fixture-authority defect, not a demonstrated numerical or field defect.
26. **Empirical runner invocation:** false.
27. **Markers:** no R9E empirical attempt, preparation, or execution marker was created.
28. **Empirical access:** 0 states and 0 edges opened; exposure is confirmed pre-access rather than inferred after a partial run.
29. **Scientific outputs:** unavailable; no partial comparison was inspected or interpreted.
30. **Public package:** [`manifest.json`](../outputs/continuous_occlusion_empirical_retry_r9e/manifest.json) binds the blocked nineteen-file schema; 17 non-QC members are unavailable.
31. **Publication result:** the R9a nineteen-file representation validator accepts the blocked package; R9E persisted-journal validation is unavailable because execution never initialized a journal.
32. **Scientific classification:** **D — blocked or invalid**.
33. **Readiness:** **4 — not justified** for another empirical execution under this unproven checkpoint.
34. **Claims and software:** README, claim ledger, dependencies, public API, release metadata, field formulas, numerical tolerances, and all historical artifacts are unchanged.
35. **Recommendation:** separately govern a bounded diagnosis of CI-portable retained-authority fixtures for the R9E pre-access tests.

## Limits

Accessibility remains **PROXY ONLY** and suppression remains **NOT
SUPPORTABLE**. R9E produced no empirical field evidence and cannot select a
directional candidate. The failed validation checkpoint must not be treated as
evidence against the representation itself.
