# Session 14R9V — Tie-boundary maximality and immutable failure-publication diagnosis

## Decision

Session 14R9V closes **numerical NF — insufficient evidence**, **publication
PA — prospective immutable-authority ownership repaired**, **execution valid**,
and **readiness 3**. The one retained constant-width invocation reproduced the
exact R9U `VerificationError: tie_boundary_not_maximal` through the unchanged
R9T route. The independent reference found both global-maximal and
third-defender-dominated portions of the proposed pair-equality interval, but
one terminal cell remained unresolved at depth 80. That incomplete interval
accounting cannot establish a named numerical mechanism or support a repair.

The publication branch independently confirmed the historical collision and
validated the prospective single-writer repair. Terminal closure owns the sole
immutable `linear_authority.json`; package writers receive a content-bound
descriptor and validators remain read-only. All nine success, failure and
ownership controls passed with one R9J review for each applicable closure.

The sole recommendation is to **separately govern an interval-only independent
bound for the one unresolved terminal cell, using the retained R9V evidence and
without another field invocation or empirical retry**. This recommendation was
not executed.

## Authority and chronology

The session began from synchronized
`d1fec793d63370f6e9ed687cbcba83519459b798`. The release tag remained at
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. The frozen protocol is
[Phase 14R9V](protocols/phase_14r9v_tie_boundary_and_publication_diagnosis.md),
SHA-256 `20710c4420dd988c5aafa68d16f487f90d7341cc86f1278ba2937f134c5302d5`,
committed as `ee6835c`.

The requested four-commit chronology is:

1. `ee6835c` — `research: freeze R9U blocker diagnosis`.
2. `f0a375b` — `research: diagnose tie-boundary maximality failure`.
3. `d65558e` — `fix: enforce immutable failure-publication ownership`.
4. The closure commit containing this report, the eleven public artifacts and
   the append-only log entry.

Checkpoint `d65558ea9e2613e6c76f6f17eeac26e0316752c3` passed distribution,
Python 3.11 and Python 3.13 in GitHub Actions run `35625352613`. The immutable
offline receipt has SHA-256
`4a65015c962b93e8b5286a02d86183f63b9288bc853abb6cac2cbd889bc25dcf`.
The governed runner performed no mandatory network request.

R9U has no historical manifest. R9V bound its protocol, report, implementation,
nine public files and retained private records directly. In particular, the
prepared file remained
`15a809060f0ba4a8cb3790011dab8008db3dad6e931c287ccb395f61ef1530b0`,
the 33,223-record journal remained
`ef3906cc5de9b1c15166720045fe4c5775a271866d0dfc8233786cd91e38ac26`,
and the retained numerical traceback remained
`421b9e65bd918992f01a442a5818b36d375b3a8d31bb91384eaf2d0f4cf2729e`.
No substitute R9U manifest was created.

## Numerical diagnosis

R9J's linear reviewer validated the retained journal once: canonical encoding,
continuous sequence, chain, terminal context, traceback binding and the durable
materialization receipt all passed. The prepared file was hashed in full, then
selection decoded only the authorized state and receiver. One state and one
edge were reopened; exposure is confirmed and not uncertain. No other retained
edge or candidate was evaluated.

The unchanged R9T invocation reproduced the exact exception at
`owner_certification` in 0.309 seconds. The non-mutating observer captured one
failing boundary frame from already evaluated values. At the positive inside
witness, the disputed pair had exact binary64 subtraction zero, the global
maximum was positive, and only one member of the pair belonged to the absolute
`1e-12` global maximum-owner block. This is the precise source meaning of
`tie_boundary_not_maximal`; it is not a test of whether the proposed equality
interval has maximal geometric extent.

The exact-input constant-width reference used the frozen `2^-256` outward
arithmetic and symbolic identity authority. It retained 81 cells after 80
subdivisions. Of these, 44 established third-defender dominance, 36 established
the tied pair as globally maximal, and one remained unresolved at maximum depth.
The pair was independently equal on resolved equality cells. Because the last
cell was unresolved and neighboring extension was not certified, the result
cannot distinguish a truncated interval, a mixed maximal/non-maximal interval
boundary, or another unresolved topology strongly enough to assign NA–NE.
Exact empirical coordinates, values, owners, coefficients and cell bounds are
private and bound by private-index SHA-256
`2abe5f916265275a94c157bd1a93c55f96d352863cbdce618a40418af3a2ebc6`.

All twelve provider-free controls passed. Their outcomes include expected
rejections for point ties, tolerance-only ties, third-defender dominance,
malformed ordering, tiny unresolved intervals and oscillatory binary64
interaction. They validate the diagnostic machinery but cannot fill the one
missing empirical bound.

## Publication diagnosis and repair

The retained publication traceback confirmed two owners for one immutable path.
`TerminalClosure.fail` successfully created `local/linear_authority.json`; the
R9U publisher then called `retain_authority()` and its second create-once write
raised `FileExistsError: immutable_record_exists`. The original numerical
failure and both tracebacks remained intact.

The new prospective adapter gives terminal closure sole write ownership. Its
descriptor binds both the persisted bytes and the verified authority record.
Package writers validate that descriptor before writing only package-owned
paths, and a second authority-owner attempt is rejected. The controls covered
success, pre-access failure, active numerical failure, publication
initialization failure, publisher failure, repeated closure, a pre-existing
authority, tampering and namespace collision. All nine passed. Applicable
closures performed exactly one R9J linear review, legacy replay was unreachable,
and publication failures remained separate from original failures. Historical
R9U and R9O modules were not changed or retroactively republished.

## Public evidence

The exact eleven-file package is under
[`outputs/continuous_occlusion_tie_boundary_publication_diagnosis`](../outputs/continuous_occlusion_tie_boundary_publication_diagnosis/manifest.json).
Its manifest binds all ten non-manifest files and the private index. Public
records contain only sanitized flags, counts, timings and hashes. Publication
checking reads persisted evidence, rejects false classifications and does not
replay the retained journal or evaluate the field.

The principal evidence is:

- `authority.json`: 33,223 records and all five authority checks passed.
- `numerical_reproduction.json`: exact failure, unchanged route, one captured
  frame and independently preserved original exception.
- `tie_invariant.json`: exact pair tie, positive global maximum, pair absent
  from the complete global owner block, and distinct tolerance semantics.
- `tie_maximality.json` and `independent_reference.json`: mixed established
  maximality/dominance with one unresolved depth-80 cell.
- `publication_collision.json`: the two-writer collision was confirmed.
- `publication_repair.json`: 9/9 controls passed and all seven repair flags are
  true.
- `qc.json`: NF / PA / execution valid / readiness 3, with one state and one
  edge reopened and no exposure uncertainty.

## Validation and boundaries

Focused R9V validation ran 15 tests: 15 passed, none skipped. Relevant R9J–R9U
selection ran 138 real tests successfully; an initial command named two stale,
nonexistent test modules, and the correct R9J/R9M modules were then run directly.
The exact checkpoint full suite ran 1,272 tests: 1,269 passed and three retained
skips. A clean tracked-only clone produced the same 1,272 / 1,269 / 3 result.
Compilation, schema/hash validation, finite JSON, privacy guards, historical
preservation, documentation links and `git diff --check` passed. The persisted
package checker reports eleven valid artifacts, NF, PA and readiness 3.

R9U remains D/readiness 4 as a historical execution. R9V changes no field,
tolerance, certificate, dependency, public API, claim ledger or scientific
result. No target, outcome, model, share, pose, protected detail or scientific
summary was accessed. No population retry, numerical repair, certificate or
behavioral validation was executed.
