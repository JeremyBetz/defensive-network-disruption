# Session 14R9Q — Expanding-field switch-equality diagnosis

2026-09-19. **NC — owner-certification interval logic is wrong; topology I —
attempted structural authority refuted; PA — prospective linear failure closure
proven; execution valid; operational readiness 3.** One authorized retained state
and edge were reopened. No population execution, numerical repair or scientific
interpretation occurred.

## Finding and limits

The single governed diagnosis reproduced exactly
`VerificationError: nonzero_inside_switch_equality_interval` at
`owner_certification` through the unchanged R9K expanding-field route. The
original exception and traceback were preserved before reference analysis.

The localization routine begins with opposite-sign witnesses and uses binary
searches whose predicates are assumed to be monotone. It then proposes an
equality region between the next float after `last` and the preceding float
before `first`. Its invariant tests exact zero of the binary64 pair subtraction
at both bounds and their **float-bit midpoint**. This is separate from the
absolute `1e-12` owner rule, and it does not prove mathematical interval equality.

The captured observations refute monotonicity of the before-sign predicate:
when the observed probes are ordered by coordinate, that predicate returns to
true after becoming false. The after-sign predicate was not refuted by the
retained observations. Ninety pair calls supplied 45 distinct probes. The
routine's own tested point had nonzero pair subtraction inside its proposed
equality region. Observation preserved the original failure; no function was
replaced and no tolerance was changed.

The independent exact-input expanding-field reference covered the entire
disputed region with one strictly signed cell. It established **zero mathematical
roots in that region**, with no unresolved cells. It rejected duplicate identity
and interval equality. This is topology I for the attempted equality authority,
not a claim about every switch on the edge or the field family generally.
Ten retained field-value comparisons passed the inherited scalar-oracle and
reference-relative `1e-12` checks. Those comparisons concern floating-point
implementation agreement; the signed reference enclosure supplies the separate
root-exclusion evidence.

These observations support **NC**, specifically the interval logic's reliance on
a non-monotone binary64 sign predicate. They do not establish NA: no mathematical
point root inside this attempted interval was proved. They also do not locate a
replacement root outside it. The failing canonical record was never returned and
remains **uncertified**. Downstream accepted partitions, integration comparisons
and scientific summaries prevented by the failure remain unavailable.

Exact geometry, empirical ordinals, coefficients, bounds, values, root records,
owners and traces remain private. The ignored private index is bound by SHA-256
`5652d6e4d57a92c0bf084d20d6cc1ee33708713f15bc5ff4c325b7a715f9c14a`.
Public [equality semantics](../outputs/continuous_occlusion_expanding_switch_equality_diagnosis/equality_semantics.json),
[root audit](../outputs/continuous_occlusion_expanding_switch_equality_diagnosis/root_switch_audit.json)
and [reference evidence](../outputs/continuous_occlusion_expanding_switch_equality_diagnosis/independent_reference.json)
contain only sanitized flags and counts.

## Authority, controls and execution

The frozen [protocol](protocols/phase_14r9q_expanding_switch_equality_diagnosis.md)
was committed before implementation. The registered R9P descriptor and unchanged
R9J reviewer validated the R9N journal once, deriving 31,161 records rather than
using a copied count as an access gate. The raw hash, chain, terminal failure,
context, traceback and selected materialization receipt matched. The immutable
review receipt exactly matched R9P's committed receipt SHA-256
`06061aeb8c6437da39e05503c8383e8c453713028c3bd75af8449d1218db82f1`.
Later publication checking validated this persisted receipt without replaying
the historical journal.

The complete retained prepared file was hash-checked without decoding other
rows. Restricted lexical selection materialized only the authorized carrier,
receiver and ordered defenders. The attempted-access record preceded selection;
the durable materialization receipt followed. Exposure is **1 retained state /
1 retained edge**, with no uncertain receipt and no other geometry access.

All eleven frozen controls passed before retained-edge access. Two used actual
expanding-field geometry; the remaining nine were explicitly analytic engineering
functions. Their independently established topology was kept separate from the
observed certification result. Three returned localization and eight correctly
preserved the specified rejection behavior. Expected rejection was not relabeled
successful certification. See the [control record](../outputs/continuous_occlusion_expanding_switch_equality_diagnosis/synthetic_controls.csv).
No fixture was tuned or newly executed after empirical exposure.

The reference used exact binary64 inputs, fixed `2^-256` outward arithmetic,
integer-certified square roots, the inherited degree-32 exponential enclosure,
and independently derived expanding-field values and derivatives. It made no
quadrature call and created no certificate. The initial reference cell was
sufficient; no bisection, precision increase or fallback was needed.

Governed wall time was 6.663 seconds: reproduction inclusive 5.267 seconds,
reference inclusive 0.024 seconds, and publication controls inclusive 0.051
seconds. Recorded exclusive numerical computation was 5.285 seconds; measured
evidence I/O was 0.014 seconds; waiting was zero; the remaining 1.364 seconds
was explicitly unattributed source review, controls, other I/O and overhead.
Inclusive measurements are not added to exclusive measurements. All frozen
deadlines were respected; no timeout, repair or second execution occurred.

## Prospective publication route

Static inspection confirms R9N called `Progress.failure()` before its intended
linear review. That historical method reconstructed snapshots through old replay.
R9Q did not execute that historical closure path or alter its bytes.

The existing R9O `TerminalClosure` instead synchronizes the R9M original traceback,
appends one terminal event using that hash, performs one R9J linear review, and
reuses its immutable authority in persisted package validation. All five governed
controls passed: success, pre-access failure, active numerical failure,
publication-initialization failure and publisher failure. Tripwires made legacy
failure/success, snapshot and replay routes fail immediately if reached. Each
closure used exactly one terminal event and one linear review. Traceback binding,
separate publication-failure preservation and repeated-execution rejection passed.

The prospective route earns **PA**; it does not retroactively publish R9N or
change R9O's historical PD classification. The nine-file R9Q package and private
index passed persisted schema, hash and decision validation. The checker derives
classification and exposure from stored observations; hash validity alone is not
treated as diagnostic success. See [publication controls](../outputs/continuous_occlusion_expanding_switch_equality_diagnosis/publication_route.json)
and the [manifest](../outputs/continuous_occlusion_expanding_switch_equality_diagnosis/manifest.json).

## Prospective numerical repair specification — not implemented

Bound a future repair to exact-sign switch localization: remove the unproved
assumption that rounded pair-sign predicates are monotone across the search
bracket. Require explicit separation of a certified mathematical crossing,
isolated binary64 zero observations, true functional interval equality and
unresolved numerical uncertainty. A true equality interval must have appropriate
independent identity or branch authority; a contiguous zero block must not be
inferred from non-monotone sign searches. Preserve the existing owner tolerance,
field formulas, canonical conventions, accepted historical structures,
integration gates and certificate scope. Unsupported cases must remain blocked.

Acceptance for that future work should include provider-free non-monotone
rounded-sign controls and the existing structural regressions, followed only by
separately authorized retained-edge verification. This is a specification, not
an implemented algorithm or new execution authority. No post-diagnostic fixture
was executed here.

## Validation and eighteen-item handoff

1. **Starting HEAD:** `da18745b1c02fdddf34e3e2d68cbf02892a890c1`, verified clean,
   synchronized locally and live before mutation.
2. **Ending HEAD:** the closure commit and final synchronized heads are reported
   in the delivery handoff after that commit and its CI complete.
3. **Retained-edge authority:** R9P's registered R9N descriptor, raw journal
   SHA-256 `ab5600ba8eea54092996bb2f4c613c76a5ba331c25c2c9d2b7bb65fa6e9555db`,
   the exact receipt above, and prepared SHA-256
   `15a809060f0ba4a8cb3790011dab8008db3dad6e931c287ccb395f61ef1530b0`.
   Exact empirical identity and selected geometry are private; access was 1/1.
4. **Reproduction:** exact unchanged-route exception and stage reproduced once;
   original failure and traceback preserved.
5. **Equality semantics:** exact binary64 subtraction at bounds and float-bit
   midpoint, distinct from owner tolerance and mathematical interval equality.
6. **Topology:** **I**, scoped to the refuted attempted equality region.
7. **Root/switch audit:** one signed reference cell, zero roots, zero unresolved
   cells; 90 pair calls, 45 unique probes; non-monotone before-sign predicate;
   10 scalar/reference checks passed. Canonical failure output remains uncertified.
8. **Synthetic controls:** **11/11 passed**, with independently established
   topology and observed acceptance or rejection recorded separately.
9. **Numerical classification:** **NC**; execution validity is **valid**.
10. **Prospective repair:** the bounded exact-sign localization specification
    above; no numerical implementation changed.
11. **Publication call path:** historical snapshot replay statically identified;
    existing direct-terminal-event/one-linear-review route verified unchanged.
12. **Publication classification:** **PA**, with all **5/5** controls passing.
13. **Tests/checks:** focused 27 run / 27 passed / 0 skipped; relevant 174 / 174 /
    0; exact-checkpoint full suite 1,152 / 1,149 / 3 retained skips; clean
    tracked-only full suite 1,152 / 1,149 / 3 retained skips; tracked focused
    27 / 27 / 0. Compilation, schemas/hashes, finite JSON, LF CSVs, privacy,
    links, historical-byte preservation, append-only log, staged review and
    diff checks passed. Numerical regressions were validation-only.
14. **CI:** checkpoint [run 35474804081](https://github.com/JeremyBetz/defensive-network-disruption/actions/runs/35474804081)
    passed distribution, Python 3.11 and Python 3.13 before access. The private
    create-once CI receipt hash is
    `206fc11a658fe56c5115cdae23a8199ea42dc50fc751868428702bdc4b02e8a1`.
    Final closure CI is reported in the delivery handoff.
15. **Commits:** protocol `131a3b74d9d4f32050601c6894ea0965d104fcd0`;
    tested tooling `2110aff3a9dcd235e422c81109a9acde9efa5a5f`; closure
    `research: close R9Q diagnosis`. Protocol SHA-256 is
    `71a7268cc5f8c793a964eb31dab576b68b87c45e787ff167678bee0ca7d74651`.
    No publication-repair commit was needed.
16. **Clean/sync and preservation:** final status follows closure CI. Historical
    source and artifacts, README, claim ledger, dependencies and root API are
    unchanged. The annotated release still targets
    `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
17. **Operational readiness:** **3**. Publication is proven, but the numerical
    blocker is diagnosed and unrepaired. R9N remains D/readiness 4, R9O
    NF/PD/readiness 4, and R9P A/readiness 1 for journal authority only. There
    is no new scientific result; accessibility remains PROXY ONLY and suppression
    NOT SUPPORTABLE. No target, outcome, model, share, provider, protected-data
    or partial scientific-summary access occurred.
18. **Sole recommendation:** separately govern a bounded repair of switch
    localization's non-monotone binary64 sign assumptions, with the existing
    prospective linear terminal adapter preserved. Do not resume Session 14R
    until that repair is validated under separate authority.
