# Session 14R9AB — Terminal-cell authority v2 acquisition

## Decision

Session 14R9AB closes **A — authority captured and fully validated / readiness
1 / execution valid**. One canonical version-2 `TerminalCellAuthority` was
created from the retained R9V selected-edge copy. It preserves two distinct
ordered pair references, all nine unique non-pair competitor references and the
frozen interval-scoped common inactive-branch lineage.

This is evidence acquisition only. No field value was evaluated, no terminal
bound was computed, no cell was refined, and no maximality or TA–TE
classification was made.

## Authority and chronology

The session began from synchronized commit
`2f487e7bfca338bff78deb845c757a3ad4927186`, with `v0.1.0` unchanged at
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`. The protocol was frozen in
`2d76f28` before implementation. The tested implementation and authority
bindings were committed as `cc35b8f`, pushed, and accepted by GitHub Actions
run `35816736154`: distribution, Python 3.11 and Python 3.13 all passed. The
create-once offline receipt for that exact commit has SHA-256
`0f59be41d2edd3baf5a30725c5b5ee92f337d62c68dce3347fa19b25d023f441`.

Only after those gates passed did the single governed acquisition begin. No
pre-access correction was needed and no retry occurred.

## Retained lineage and access

The acquisition validated the frozen R9V private index, boundary capture,
selected-edge hash and complete ordered 81-cell partition before reading the
selected-edge contents. The sole unresolved cell record retained the required
depth, equal-pair status and unresolved maximum status.

An access attempt was synchronized before materialization. The selected-edge
copy was then hash-checked and read once. This reopened one previously exposed
state and one previously exposed edge. It opened zero new population states and
zero new population edges, and exposure uncertainty is false. The prepared
population and provider source were not opened.

## Captured authority

The acquisition used the unchanged R9X leaf-partition derivation and exact
binary64-to-rational coefficient construction. It created one canonical schema
version 2 object through the R9AA constructor and compatibility loader.

The two ordered pair coefficient hashes are distinct. The retained R9V equal
cell and the historical R9Y distinct-hash failure therefore support the frozen
`common_inactive_branch` relation; the acquisition did not fall back to
symbolic identity or tolerance-only authority. The object contains eleven
unique coefficient records and nine unique competitor references. Private
cardinality evidence confirms that every non-pair defender was included before
hash-based deduplication.

The boundary, interval, structural-lineage and tolerance authorities were
independently hash-closed under their frozen definitions. Schema, canonical
fractions, ordered pair references, competitor completeness, source hashes,
tie provenance and overall provenance validated. Compatibility loading and
serialize-load-serialize produced byte-identical canonical content.

The private authority and its exact bounds, coefficients, pair details and
receipts remain ignored and are bound through private-index SHA-256
`25225bb78324b1092c0d91950ecc2e9021b336cb14753fe71c5db67e9d7ff023`.
The immutable linear closure authority has SHA-256
`43ddc2f3a71dc0385427b3f56fe26ed297744fe0d76293ce2092ceafbf2932bf`.

## Lifecycle, publication and privacy

The acquisition journal contained only lineage review, access attempt,
materialization, authority persistence, structural validation and terminal
success. Its single-pass reviewer ran once and its immutable result was reused
for private and public validation. It emitted no numerical, candidate or
scientific-completion events.

The seven-file public package matches the frozen inventory. Persisted hashes,
cross-file bindings, finite JSON, classification constraints and privacy guards
passed. Public files contain only hashes, versions, counts, flags, exposure and
decision fields. Exact geometry, bounds, coefficients, empirical identities and
receipts remain private. Historical R9V–R9AA evidence, the version-1 loader,
public APIs, dependencies, formulas, tolerances, certificates and the
unavailable R9Y traceback are unchanged.

## Validation

Focused R9AB and relevant R9AA/R9X/R9Y/R9Z/R9M/R9J validation ran 87 tests and
passed all 87. The active suite ran 1,316 tests: 1,313 passed and three retained
skips. A clean tracked-only checkout independently produced the same
1,313-pass/three-skip result. Compilation, schema and hash checks, finite JSON,
privacy guards, historical preservation, staged inspection and
`git diff --check` passed. The checkpoint CI jobs all passed before access.

Post-execution validation confirmed:

- one schema-version-2 authority captured;
- two distinct ordered pair references preserved;
- all non-pair competitors represented;
- interval-scoped tie lineage and provenance valid;
- canonical loader round trip byte-identical;
- one previously exposed state and edge reopened;
- zero new population exposure;
- zero candidate evaluations, field evaluations, refinements and maximality
  classifications; and
- no prohibited bound, classification, localization, owner-certification,
  prepared-population, provider or scientific-summary route invoked.

## Handoff

The result supports exactly one next action: **separately govern the
terminal-cell independent bound using only the persisted TerminalCellAuthority
v2, with zero empirical reopening.** That bound was not executed in R9AB.
