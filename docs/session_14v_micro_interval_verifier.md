# Session 14v — Independent-verifier micro-interval contract repair

Date: 2026-09-11
Status: **F — INVALID; retry readiness 4**

## Result

Session 14v did not establish retry authority. The protocol and tested repair
were committed before the one governed audit. The synthetic implementation tests
passed the frozen 108-case, 366-reference and 399-permutation authority, but the
governed audit stopped on the single authorized edge because the observed repair
record did not satisfy the prospectively frozen assertion of exactly one bounded
piece and eleven quadrature pieces. The preserved failure is
`GateFailure: micro_routing_changed`.

The stop is an execution-invalid result, not evidence against the bounded
residual idea or any occlusion representation. The execution marker remains
claimed, the audit was not rerun, no other state or edge was opened, and Session
14R remains D/readiness 3 and paused.

The failure-state publication check also failed because its validator required a
complete success output membership rather than accepting the generated
failure-state manifest. That is a second closure-path defect discovered after
the governed execution began. It was preserved rather than repaired.

The generic failure writer recorded zero opened states and edges in `qc.json`.
That counter is inaccurate: the preserved traceback shows the failure occurred
inside `edge_regression()` after the one authorized row and edge had been loaded.
This later report records the actual access as one state and one edge without
rewriting the generated QC bytes.

## Twenty-nine-item handoff

1. **Starting authority:** clean synchronized
   `cc8cb1a7c5c6706ef9db6e0c74d41c32f7fe54d5`.
2. **Release authority:** annotated `v0.1.0` still peeled to
   `f00690c05d8c1c6db308a65bd311c43f9a8ef2fa` before execution.
3. **Protocol checkpoint:** `8a706f6` froze the contract before implementation
   or the governed audit.
4. **Implementation checkpoint:** `fa49a0b` committed the tested isolated repair
   before authorized-edge access.
5. **Historical preservation:** no Session 14-series implementation, output,
   protocol or classification was rewritten.
6. **Field invariant:** the established maximum field bound is `[0,1]`.
7. **Residual budget:** `1e-12` globally per integral.
8. **Eligibility:** realized float64 width at most `1e-12 / N`, where `N` is the
   number of positive structural pieces.
9. **Eligible-piece representation:** a closed contribution interval from zero
   to width; no zero or midpoint value is asserted.
10. **Summation:** ordinary estimates and residual bounds use deterministic
    `math.fsum`, with outward-rounded aggregate interval endpoints.
11. **Structural partitions:** preserved; only the adaptive work list changes.
12. **Merging:** absent and unevaluated, so classifications B and C were not
    available.
13. **Ordinary pieces:** retain historical strict and repeat adaptive quadrature;
    their warnings remain blocking.
14. **Production estimator:** the joint Simpson implementation, component order,
    interval ladder and tolerances were not changed.
15. **Synthetic oracle coverage:** ordinary, onset-adjacent, switch-adjacent,
    endpoint, adjacent, multiple, threshold-neighbor, zero and constant-one
    cases were implemented and passed in pre-execution tests.
16. **Synthetic authority in tests:** 108 frozen cases, 366 references and 399
    mapped permutations passed before the governed audit.
17. **Governed synthetic completion:** unavailable in the closed result package;
    the runner did not persist counters before the later edge assertion failed.
18. **Authorized empirical scope:** only prepared row ordinal 1 and its already
    authorized constant-width receiver-ordinal-7 edge were loaded. The generated
    QC's zero counters are a known failure-writer defect superseded by the
    traceback-grounded access record in this report.
19. **Additional access:** zero additional states and zero additional edges.
20. **Recovered structure:** the assertion first confirmed 12 pieces, one switch
    and no tie intervals; the exact bounded/quadrature split was not retained.
21. **Known micro width:** the edge contained the previously authorized
    `3.3306690738754696e-15` onset-adjacent width before the routing assertion.
22. **Warning result:** unavailable because the execution stopped at the routing
    assertion before writing the edge result.
23. **Joint Simpson edge result:** unavailable in the governed package for the
    same reason; no new value is inferred from the pre-execution tests.
24. **Primary failure:** `micro_routing_changed`; the exact observed qualifying
    count is unavailable and is not reconstructed after the stop.
25. **Closure defect:** failure publication validation rejected the valid
    manifest shape because only `qc.json` existed and five success artifacts
    were explicitly unavailable.
26. **Public evidence:** [QC](../outputs/continuous_occlusion_micro_interval_verifier/qc.json)
    and the [manifest](../outputs/continuous_occlusion_micro_interval_verifier/manifest.json),
    whose SHA-256 is
    `5bd01c0ce27c7e6614331e65b02146bbcd960d67066e62df69f52d69a030fe2e`.
27. **Tests:** focused/relevant checks ran 38 and passed 38; the full active suite
    ran 473, passed 470 and retained 3 documented skips. Compilation passed.
28. **Failed checks:** the governed audit and failure-state publication check
    failed as described. Those failures prevent a PASS regardless of passing
    software tests.
29. **Classification and next action:** **F — INVALID**, readiness **4**. The one
    recommendation is a separately governed failure-state evidence repair that
    records the qualifying-piece count and widths before enforcing the routing
    assertion, and makes publication validation accept explicit failure
    membership, without opening another edge.

## Boundaries and interpretation

No model, coefficient, utility, option share, target, outcome, provider product,
protected or withheld data, pose, xT or progression material was accessed. No
partial Session 14R scientific summary was inspected. The one development edge
was used only under its existing numerical-diagnostic authority.

The claim ledger remains unchanged. Accessibility remains **PROXY ONLY** and
suppression remains **NOT SUPPORTABLE**. The invalid execution provides no
candidate selection, cover-shadow evidence or scientific result. A fresh
Session 14R retry is not authorized.
