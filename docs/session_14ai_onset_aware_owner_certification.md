# Session 14ai — onset-aware owner certification and numerical-failure publication

## Closed engineering evidence

**Switch A — PARTITION-AWARE OWNER CERTIFICATION REPAIRED.**
**Switch readiness 1. Publication P1 — repaired and fail-closed.**
These are bounded engineering conclusions, conditional on the final delivery CI
checks. They do not validate an empirical representation, a cover shadow or
suppression, and do not execute or authorize a full representation retry.

The single governed audit passed 12 topology oracles, the complete historical
108-case / 366-component / 399-permutation comparison, and 21 publication controls.
It then selectively opened only the already-authorized state 2 / receiver 8
`constant_width` edge. New certification produced a valid partition while keeping
both owner witnesses inside their structural region. Joint Simpson retained
4,096 intervals; independent maximum verification passed the unchanged
interval-aware gates. The saved nine-point historical neighborhood agreed exactly
with production values and within the inherited tolerance with the scalar oracle.
No scientific magnitude was interpreted.

[Closed manifest](../outputs/session14_onset_owner_repair/manifest.json): SHA-256
`f70be884941b908ad8638a5207df5a188f48cb8127223d115c8ebced710be28c`.
Detailed certified roots, owners, witnesses, onsets and numerical values remain
private, bound by SHA-256
`b48e302b4da06ddbbec6d319af8321f791e0d218a0959f24458e89ee228da789`.

## Thirty-eight-item handoff

1. **Starting HEAD:** `73f72b1d6b0556660594abc22b031a625a4e04c5`;
   clean local/tracking/live agreement was verified before mutation. The first
   sandboxed live check failed DNS; the permitted live check subsequently passed.
2. **Ending authority:** the closure commit introducing this report, following
   tested implementation `e18b006`. Its exact HEAD and synchronization are
   verified after push and returned in the delivery handoff; a report cannot
   embed the hash of its own containing commit.
3. **Protocol:** [Phase 14ai](protocols/phase_14ai_onset_aware_owner_certification.md),
   commit `0830505`, SHA-256
   `d66158f7f866dcfdf3d6144f445e8f62196ec44b95610b44cbe5d9169cbfa7a8`.
4. **Historical failure:** Session 14ah's detector before-probe remained after
   an `ell=0` onset; the wider certifier witness crossed it and observed a
   different tolerance-owner block, causing `switch_owner_semantics_changed`.
   The prior report's D/readiness 1 and publication blocker remain unchanged.
5. **Witness rule:** canonical structural limits constrain the symmetric
   `min(1e-7, quarter-left-spacing, quarter-right-spacing)` probes. Rounding
   fallback uses an available interior float; no witness expands across a limit.
   Pair sign order and tolerance-based owners remain separate checks.
6. **Coincidence:** exact onset/switch coincidence is recorded and rejected.
   No simultaneous activation/switch representation was introduced.
7. **Few-float handling:** one-ULP/no-interior cases reject explicitly. The frozen
   few-ULP oracle preserves its available probe region and rejects an owner-block
   mismatch. These expected negative outcomes are passing tests, not successful
   certifications of unsupported topology.
8. **Topology:** 12/12 passed, including ordinary, onset-adjacent, multiway,
   plateau, neighbor-switch and endpoint cases; three exercise expected rejection.
   See [topology oracles](../outputs/session14_onset_owner_repair/synthetic_topology_oracles.csv).
   Engineering functions test boundary semantics; they are not additional field
   candidates or fitted representations.
9. **Historical regression:** 108 cases, 366 component comparisons and 399
   permutations passed through the new path. Canonical records matched the
   unchanged historical implementation in the validation environment; accepted
   resolution and component order remained exact. Final values retained the
   64-epsilon contract. See [regression](../outputs/session14_onset_owner_repair/historical_switch_regression.csv).
10. **Empirical authority:** prepared zero-based row 2, receiver 8,
    `constant_width` only. Complete prepared/canonical file hashes were checked
    before selective decoding. Selected serialization and line identities matched
    the closed 14ah evidence. This is selected-edge equivalence, not a claim to
    reconstruct the complete row without exposing other receivers.
11. **Onset relationship:** historical certified-onset records and the raw switch
    location reproduced. Exact coordinates/bits are private. No earlier certified
    switch was claimed: 14ah returned none.
12. **Owners before:** certified under unchanged `1e-12` block membership inside
    the post-onset region; exact ordinals are private under the evidence hash above.
13. **Owners at:** checked explicitly at the new canonical switch coordinate;
    exact set private. A tolerance block is not proof of exact field equality.
14. **Owners after:** certified in the same bounded structural neighborhood;
    exact set private.
15. **Witness locations:** both satisfy strict interior ordering against their
    neighboring limits. Exact values remain private; no onset crossing occurred.
16. **Continuity:** the unchanged analytical continuity authority applies, and
    saved-neighborhood values matched the independent scalar oracle. Finite probes
    do not constitute an independent mathematical continuity proof or global
    stability guarantee near excluded origin/defender coincidence.
17. **Canonical partition:** returned successfully, with nine partition points
    and one certified switch. Canonical structure is distinct from raw solver
    provenance; preserved certified tie endpoints are not tolerance-collapsed.
18. **Joint Simpson:** finite, accepted at 4,096 intervals with the unchanged
    componentwise convergence rule. No estimator or field formula changed.
19. **Maximum verifier:** passed strict/repeat, onset-adaptive, direct-Simpson and
    joint-maximum interval-aware gates. Bounded residuals and warning handling
    remained unchanged. Historical split-Simpson tests remained synthetic-only.
    See [numerical health](../outputs/session14_onset_owner_repair/numerical_health.json).
20. **Switch classification:** A, on the complete bounded evidence above.
21. **Readiness:** 1 for a separately governed retry recommendation only, subject
    to the final delivery gates. This is not a guarantee for every empirical edge.
22. **Publication valid-control defect:** 14ah started ordinary `constant_width`
    work before preceding candidate calls, so replay raised `field_order`.
    Its checker then accepted the false oracle flag. Neither record was rewritten.
23. **Stage schema:** geometry → joint_simpson → envelope → owner_certification →
    partition_construction → routing → strict_piecewise → repeat_piecewise →
    onset_adaptive → direct_simpson → accepted. Legacy R6 events may omit the two
    adapter stages; geometry-to-accepted supports endpoint-only completion.
    Routing alone permits typed pieces/bounded/quadrature counts with exact sum
    agreement. Context and terminal ordering are validated chronologically.
24. **Valid controls:** correctly ordered normal failure and success packages
    both pass the actual persisted validator. Separate diagnostic work does not
    invent preceding candidate evaluations or full three-candidate edge completion.
25. **Negative controls:** all 18 reject as expected, including malformed stages,
    premature/postterminal events, missing context, false success, false acceptance
    gates, cross-file exposure/hash mismatches, absent trace/evidence and truncated
    journals. See [controls](../outputs/session14_onset_owner_repair/publication_negative_controls.csv).
26. **Failure closure:** independent emergency preservation also passed, bringing
    the publication-control total to 21. Actual runner tests retained simulated
    access and the original traceback after unexpected failure. Public integrity
    validation is explicitly distinguished from repair acceptance.
27. **Publication classification:** P1 for the new route. R6/14ah failure packages
    remain historical, not retroactively validated. See
    [publication contract](../outputs/session14_onset_owner_repair/publication_contract.json).
28. **Combined authority:** A/1/P1 supports the single conditional recommendation
    below only after successful CI and synchronized delivery.
29. **Real access:** one unique state, one unique receiver edge. Diagnostic work
    is separate from full candidate/edge/state completion. Actual journal-derived
    counts and snapshots are bound in [QC](../outputs/session14_onset_owner_repair/qc.json).
30. **Selection:** no second state or receiver geometry was materialized. Complete
    files were hashed as bytes; other receiver values were lexically skipped.
31. **R6 scientific products:** neither completed-state summaries nor the 28
    completed-edge scientific values were inspected. No scientific aggregate exists
    for this phase.
32. **Prohibited access:** no targets, outcomes, model coefficients, option shares,
    provider acquisition, reserved/withheld detail, pose, xT or progression access.
33. **Local validation:** final test totals are recorded in the validation section
    below. Historical files, claims, README, dependencies, release metadata and
    existing scientific artifacts remain unchanged.
34. **Python 3.11 CI:** required for the closure commit; this report is authored
    before that push. The verified run result and URL are returned in the final
    delivery handoff, not predicted here.
35. **Python 3.13 CI:** same required final-commit gate; no dedicated numerical
    diagnostic dispatch or tolerance change is introduced.
36. **Distribution CI:** same required gate. Package version and root exports
    are unchanged; no release or tag action occurs.
37. **Commits/push:** protocol `0830505`; tested implementation `e18b006`; one
    closure commit for this report, eleven public artifacts and append-only log.
    Final clean local/tracking/live equality and unchanged annotated release target
    are checked after push. CI is available through the
    [official workflow](https://github.com/JeremyBetz/defensive-network-disruption/actions/workflows/ci.yml).
38. **Single next action:** after A/1/P1 and delivery gates, **separately govern a
    fresh Session 14R empirical representation retry using the onset-aware
    owner-certification and repaired numerical-failure publication contracts.**
    Do not execute it automatically.

## Validation and qualifications

Final local validation: **676 run / 673 passed / 3 retained skips** (1,069.994 s).
Focused Session 14ai: **22 run / 22 passed / 0 skipped**. Relevant 14ah, 14af,
14ag and R6 tests: **50 run / 50 passed / 0 skipped**. Before governed access,
the earlier full run passed 675 tests with three skips; two additional targeted
closure checks passed, including the newly added failure regression. The final
full collection includes that added test.

Compilation, post-closure publication validation, JSON/CSV and hash checks,
new-document link resolution, staged public-artifact privacy review, historical
byte preservation, append-only research-log verification and diff checks passed.
Exactly eleven public files were reviewed; no private journal, geometry or trace
was staged. The release still peels to
`f00690c05d8c1c6db308a65bd311c43f9a8ef2fa`.
Final GitHub CI and remote synchronization are delivery checks performed after
this report's closure commit; their observed result belongs to the final handoff.

The governed audit executed once behind an exclusive marker. Outputs were closed
and hash-validated before classification. No post-exposure implementation repair
or rerun occurred. Before access, a static reporting defect was corrected: an
unexpected empirical-path failure must not retain a misleading default
`pre_access_gates` reason. A dedicated runner regression proved preservation of
simulated nonzero exposure, original traceback and unaccepted failure evidence.

The numerical adapter completes joint estimation, certification and independent
maximum verification before checking the saved historical neighborhood against the
scalar oracle. All checks passed on the same authorized edge; no additional edge
or scientific quantity was inspected. Newly certified roots are new prospective
authority, not previously successful 14ah records.

Finite-grid switch coverage, fixed field scales and persistent tails remain
limitations. This bounded result does not choose a directional representation,
validate real cover shadows, establish suppression or infer defender value.
Accessibility remains **PROXY ONLY**; suppression remains **NOT SUPPORTABLE**.
The claim ledger and all historical classifications remain unchanged.
