# Session 14R9Z — Pair-authority and failure-evidence diagnosis

## Decision

Session 14R9Z closes **A / readiness 1 / execution valid**. The metadata-only
review established both bounded defects from retained evidence and tracked
source. It reopened zero states and zero edges and performed no empirical or
numerical computation.

The sole recommendation is: **separately govern one bounded repair of the
TerminalCellAuthority pair-equality schema and failure-traceback capture, with
zero empirical reopening, then stop before another acquisition.** The repair
and another acquisition were not executed.

## Pair-authority diagnosis

R9X's frozen minimum schema requires two pair coefficient references. The
version-1 creation and loading paths instead both require those references to
have the same coefficient-authority hash. R9V's source authority can label a
pair equal on a branch when two distinct functions share a common inactive
branch, so exact symbolic identity is a narrower condition than the retained
pair-equality authority.

R9Y's retained exception establishes only that its two coefficient hashes
differed. Their values, the reason for the difference, tolerance-only
co-ownership and terminal-cell topology remain unavailable. Five provider-free
exact-rational controls distinguished symbolic identity, common inactive-branch
equality, unresolved distinct functions and tolerance-only proximity. They also
showed that collapsing two distinct references loses required authority.

The prospective repair contract preserves two ordered references, records an
independently validated cell-local equality authority, distinguishes symbolic
identity from common-branch equality and unresolved or tolerance-only
relations, and retains both functions unless symbolic identity is proven. R9Z
specified this contract but did not implement it.

## Failure-evidence diagnosis

The R9Y runner constructed blocked QC state in memory after authority
construction failed, then re-raised the exception. It did not initialize or use
the hardened failure controller and did not synchronize a traceback before
terminal exit. The retained record therefore preserves the exception type,
message and precise authority-construction stage while the exact historical
traceback remains unavailable. R9Z did not recreate it.

Synthetic exceptions at lineage review, materialization, coefficient
derivation, authority construction, validation and publication validated the
prospective contract: capture before package closure, one immutable traceback
hash, independent publication-failure evidence and blocked publication without
repeating acquisition. This contract also remains unimplemented.

## Authority, access and evidence

The protocol was frozen at commit `355038a`; tested tooling was committed at
`616dac3`. The exact checkpoint commit passed distribution, Python 3.11 and
Python 3.13 CI before its create-once offline receipt authorized the review.
The review read tracked source and only the allowlisted non-geometric R9Y
failure, lineage, materialization and private-index metadata. It did not open
the selected-edge contents, boundary geometry, prepared population, provider
data, coefficient values, field evaluators, bounds, refinement routines or
scientific summaries.

The eight public artifacts contain categorical findings, counts, flags and
hashes only. Their manifest binds a private index containing the exact source,
historical authority, CI and synthetic-failure evidence. Persisted publication
validation passed with zero reopened states, zero reopened edges and zero
empirical computations.

## Validation and chronology

Focused R9X/R9Y/R9Z validation passed 21/21 tests. Relevant R9V–R9Z validation
passed 42/42 tests. The active suite and a clean tracked-only clone each ran
**1,299 tests: 1,296 passed and 3 retained skips**. Compilation, deterministic
serialization, schema/hash checks, finite JSON, privacy and access tripwires,
documentation links, historical preservation, staged inspection and diff
checks passed. Checkpoint CI passed distribution, Python 3.11 and Python 3.13.

The preserved chronology is:

1. `research: freeze pair-authority failure diagnosis`
2. `research: diagnose pair-authority and failure-evidence contracts`
3. `research: close pair-authority failure diagnosis`

The [manifest](../outputs/continuous_occlusion_pair_authority_diagnosis/manifest.json)
hash-closes the eight-file public package.
