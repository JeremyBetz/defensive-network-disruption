# Session 14R9K — Onset-only comparator repair

2026-09-18. One governed retained-edge acceptance after synthetic and CI gates.

## Decision

**A — comparator repair valid / readiness 1.**

The prospective adapter replaces the unreliable onset-only comparison route
with an independent adaptive calculation over the complete certified structural
partition. It retains the historical publication stage `onset_adaptive` and
gate token `piecewise_unsplit` for schema compatibility. Their prospective
meaning is now a whole-integral comparison between the unchanged strict
piecewise interval and a separately evaluated structure-aware interval.

The comparator uses certified onsets, accepted switches and owner witnesses,
both endpoints of every certified tie enclosure, and the domain endpoints. It
requires exact agreement between those tagged records and the canonical
partition tuple. It makes one bounded adaptive call per positive non-micro
piece, applies the inherited micro-residual rule using the full positive-piece
count, and performs one outward-rounded interval aggregation. It does not call
the certificate-aware strict integration route, consume strict/repeat
contributions, or reuse production Simpson values. All warnings and exceptions
remain blocking, and no certificate route exists for this comparator.

Production estimation, strict/repeat verification, direct Simpson, joint
maximum, split-Simpson obligations, certificate behavior, tolerances, field
formulas, released API and dependencies are unchanged. The [protocol](protocols/phase_14r9k_onset_only_comparator_repair.md)
and [manifest](../outputs/continuous_occlusion_onset_comparator_repair/manifest.json)
bind the implementation and evidence.

## Synthetic and failure evidence

Nine analytic controls covered smooth, onset, certified-switch, close
onset/switch, exact-tie-endpoint, multiway-plateau, multiple-switch,
adjacent-float and micro-residual topology. Each independent reference check
passed the existing `1e-10` authority. The complete historical authority then
passed **108/108 cases, 366/366 components and 399/399 mapped permutations** with
exact canonical structure, component order and accepted resolutions and the
unchanged finite-component equivalence contract.

All eleven negative controls blocked: missing and additional boundaries,
malformed and overlapping partitions, incomplete domain coverage, invalid tie
endpoints, mismatched witnesses, nonfinite boundaries, a biased integrand, an
unrelated warning and a nonfinite integrand. Certificate matching remains
confined to the unchanged strict piecewise route and cannot rescue the repaired
comparator. Repair selection contains no edge identity or retained empirical
constant.

The frozen performance controls exercised six structural pieces and six
adaptive calls, with zero micro pieces. Operation counts establish one adaptive
call per ordinary structural piece and linear aggregation. Timings are supporting
evidence only and remained within the frozen budget.

## Retained-edge acceptance

Only previously exposed retained R9I state ordinal 4, receiver ordinal 7 was
materialized after its prepared authority and durable receipt were verified.
The three candidates ran once in canonical order. Isotropic passed at 2,048
intervals, expanding at 8,192 and constant-width at 2,048. Production and strict
piecewise evidence matched R9J's private hash-bound records; canonical structure
and authority hashes were unchanged. The repaired constant-width comparator
satisfied the existing gate for the intended structural reason: its partition
includes the certified switch that the historical onset-only comparator omitted.
No warning suppression, comparator certificate, new certificate or reference
recalculation occurred. Exact geometry, boundaries, owners, estimates and traces
remain private and hash-bound.

The governed execution reopened one already exposed state and one already
exposed edge. The receipt is resolved. It did not open another edge, inspect a
scientific summary or run the population study. This is numerical repair
evidence, not a scientific result or candidate selection.

## Linear publication and preservation

The retained 30,881-entry R9I journal was read once through the unchanged R9J
linear validator. Its raw hash, chain head, counters, active context and failure
identity matched the committed PA receipt: 7,227 states and 72,270 edges opened,
four states and 40 edges completed, 148 candidate calls started and 147
completed, and 72,230 edges unresolved. The persisted receipt was then validated
without rereading the journal. Synthetic success, blocked and failure packages
also passed the same authority. Historical prefix replay was not invoked at
scale, and no missing R9I package was regenerated.

Historical R9I remains D/readiness 4 and R9J remains NA/PA/readiness 3. Their
files and classifications are unchanged. R9K provides prospective comparator
and publication authority for a separately governed retry; it does not
retroactively complete either execution.

## Validation and preservation

Before retained access, focused R9K tests passed **13 run / 13 passed / 0
skipped**; relevant R9J/R9E/R9F/R9H tests passed **67/67/0**; broader Session 14
and R9 tests passed **91/91/0**; the full active suite passed **1,045 run / 1,042
passed / 3 retained skips**; and a clean tracked checkout passed **63/63/0**.
Compilation, schema/hash checks, privacy and access guards, documentation links,
historical-byte comparisons, staged review and diff checks passed. Checkpoint CI
on the exact implementation commit passed Python 3.11, Python 3.13 and
distribution. Final closure CI and synchronization are recorded in the delivery
after the closure commit.

README, claim ledger, dependencies, public API, release metadata, historical
protocols, implementations and artifacts remain unchanged. No scientific claim,
population run, certificate or tolerance changed.

## 25-item handoff map

1. **Starting HEAD:** `1dabec1b1c953004e3e662370c0a4973e1e1ba06`.
2. **Ending HEAD:** the closure commit containing this report; supplied in the
   final delivery after synchronization.
3. **Protocol:** commit `04ed70a`; its exact SHA-256 is bound in the manifest.
4. **Implementation:** commit `5c49c797f22b5089d57a29e35f4c26741e27cd31`.
5. **Comparator authority:** immutable, exact tagged-boundary and canonical-
   partition agreement; finite ordered complete `[0,1]` coverage required.
6. **Independence:** separate bounded-adaptive calls; no strict/repeat,
   certificate or production-value reuse.
7. **Tolerance:** adaptive `1e-13`, `limit=1000`; comparison gate remains
   interval distance `1e-10`.
8. **Micro residuals:** inherited `width <= 1e-12/N`, using the full positive
   structural-piece count.
9. **Aggregation:** one stable, conservative outward-rounded aggregate interval.
10. **Compatibility:** stage `onset_adaptive`; gate `piecewise_unsplit`.
11. **Other gates:** direct `1e-9`, joint maximum `1e-6`, repeat `1e-10` and
    historical split-Simpson obligations unchanged.
12. **Positive controls:** 9/9 passed.
13. **Negative controls:** 11/11 blocked as required.
14. **Historical cases:** 108/108 passed.
15. **Historical components:** 366/366 passed.
16. **Mapped permutations:** 399/399 passed.
17. **Retained authority:** prepared evidence and durable receipt passed; one
    retained state and edge reopened with no exposure uncertainty.
18. **Candidate replay:** all three passed; accepted resolutions 2,048, 8,192
    and 2,048 in canonical candidate order.
19. **Constant-width result:** repaired comparator passed; exact values private.
20. **Preservation:** production, piecewise, certificates, tolerances and
    canonical structure unchanged.
21. **Performance:** one adaptive call per ordinary piece and linear aggregation;
    budget passed.
22. **Publication:** one 30,881-record linear replay matched R9J PA; persisted
    receipt and synthetic packages validated without historical prefix replay.
23. **Classification:** **A — comparator repair valid**.
24. **Readiness:** **1**.
25. **Claims and release:** no claim-ledger or scientific result change; public
    API, dependencies and `v0.1.0` remain unchanged.

The sole recommendation is to **separately govern one fresh empirical
continuous-occlusion representation retry using the repaired comparator and
proven linear publication validator.**
